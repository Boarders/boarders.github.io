<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title></title>
    <link href="https://boarders.github.io/atom.xml" rel="self" type="application/rss+xml" />
  <updated>2026-03-21T23:32:SZ</updated>
  <author>
      <name>Callan Mcgill</name>
  </author>
  <id>https://boarders.github.io/</id>

  <entry>
      <title>The Halting Problem (part 1)</title>
      <link href="https://boarders.github.ioposts/halting1.html"/>
      <id>https://boarders.github.ioposts/halting1.html</id>
      <updated>2020-12-07T00:00:SZ</updated>
      <category term="Halting Problem"/>
      <category term="Haskell"/>
      <summary>Exploring the Halting problem in Haskell</summary>
      <content type="html"><![CDATA[<p>The halting problem states, informally, that there is no algorithm to
determine whether an <em>arbitrary</em> program (when provided with some
given input) will halt. Even for specific programs this can lead to
interesting unsolved questions. A well-known example is the <a
href="https://en.wikipedia.org/wiki/Collatz_conjecture">Collatz
conjecture</a>, which states, that the following function halts for all
inputs:</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="ot">collatz ::</span> <span class="dt">Natural</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a>collatz <span class="dv">1</span> <span class="ot">=</span> <span class="dt">True</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a>collatz n <span class="op">|</span> <span class="fu">even</span> n    <span class="ot">=</span> collatz (n <span class="ot">`div`</span> <span class="dv">2</span>)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>collatz n <span class="op">|</span> <span class="fu">otherwise</span> <span class="ot">=</span> collatz (<span class="dv">3</span> <span class="op">*</span> n <span class="op">+</span> <span class="dv">1</span>)</span></code></pre></div>
<pre class="terminal"><code>&gt; all id (fmap collatz [1..10000])
True</code></pre>
<p>Typically, the halting problem is formalised by first picking some
specific theory of computation, and then demonstrating, within that
theory, that no such halting algorithm can be written. Unfortunately,
the theory typically chosen is that of Turing machines. These are hard
to formalise (<a
href="https://en.wikipedia.org/wiki/Turing_machine#Formal_definition">Wikipedia</a>
informs me, for example, that “a (one-tape) Turing machine can be
formally defined as a [certain] 7-tuple”!) and don’t offer a
particularly good foundation for programming. A Turing machine, after
all, is <em>not</em> (and was never meant to be) a programming
language!</p>
<p>Instead let’s take an alternative approach: we will use the lambda
calculus as the basis for computation. The lambda calculus is both a
programming language in itself and the foundation of all other
functional languages. As a testament to this idea, we will first prove
halting for the lambda calculus and then see how the same argument looks
when transplanted to Haskell. Finally, in <a
href="https://boarders.github.io/posts/halting2.html">part two of this
post</a>, we will formalise the argument in Agda and fill in most of the
lingering details we brush aside here.</p>
<p>In the setting of the lambda calculus, a precise statement of halting
can be given thusly:</p>
<strong>Theorem (<span
class="math inline">\(\lambda\)</span>-Halting)</strong>: There does not
exist a <span class="math inline">\(\lambda\)</span>-term
<div style="display:none">
( )
</div>
<p><span class="math inline">\(\mathbf{HALT}\)</span> such that for any
given lambda term <span class="math inline">\(\mathrm{L}\)</span>, <span
class="math inline">\(\mathbf{HALT}\text{ } \mathrm{L}\)</span>
evaluates to <span class="math inline">\(\mathbf{true}\)</span> when
<span class="math inline">\(\mathrm{L}\)</span> terminates and <span
class="math inline">\(\mathbf{false}\)</span> otherwise.</p>
<p>Here, by termination, we mean what is otherwise called normalization.
A term is normalizing if there exists a finite sequence of <a
href="https://en.wikipedia.org/wiki/Lambda_calculus#%CE%B2-reduction_2"><span
class="math inline">\(\beta\)</span>-reductions</a> (performed anywhere
within the term) after which, the term contains no further <span
class="math inline">\(\beta\)</span>-redexes. A well-known result in the
study of the lambda calculus says that a term normalizes if and only if
it normalizes when we always pick the leftmost, outermost redex. This
gives us an algorithm that will <em>confirm</em> that a given term does
indeed terminate but will never disconfirm whether a term loops
indefinitely. In other words, there is a <a
href="https://en.wikipedia.org/wiki/Decidability_(logic)#Semidecidability">semi-decision
procedure</a> for halting.</p>
<p>Let’s turn to the proof of <span
class="math inline">\(\lambda\)</span>-Halting. The arguments here are
adapted from the introduction to the paper <a
href="https://www.sciencedirect.com/science/article/pii/0304397593900858">Computational
Foundations of Basic Recursive Function Theory</a> by Constable and
Smith. Essential to this argument is that lambda calculus allows us to
encode arbitrary recursive functions. Such recursion is performed by
fixed-point combinators. We will make use of perhaps the most well-known
fixed-point combinator (though any other would do for our purposes), the
<a
href="https://en.wikipedia.org/wiki/Fixed-point_combinator#Y_combinator"><span
class="math inline">\(\mathrm{Y}\)</span>-combinator</a>, defined as
follows:</p>
<p><span class="math display">\[
\def\Y{\mathrm{Y}}
\def\mr#1{\mathrm{#1}}
\def\la#1{\lambda \text{ }\mathrm{#1} \text{ }. \text{ }}
\def\ap#1#2{\mathrm{#1} \text{ }{\mathrm{#2}}}
\def\be{\mapsto_{\beta}}
\Y = \la{f} \ap{(\la{x} \ap{f}{(\ap{x}{x})})}{(\la{x}
\ap{f}{(\ap{x}{x})})}
  \]</span></p>
<p>The key property that fixed-point combinators satisfy is that for any
lambda term <span class="math inline">\(\rm{g}\)</span> we have that
<span class="math inline">\((\ap{\Y}{\rm{g}})\)</span> is a fixed point
for <span class="math inline">\(\rm{g}\)</span> in the sense that <span
class="math inline">\(\ap{g}{(\ap{\Y}{g})}\)</span> is <span
class="math inline">\(\beta\)</span>-equivalent to <span
class="math inline">\(\ap{\Y}{g}\)</span>. This is straightforward to
see as follows:</p>
<p><span class="math display">\[
     \begin{aligned}
      \ap{\Y}{g} \be \; &amp;\ap{(\la{x} \ap{g}{(\ap{x}{x})})}{(\la{x}
\ap{g}{(\ap{x}{x})})} \\
                 \be \; &amp;\ap{g}({\ap{(\la{x}
\ap{g}{(\ap{x}{x})})}{(\la{x} \ap{g}{(\ap{x}{x})})}}) \\
        \equiv_\beta \; &amp;\ap{g}{(\ap{Y}{g})}
    \end{aligned}\]</span></p>
<p>Fixed point combinators allow us to write recursive functions. For
example, supposing we have already <a
href="https://en.wikipedia.org/wiki/Church_encoding">encoded</a>
Booleans and Natural numbers as certain lambda terms. In a language
which allowed recursive definitions we could write: <span
class="math display">\[
  \mathrm{fact} (n) = \mathrm{if}\; n = 0\;
                        \mathrm{then} \; 1 \;
                        \mathrm{else} \; n * \;\mathrm{fact} (n - 1)
\]</span> In the lambda calculus we can’t refer to the variables we are
defining in their body and so instead we make use of a fixed point
combinator like so: <span class="math display">\[
  \mathrm{fact} = \Y ( \la{f} \la{n} \mathrm{if}\; n = 0\;
                        \mathrm{then} \; 1 \;
                        \mathrm{else} \; n * f \; (n - 1))
\]</span></p>
<p>Given this, and supposing we are given such a <span
class="math inline">\(\mathbf{HALT}\)</span> term as above, we then
introduce the following terms:</p>
<p><span class="math display">\[
     \begin{aligned}
        \bot  &amp; = \ap{Y}{(\la{x}x)} \\
        \rm{p}&amp; = {\la{f}\text{if $(\ap{\mathbf{HALT}}{f})$ then
$\bot$ else true}} \\
        \rm{d}&amp; = \ap{Y}{p}
  \def\betaStep{\mapsto_{\beta}}
      \end{aligned}  \]</span></p>
<p>The suggestively named <span class="math inline">\(\bot\)</span> is
an infinitely looping expression:</p>
<p><span class="math display">\[
     \begin{aligned}
      \ap{\Y}({\la{x}x}) \be \;
        &amp;  \ap{(\la{x} x)}(\ap{\Y}({\la{x}x})) \\
        \be \; &amp;{\ap{Y}{(\la{x}x})}
    \end{aligned}\]</span></p>
<p>The term <span class="math inline">\(\rm{p}\)</span> takes any
argument <span class="math inline">\(\mathrm{f}\)</span> and returns
true if the argument doesn’t terminate and otherwise loops forever. We
then define <span class="math inline">\(\mathrm{d}\)</span> as the fixed
point of this function. With ordinary recursion we would write this as
follows:</p>
<p><span class="math display">\[
  \rm{p} = \text{if } (\ap{\mathbf{HALT}}{\rm{p}}) \text{ then } \bot
\text{ else  true}
\]</span></p>
<p>Intuitively, we can see the problem with this term - if it halts then
it reduces to <span class="math inline">\(\bot\)</span> and if it
doesn’t halt then it reduces to <span
class="math inline">\(\mathrm{true}\)</span>. In slightly more
detail:</p>
<ul>
<li>if <span class="math inline">\(\ap{\mathbf{HALT}}{d}\)</span> is
<span class="math inline">\(\rm{true}\)</span> then: <span
class="math display">\[
\rm{d} \equiv_\beta \ap{p}{d} :\equiv
   \text{if $(\ap{\mathbf{HALT}}{d})$ then $\bot$ else true} \betaStep
\bot
\]</span> We therefore have that <span
class="math inline">\(\rm{d}\)</span> does not terminate and so <span
class="math inline">\(\ap{\mathbf{HALT}}{d}\)</span> must be <span
class="math inline">\(\rm{false}\)</span>.</li>
<li>Similarly if <span
class="math inline">\(\ap{\mathbf{HALT}}{d}\)</span> is <span
class="math inline">\(\rm{false}\)</span> then: <span
class="math display">\[
\rm{d} \equiv_\beta \ap{p}{d} :\equiv
   \text{if $(\ap{\mathbf{HALT}}{d})$ then $\bot$ else true} \betaStep
\text{true}
  \]</span> and so we get that <span
class="math inline">\(\rm{d}\)</span> terminates and so we get that
<span class="math inline">\(\ap{\mathbf{HALT}}{d}\)</span> is true.</li>
</ul>
<p>Let us see how easily these concepts translate to a language like
Haskell. Note that in Haskell all types are <em>partial</em> (using
Constable’s terminology). This means that every type is inhabited by
some non-terminating term; this is typically denoted <span
class="math inline">\(\bot\)</span> (analogous to the term considered
above). Reformulating the theorem with this in mind we get:</p>
<p><strong>Theorem (Haskell-Halting)</strong>: In Haskell there is no
function <span class="math inline">\(\mathbf{halt}\)</span> with the
following behaviour:</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="ot">halt ::</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Nat</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a>halt ⊥ <span class="ot">=</span> <span class="dv">0</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a>halt _ <span class="ot">=</span> <span class="dv">1</span></span></code></pre></div>
<p>Of course, this specification is not legal Haskell (and moreover we
are claiming that no function can have this behaviour). This formulation
may appear slightly different to the Halting problem insomuch as we are
only considering the (partial) natural numbers, but we can observe that
for any <code>f :: Nat -&gt; Nat</code> we can use
<code>halt (f n)</code> to determine if <code>f</code> halts on input
<code>n</code>, and so this would allow us to determine on which inputs
<code>f</code> terminates.</p>
<p>In order to mimic the argument above, let’s use a fixed point
function similar to <span class="math inline">\(\Y\)</span>, aptly named
<a
href="https://hackage.haskell.org/package/base-4.14.0.0/docs/Data-Function.html#v:fix"><span
class="math inline">\(\mathrm{fix}\)</span></a>:</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a><span class="ot">fix ::</span> (a <span class="ot">-&gt;</span> a) <span class="ot">-&gt;</span> a</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true" tabindex="-1"></a>fix f <span class="ot">=</span> <span class="kw">let</span> x <span class="ot">=</span> f x <span class="kw">in</span> x</span></code></pre></div>
<p>We note that <span class="math inline">\(\rm{fix}\)</span> satisfies
precisely the same fixed-point property as <span
class="math inline">\(\Y\)</span>: <span class="math display">\[
  \ap{f}{(\ap{fix}{f})} \equiv_\beta \ap{fix}{f}
  \]</span></p>
<p>As in the lambda calculus, <span
class="math inline">\(\mathrm{fix}\)</span> allows us to capture all
forms of recursion in a single function. For instance, if we define an
“openly recursive” sum function:</p>
<div class="sourceCode" id="cb5"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true" tabindex="-1"></a><span class="ot">{-# LANGUAGE LambdaCase #-}</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true" tabindex="-1"></a><span class="ot">sumR ::</span> <span class="kw">forall</span> n <span class="op">.</span> (<span class="dt">Num</span> n) <span class="ot">=&gt;</span> ([n] <span class="ot">-&gt;</span> n) <span class="ot">-&gt;</span> ([n] <span class="ot">-&gt;</span> n)</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true" tabindex="-1"></a>sumR recFn <span class="ot">=</span> \<span class="kw">case</span></span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true" tabindex="-1"></a>  []     <span class="ot">-&gt;</span> <span class="dv">0</span></span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true" tabindex="-1"></a>  (x<span class="op">:</span>xs) <span class="ot">-&gt;</span> x <span class="op">+</span> recFn xs</span></code></pre></div>
<p>Then taking <span class="math inline">\(\mathrm{fix}\)</span>
recovers the usual sum:</p>
<div class="sourceCode" id="cb6"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true" tabindex="-1"></a><span class="ot">mySum ::</span> <span class="kw">forall</span> n <span class="op">.</span> (<span class="dt">Num</span> n) <span class="ot">=&gt;</span> [n] <span class="ot">-&gt;</span> n</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true" tabindex="-1"></a>mySum <span class="ot">=</span> fix sumR</span></code></pre></div>
<pre class="shell"><code>&gt; mySum [1..100]
5050</code></pre>
<p>Recasting our problem terms from earlier we get:</p>
<div class="sourceCode" id="cb8"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true" tabindex="-1"></a><span class="ot">bottom ::</span> a</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true" tabindex="-1"></a>bottom <span class="ot">=</span> fix <span class="fu">id</span></span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true" tabindex="-1"></a><span class="ot">p ::</span> <span class="dt">Natural</span> <span class="ot">-&gt;</span> <span class="dt">Natural</span></span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true" tabindex="-1"></a>p n <span class="ot">=</span> <span class="kw">if</span> halt n <span class="op">==</span> <span class="dv">0</span> <span class="kw">then</span> <span class="dv">1</span> <span class="kw">else</span> bottom</span>
<span id="cb8-6"><a href="#cb8-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb8-7"><a href="#cb8-7" aria-hidden="true" tabindex="-1"></a><span class="ot">d ::</span> <span class="dt">Natural</span></span>
<span id="cb8-8"><a href="#cb8-8" aria-hidden="true" tabindex="-1"></a>d <span class="ot">=</span> fix p</span></code></pre></div>
<p>As above let us think about the value of <span
class="math inline">\((\ap{h}{problem})\)</span>:</p>
<div class="sourceCode" id="cb9"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true" tabindex="-1"></a>    d</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true" tabindex="-1"></a>  ≡ fix d</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true" tabindex="-1"></a>  ≡ p d</span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true" tabindex="-1"></a>  ≡ <span class="kw">if</span> halt d <span class="op">==</span> <span class="dv">0</span> <span class="kw">then</span> <span class="dv">1</span> <span class="kw">else</span> bottom</span></code></pre></div>
<p>As before:</p>
<ul>
<li>If <code>halt d</code> is <span class="math inline">\(0\)</span>
then it is 1.</li>
<li>If <code>halt d</code> is <span class="math inline">\(1\)</span>
then it is 0.</li>
</ul>
<p>We therefore conclude that no such function <code>h</code> can be
written in Haskell and so we cannot tell (in general) whether a term of
type <span class="math inline">\(\mathrm{Natural}\)</span> will diverge
or not. We should note that there is nothing special about <span
class="math inline">\(\mathrm{Natural}\)</span> in this argument and we
could have picked any type containing at least two distinct terms along
with some ability to compare terms for equality.</p>
<p>Thank you for reading! Hopefully this has demonstrated the unity of
ideas between the lambda calculus and a functional language like Haskell
and the naturality of studying computability theory from this
perspective. In the <a
href="https://boarders.github.io/posts/halting2.html">next post</a>, we
will formalise this argument in Agda.</p>
<p><i>With warmest thanks to Alixandra Prybyla and Sam Derbyshire for
their valuable feedback.</i></p>]]></content>
  </entry>
  <entry>
      <title>The Halting Problem (part 2)</title>
      <link href="https://boarders.github.ioposts/halting2.html"/>
      <id>https://boarders.github.ioposts/halting2.html</id>
      <updated>2020-12-07T00:00:SZ</updated>
      <category term="Halting Problem"/>
      <category term="Agda"/>
      <summary>Exploring the Halting problem in Agda</summary>
      <content type="html"><![CDATA[<p><a href="https://boarders.github.io/posts/halting1.html">Last
time</a>, we showed the undecidability of the halting problem using the
lambda calculus as our model of computation. In this post, we are going
to take that argument and formalise it in Agda. To begin, let’s grab
some imports:</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> <dfn>Halt</dfn> <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Data<span class="ot">.</span>List</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(</span>List<span class="ot">;</span> <dfn>[</dfn>]<span class="ot">;</span> <span class="ot">_</span>∷<span class="ot">_)</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Relation<span class="ot">.</span>Nullary</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a>   <span class="kw">using</span> <span class="ot">(</span>¬<span class="ot">_)</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Data<span class="ot">.</span>Empty</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(</span>⊥<span class="ot">;</span> ⊥-elim<span class="ot">)\</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Data<span class="ot">.</span>Product</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(</span>Σ-syntax<span class="ot">;</span> <span class="ot">_</span>×<span class="ot">_)</span> <span class="kw">renaming</span> <span class="ot">(_</span><dfn>,</dfn><span class="ot">_</span> <span class="kw">to</span> Sg<span class="ot">)</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Relation<span class="ot">.</span>Binary<span class="ot">.</span>PropositionalEquality</span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(_</span>≡<span class="ot">_;</span> refl<span class="ot">)</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Data<span class="ot">.</span>Sum</span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(_</span>⊎<span class="ot">_;</span> inj₁<span class="ot">;</span> inj₂<span class="ot">)</span></span></code></pre></div>
<p>For this development we will use a typed lambda calculus essentially
identical to <a
href="https://en.wikipedia.org/wiki/Programming_Computable_Functions">PCF</a>
(only with booleans instead of natural numbers), as this makes the
formalisation quite tidy. In order to get the basic semantics of the
language we will closely follow the <a
href="https://plfa.github.io/DeBruijn/">DeBruijn</a> chapter from the
fantastic <a href="https://plfa.github.io/">Programming Language
Foundations in Agda</a>.</p>
<p>Our language will be simply-typed, having only booleans, <span
class="math inline">\(\mathbb{B}\)</span>, and function types:</p>
<div class="sourceCode" id="cb2"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn><span class="dt">Type</span></dfn> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">𝔹</span>  <span class="ot">:</span>  <dfn><span class="dt">Type</span></dfn></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span> <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn> <span class="ot">→</span> <dfn><span class="dt">Type</span></dfn> <span class="ot">→</span> <dfn><span class="dt">Type</span></dfn></span></code></pre></div>
<p>We make use of intrinsically well-scoped, well-typed terms and so we
use ‘proof-carrying’ de-bruijn indices for variables. In this set-up
indices act both as an index into a typing context and as a
<em>proof</em> that the variable is well-typed in the current
context.</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- A typing context is represented as a list of types.</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a><dfn>Con</dfn> <span class="ot">:</span> <span class="dt">Set</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a><dfn>Con</dfn> <span class="ot">=</span> List <dfn><span class="dt">Type</span></dfn></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- We use <dfn>∙</dfn> for the empty context.</span></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true" tabindex="-1"></a><dfn>∙</dfn> <span class="ot">:</span> <dfn>Con</dfn></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true" tabindex="-1"></a><dfn>∙</dfn> <span class="ot">=</span> <dfn>[</dfn>]</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- _,_ extends contexts to the right as is typical in type theory</span></span>
<span id="cb3-10"><a href="#cb3-10" aria-hidden="true" tabindex="-1"></a><span class="co">-- and so we use a view the list in reverse order.</span></span>
<span id="cb3-11"><a href="#cb3-11" aria-hidden="true" tabindex="-1"></a><span class="kw">infixl</span> <span class="dv">6</span> <span class="ot">_</span><dfn>,</dfn><span class="ot">_</span></span>
<span id="cb3-12"><a href="#cb3-12" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>,</dfn><span class="ot">_</span> <span class="ot">:</span> <dfn>Con</dfn> <span class="ot">→</span> <dfn><span class="dt">Type</span></dfn> <span class="ot">→</span> <dfn>Con</dfn></span>
<span id="cb3-13"><a href="#cb3-13" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>,</dfn><span class="ot">_</span> con ty <span class="ot">=</span> ty ∷ con</span>
<span id="cb3-14"><a href="#cb3-14" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb3-15"><a href="#cb3-15" aria-hidden="true" tabindex="-1"></a><span class="co">-- A type for de-bruijn indices into a context. The index represents</span></span>
<span id="cb3-16"><a href="#cb3-16" aria-hidden="true" tabindex="-1"></a><span class="co">-- a pointer into a context along with a proof that the</span></span>
<span id="cb3-17"><a href="#cb3-17" aria-hidden="true" tabindex="-1"></a><span class="co">-- context contains the given type at that position.</span></span>
<span id="cb3-18"><a href="#cb3-18" aria-hidden="true" tabindex="-1"></a><span class="co">-- For example, given the context:</span></span>
<span id="cb3-19"><a href="#cb3-19" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb3-20"><a href="#cb3-20" aria-hidden="true" tabindex="-1"></a><span class="co">--   Γ = <span class="cn">𝔹</span><dfn>,</dfn> <span class="cn">𝔹</span> <span class="cn">⇒</span> <span class="cn">𝔹</span></span></span>
<span id="cb3-21"><a href="#cb3-21" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb3-22"><a href="#cb3-22" aria-hidden="true" tabindex="-1"></a><span class="co">-- we have:</span></span>
<span id="cb3-23"><a href="#cb3-23" aria-hidden="true" tabindex="-1"></a><span class="co">--    <span class="cn">z</span>   : <span class="cn">𝔹</span> <span class="cn">⇒</span> <span class="cn">𝔹</span> ∈ Γ</span></span>
<span id="cb3-24"><a href="#cb3-24" aria-hidden="true" tabindex="-1"></a><span class="co">--    <span class="cn">s</span> <span class="cn">z</span> : <span class="cn">𝔹</span> ∈ Γ</span></span>
<span id="cb3-25"><a href="#cb3-25" aria-hidden="true" tabindex="-1"></a><span class="kw">infix</span> <span class="dv">4</span> <span class="ot">_</span>∈<span class="ot">_</span></span>
<span id="cb3-26"><a href="#cb3-26" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>∈<span class="ot">_</span>  <span class="ot">(</span>t <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn><span class="ot">)</span> <span class="ot">:</span> <dfn>Con</dfn> <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb3-27"><a href="#cb3-27" aria-hidden="true" tabindex="-1"></a>  <span class="cn">z</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>ts<span class="ot">}</span> <span class="ot">→</span> t ∈ <span class="ot">(</span>t ∷ ts<span class="ot">)</span></span>
<span id="cb3-28"><a href="#cb3-28" aria-hidden="true" tabindex="-1"></a>  <span class="cn">s</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>r<span class="ot">}</span> <span class="ot">{</span>ts<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>t ∈ ts<span class="ot">)</span> <span class="ot">→</span> t ∈ <span class="ot">(</span>r ∷ ts<span class="ot">)</span></span></code></pre></div>
<p>We can now define the terms of our language. Here
<code>Expr Γ ty</code> denotes a term of type <code>ty</code> in the
typing context <code>Γ</code>:</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <span class="ot">:</span> <dfn>Con</dfn><span class="ot">)</span> <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn> <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">var</span>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn><span class="ot">}</span> <span class="ot">→</span> a ∈ Γ <span class="ot">→</span> <dfn>Expr</dfn> Γ a</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true" tabindex="-1"></a>  <span class="cn">app</span>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ <span class="ot">(</span>a <span class="cn">⇒</span> b<span class="ot">)</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ a <span class="ot">→</span> <dfn>Expr</dfn> Γ b</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true" tabindex="-1"></a>  <span class="cn">lam</span>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ, a<span class="ot">)</span> b <span class="ot">→</span> <dfn>Expr</dfn> Γ <span class="ot">(</span>a <span class="cn">⇒</span> b<span class="ot">)</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true" tabindex="-1"></a>  <span class="cn">tt</span>   <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ff</span>   <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span></span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true" tabindex="-1"></a>  <span class="cn">bool</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ a <span class="ot">→</span> <dfn>Expr</dfn> Γ a <span class="ot">→</span> <dfn>Expr</dfn> Γ a</span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true" tabindex="-1"></a>  <span class="cn">fix</span>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> a<span class="ot">)</span> a <span class="ot">→</span> <dfn>Expr</dfn> Γ a</span></code></pre></div>
<p>The names are largely self-explanatory but we observe that we use
<code>bool</code> for the conditional instead of <code>if</code>. It is
also worth noting that as <code>lam</code> and <code>fix</code> are
binding forms, they take arguments with contexts extended by the type of
the bound variable.</p>
<p>We give an identical approach to variable substitution as in <a
href="https://plfa.github.io/">PLFA</a> by first defining context
extension and variable renaming:</p>
<div class="sourceCode" id="cb5"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ <span class="ot">:</span> <dfn>Con</dfn><span class="ot">}</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn><span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> ty ∈ Δ<span class="ot">)</span></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty tyB <span class="ot">:</span> <dfn><span class="dt">Type</span></dfn><span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <dfn>,</dfn> tyB <span class="ot">→</span> ty ∈ Δ <dfn>,</dfn> tyB<span class="ot">)</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> ρ <span class="cn">z</span> <span class="ot">=</span> <span class="cn">z</span>             <span class="co">-- if it is the newly bound variable</span></span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true" tabindex="-1"></a>                        <span class="co">-- we simply return it.</span></span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> ρ <span class="ot">(</span><span class="cn">s</span> x<span class="ot">)</span> <span class="ot">=</span> <span class="cn">s</span> <span class="ot">(</span>ρ x<span class="ot">)</span>   <span class="co">-- otherwise we perform the substitution</span></span>
<span id="cb5-7"><a href="#cb5-7" aria-hidden="true" tabindex="-1"></a>                        <span class="co">-- and take successor.</span></span>
<span id="cb5-8"><a href="#cb5-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb5-9"><a href="#cb5-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- <dfn>rename</dfn> is defined by structural recursion, extending the renaming</span></span>
<span id="cb5-10"><a href="#cb5-10" aria-hidden="true" tabindex="-1"></a><span class="co">-- at binding sites and applying it to variables.</span></span>
<span id="cb5-11"><a href="#cb5-11" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ<span class="ot">}</span></span>
<span id="cb5-12"><a href="#cb5-12" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> ty ∈ Δ<span class="ot">)</span></span>
<span id="cb5-13"><a href="#cb5-13" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">→</span> <dfn>Expr</dfn> Δ ty<span class="ot">)</span></span>
<span id="cb5-14"><a href="#cb5-14" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="ot">(</span><span class="cn">var</span> x<span class="ot">)</span> <span class="ot">=</span> <span class="cn">var</span> <span class="ot">(</span>ρ x<span class="ot">)</span></span>
<span id="cb5-15"><a href="#cb5-15" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="ot">(</span><span class="cn">app</span> rator rand<span class="ot">)</span> <span class="ot">=</span> <span class="cn">app</span> <span class="ot">(</span><dfn>rename</dfn> ρ rator<span class="ot">)</span> <span class="ot">(</span><dfn>rename</dfn> ρ rand<span class="ot">)</span></span>
<span id="cb5-16"><a href="#cb5-16" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="ot">(</span><span class="cn">lam</span> body<span class="ot">)</span> <span class="ot">=</span> <span class="cn">lam</span> <span class="ot">(</span><dfn>rename</dfn> <span class="ot">(</span><dfn>ext</dfn> ρ<span class="ot">)</span> body<span class="ot">)</span></span>
<span id="cb5-17"><a href="#cb5-17" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="cn">tt</span> <span class="ot">=</span> <span class="cn">tt</span></span>
<span id="cb5-18"><a href="#cb5-18" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="cn">ff</span> <span class="ot">=</span> <span class="cn">ff</span></span>
<span id="cb5-19"><a href="#cb5-19" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="ot">(</span><span class="cn">bool</span> b th el<span class="ot">)</span> <span class="ot">=</span> <span class="cn">bool</span> <span class="ot">(</span><dfn>rename</dfn> ρ b<span class="ot">)</span> <span class="ot">(</span><dfn>rename</dfn> ρ th<span class="ot">)</span> <span class="ot">(</span><dfn>rename</dfn> ρ el<span class="ot">)</span></span>
<span id="cb5-20"><a href="#cb5-20" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> ρ <span class="ot">(</span><span class="cn">fix</span> body<span class="ot">)</span> <span class="ot">=</span> <span class="cn">fix</span> <span class="ot">(</span><dfn>rename</dfn> <span class="ot">(</span><dfn>ext</dfn> ρ<span class="ot">)</span> body<span class="ot">)</span></span></code></pre></div>
<p>Here, a variable renaming, <code>(∀ {ty} → ty ∈ Γ → ty ∈ Δ)</code>,
simply takes an index into one context of a particular type and gives an
index into a different context of the same type.</p>
<p>We extend this from variable renaming to arbitrary context
morphisms:</p>
<div class="sourceCode" id="cb6"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- extend a context morphism with a new bound variable.</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true" tabindex="-1"></a><dfn>exts</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ<span class="ot">}</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span>  ty ∈ Γ <span class="ot">→</span> <dfn>Expr</dfn> Δ ty<span class="ot">)</span></span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty tyB<span class="ot">}</span> <span class="ot">→</span> ty ∈ <span class="ot">(</span>Γ <dfn>,</dfn> tyB<span class="ot">)</span> <span class="ot">→</span> <dfn>Expr</dfn> <span class="ot">(</span>Δ <dfn>,</dfn> tyB<span class="ot">)</span> ty<span class="ot">)</span></span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true" tabindex="-1"></a><dfn>exts</dfn> ρ <span class="cn">z</span>     <span class="ot">=</span> <span class="cn">var</span> <span class="cn">z</span></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true" tabindex="-1"></a><dfn>exts</dfn> ρ <span class="ot">(</span><span class="cn">s</span> x<span class="ot">)</span> <span class="ot">=</span> <dfn>rename</dfn> <span class="cn">s</span> <span class="ot">(</span>ρ x<span class="ot">)</span></span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true" tabindex="-1"></a><span class="co">-- Perform structural recursion, extending the context morphism at</span></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- binding sites and applying it to variables.</span></span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ<span class="ot">}</span></span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> <dfn>Expr</dfn> Δ ty<span class="ot">)</span></span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">→</span> <dfn>Expr</dfn> Δ ty<span class="ot">)</span></span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="ot">(</span><span class="cn">var</span> x<span class="ot">)</span> <span class="ot">=</span> ρ x</span>
<span id="cb6-14"><a href="#cb6-14" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="ot">(</span><span class="cn">app</span> rator rand<span class="ot">)</span> <span class="ot">=</span> <span class="cn">app</span> <span class="ot">(</span><dfn>subst</dfn> ρ rator<span class="ot">)</span> <span class="ot">(</span><dfn>subst</dfn> ρ rand<span class="ot">)</span></span>
<span id="cb6-15"><a href="#cb6-15" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="ot">(</span><span class="cn">lam</span> body<span class="ot">)</span> <span class="ot">=</span> <span class="cn">lam</span> <span class="ot">(</span><dfn>subst</dfn> <span class="ot">(</span><dfn>exts</dfn> ρ<span class="ot">)</span> body<span class="ot">)</span></span>
<span id="cb6-16"><a href="#cb6-16" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="cn">tt</span> <span class="ot">=</span> <span class="cn">tt</span></span>
<span id="cb6-17"><a href="#cb6-17" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="cn">ff</span> <span class="ot">=</span> <span class="cn">ff</span></span>
<span id="cb6-18"><a href="#cb6-18" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="ot">(</span><span class="cn">bool</span> b th el<span class="ot">)</span> <span class="ot">=</span> <span class="cn">bool</span> <span class="ot">(</span><dfn>subst</dfn> ρ b<span class="ot">)</span> <span class="ot">(</span><dfn>subst</dfn> ρ th<span class="ot">)</span> <span class="ot">(</span><dfn>subst</dfn> ρ el<span class="ot">)</span></span>
<span id="cb6-19"><a href="#cb6-19" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> ρ <span class="ot">(</span><span class="cn">fix</span> body<span class="ot">)</span> <span class="ot">=</span> <span class="cn">fix</span> <span class="ot">(</span><dfn>subst</dfn> <span class="ot">(</span><dfn>exts</dfn> ρ<span class="ot">)</span> body<span class="ot">)</span></span></code></pre></div>
<p>This gives parallel substitution across an entire context
<code>Γ</code> to another context <code>Δ</code>. Intuitively we take an
open term with variables of type <code>Γ</code> and replace them with
terms of type <code>Δ</code>. To make this clearer let us give an
example:</p>
<pre><code>-- Our example context has three 𝔹 variables. 
con₁ : Con
con₁ = ∙ , 𝔹 , 𝔹 , 𝔹

-- Our open term is then, roughly, &quot;if x then y else z&quot; where
-- x : 𝔹, y : 𝔹 and z : 𝔹
term₁ : Expr con₁ 𝔹
term₁ = bool (var z) (var (s z)) (var (s (s z)))

-- For those variables we can then substitute closed terms:
sub₁ : ∀ {ty} → ty ∈ con₁ → Expr ∙ ty
sub₁ z = tt
sub₁ (s z) = ff
sub₁ (s (s z)) = tt

-- Our parallel substitution then works as expected:
subst-term₁ : subst sub₁ term₁ ≡ bool tt ff tt
subst-term₁ = refl</code></pre>
<p>From parallel substitution, it is easy for us to define ordinary
substitution of a single binding variable. We define the context
morphism that decrements all variables in <span
class="math inline">\(\Gamma\)</span> and returns the substituting term
for the initial variable:</p>
<div class="sourceCode" id="cb8"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true" tabindex="-1"></a><dfn>sub</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty tyB<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ tyB <span class="ot">→</span> ty ∈ <span class="ot">(</span>Γ <dfn>,</dfn> tyB<span class="ot">)</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true" tabindex="-1"></a><dfn>sub</dfn> term <span class="cn">z</span>   <span class="ot">=</span> term</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true" tabindex="-1"></a><dfn>sub</dfn> <span class="ot">_</span> <span class="ot">(</span><span class="cn">s</span> pf<span class="ot">)</span> <span class="ot">=</span> <span class="cn">var</span> pf</span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>[</dfn><span class="ot">_</span>] <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty tyB<span class="ot">}</span></span>
<span id="cb8-6"><a href="#cb8-6" aria-hidden="true" tabindex="-1"></a>        <span class="ot">→</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> tyB<span class="ot">)</span> ty</span>
<span id="cb8-7"><a href="#cb8-7" aria-hidden="true" tabindex="-1"></a>        <span class="ot">→</span> <dfn>Expr</dfn> Γ tyB</span>
<span id="cb8-8"><a href="#cb8-8" aria-hidden="true" tabindex="-1"></a>        <span class="ot">→</span> <dfn>Expr</dfn> Γ ty</span>
<span id="cb8-9"><a href="#cb8-9" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>[</dfn><span class="ot">_</span>] <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">{</span>tyB<span class="ot">}</span> body term <span class="ot">=</span> <dfn>subst</dfn> <span class="ot">{</span>Γ <dfn>,</dfn> tyB<span class="ot">}</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">(</span><dfn>sub</dfn> term<span class="ot">)</span> body</span></code></pre></div>
<p>Again, let’s give a simple example:</p>
<div class="sourceCode" id="cb9"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- This time our context has n : <span class="cn">𝔹</span> <span class="cn">⇒</span> <span class="cn">𝔹</span> and b : <span class="cn">𝔹</span></span></span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true" tabindex="-1"></a><dfn>con₂</dfn>  <span class="ot">:</span> <dfn>Con</dfn></span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true" tabindex="-1"></a><dfn>con₂</dfn>  <span class="ot">=</span> <dfn>∙</dfn> <dfn>,</dfn> <span class="cn">𝔹</span> <span class="cn">⇒</span> <span class="cn">𝔹</span> <dfn>,</dfn> <span class="cn">𝔹</span></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- Our open term is n applied to b:</span></span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true" tabindex="-1"></a><dfn>term₂</dfn>  <span class="ot">:</span> <dfn>Expr</dfn> <dfn>con₂</dfn> <span class="cn">𝔹</span></span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true" tabindex="-1"></a><dfn>term₂</dfn>  <span class="ot">=</span> <span class="cn">app</span> <span class="ot">(</span><span class="cn">var</span> <span class="ot">(</span><span class="cn">s</span> <span class="cn">z</span><span class="ot">))</span> <span class="ot">(</span><span class="cn">var</span> <span class="cn">z</span><span class="ot">)</span></span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb9-9"><a href="#cb9-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- Once we substitute b ↦ <span class="cn">tt</span> we have the smaller typing</span></span>
<span id="cb9-10"><a href="#cb9-10" aria-hidden="true" tabindex="-1"></a><span class="co">-- context: </span></span>
<span id="cb9-11"><a href="#cb9-11" aria-hidden="true" tabindex="-1"></a><span class="co">--   <dfn>∙</dfn> <dfn>,</dfn> <span class="cn">𝔹</span> <span class="cn">⇒</span> <span class="cn">𝔹</span> </span></span>
<span id="cb9-12"><a href="#cb9-12" aria-hidden="true" tabindex="-1"></a><span class="co">-- and so we decrement the n variable.</span></span>
<span id="cb9-13"><a href="#cb9-13" aria-hidden="true" tabindex="-1"></a><dfn>subst-term₂</dfn> <span class="ot">:</span> <dfn>term₂</dfn> <dfn>[</dfn> <span class="cn">tt</span> ] ≡ <span class="cn">app</span> <span class="ot">(</span><span class="cn">var</span> <span class="cn">z</span><span class="ot">)</span> <span class="cn">tt</span></span>
<span id="cb9-14"><a href="#cb9-14" aria-hidden="true" tabindex="-1"></a><dfn>subst-term₂</dfn> <span class="ot">=</span> refl</span></code></pre></div>
<p>Next we can define the values of our language - that is, those terms
which terminating programs return. Along with values we define the
small-step operational semantics of the language, showing how reduction
takes place:</p>
<div class="sourceCode" id="cb10"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>Value</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- lambdas are values</span></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true" tabindex="-1"></a>  V-↦ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ <span class="ot">}</span> <span class="ot">{</span>ty tyB<span class="ot">}</span> <span class="ot">{</span>body <span class="ot">:</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> tyB<span class="ot">)</span> ty<span class="ot">}</span></span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <dfn>Value</dfn> <span class="ot">(</span><span class="cn">lam</span> body<span class="ot">)</span></span>
<span id="cb10-5"><a href="#cb10-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- <span class="cn">tt</span> is a value</span></span>
<span id="cb10-6"><a href="#cb10-6" aria-hidden="true" tabindex="-1"></a>  <span class="cn">V-tt</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Value</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> <span class="cn">tt</span></span>
<span id="cb10-7"><a href="#cb10-7" aria-hidden="true" tabindex="-1"></a><span class="co">-- <span class="cn">ff</span> is a value</span></span>
<span id="cb10-8"><a href="#cb10-8" aria-hidden="true" tabindex="-1"></a>  <span class="cn">V-ff</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Value</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> <span class="cn">ff</span></span>
<span id="cb10-9"><a href="#cb10-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-10"><a href="#cb10-10" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>↓<span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">-&gt;</span> <dfn>Expr</dfn> Γ ty <span class="ot">-&gt;</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb10-11"><a href="#cb10-11" aria-hidden="true" tabindex="-1"></a><span class="co">-- reduce on the left in an application.</span></span>
<span id="cb10-12"><a href="#cb10-12" aria-hidden="true" tabindex="-1"></a>  l-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty tyB<span class="ot">}</span> <span class="ot">{</span>L L&#39; <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="ot">(</span>ty <span class="cn">⇒</span> tyB<span class="ot">)}</span> <span class="ot">{</span>R <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb10-13"><a href="#cb10-13" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> L ↓ L&#39;</span>
<span id="cb10-14"><a href="#cb10-14" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="cn">app</span> L R ↓ <span class="cn">app</span> L&#39; R</span>
<span id="cb10-15"><a href="#cb10-15" aria-hidden="true" tabindex="-1"></a><span class="co">-- reduce on the right so long as we have already reduced the left</span></span>
<span id="cb10-16"><a href="#cb10-16" aria-hidden="true" tabindex="-1"></a><span class="co">-- argument to a value.</span></span>
<span id="cb10-17"><a href="#cb10-17" aria-hidden="true" tabindex="-1"></a>  r-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty tyB<span class="ot">}</span> <span class="ot">{</span>VL <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="ot">(</span>ty <span class="cn">⇒</span> tyB<span class="ot">)}</span> <span class="ot">{</span> R R&#39; <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb10-18"><a href="#cb10-18" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="ot">(</span><dfn>Value</dfn> VL<span class="ot">)</span></span>
<span id="cb10-19"><a href="#cb10-19" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> R ↓ R&#39;</span>
<span id="cb10-20"><a href="#cb10-20" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="cn">app</span> VL R ↓ <span class="cn">app</span> VL R&#39;</span>
<span id="cb10-21"><a href="#cb10-21" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-22"><a href="#cb10-22" aria-hidden="true" tabindex="-1"></a><span class="co">-- Perform beta-reduction.</span></span>
<span id="cb10-23"><a href="#cb10-23" aria-hidden="true" tabindex="-1"></a>  β-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty tyB<span class="ot">}</span> <span class="ot">{</span>N <span class="ot">:</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> tyB<span class="ot">)</span> ty<span class="ot">}</span> <span class="ot">{</span>V <span class="ot">:</span> <dfn>Expr</dfn> Γ tyB<span class="ot">}</span></span>
<span id="cb10-24"><a href="#cb10-24" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span><span class="cn">lam</span> N<span class="ot">)</span> V<span class="ot">)</span> ↓ <span class="ot">(</span>N <dfn>[</dfn> V ]<span class="ot">)</span></span>
<span id="cb10-25"><a href="#cb10-25" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-26"><a href="#cb10-26" aria-hidden="true" tabindex="-1"></a><span class="co">-- reduce boolean in a conditional.</span></span>
<span id="cb10-27"><a href="#cb10-27" aria-hidden="true" tabindex="-1"></a>  if-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">{</span>b b&#39; <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span><span class="ot">}</span> <span class="ot">{</span>th el <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb10-28"><a href="#cb10-28" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> b ↓ b&#39;</span>
<span id="cb10-29"><a href="#cb10-29" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="ot">(</span><span class="cn">bool</span> b th el<span class="ot">)</span> ↓ <span class="ot">(</span><span class="cn">bool</span> b&#39; th el<span class="ot">)</span></span>
<span id="cb10-30"><a href="#cb10-30" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-31"><a href="#cb10-31" aria-hidden="true" tabindex="-1"></a><span class="co">-- reduce conditional to true branch.</span></span>
<span id="cb10-32"><a href="#cb10-32" aria-hidden="true" tabindex="-1"></a>  if-tt-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">{</span>th el <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb10-33"><a href="#cb10-33" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="ot">(</span><span class="cn">bool</span> <span class="cn">tt</span> th el<span class="ot">)</span> ↓ th</span>
<span id="cb10-34"><a href="#cb10-34" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-35"><a href="#cb10-35" aria-hidden="true" tabindex="-1"></a><span class="co">-- reduce conditional to false branch.</span></span>
<span id="cb10-36"><a href="#cb10-36" aria-hidden="true" tabindex="-1"></a>  if-ff-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">{</span>th el <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb10-37"><a href="#cb10-37" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="ot">(</span><span class="cn">bool</span> <span class="cn">ff</span> th el<span class="ot">)</span> ↓ el</span>
<span id="cb10-38"><a href="#cb10-38" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb10-39"><a href="#cb10-39" aria-hidden="true" tabindex="-1"></a><span class="co">-- recursively substitute <span class="cn">fix</span> expression.</span></span>
<span id="cb10-40"><a href="#cb10-40" aria-hidden="true" tabindex="-1"></a>  fix-↓ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>expr <span class="ot">:</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> ty<span class="ot">)</span> ty<span class="ot">}</span></span>
<span id="cb10-41"><a href="#cb10-41" aria-hidden="true" tabindex="-1"></a>    <span class="ot">-&gt;</span> <span class="cn">fix</span> expr ↓ <span class="ot">(</span>expr <dfn>[</dfn> <span class="cn">fix</span> expr ]<span class="ot">)</span></span></code></pre></div>
<p>We use (something close to) a call-by-name semantics and so don’t
necessarily reduce arguments to values before performing <span
class="math inline">\(\beta\)</span>-reduction. We also fix a leftmost
evaluation order for applications reducing the left argument to a value
before the right argument. We extend this relation to its reflective,
transitive closure - the stepping relation - that one expression
reduces, in some number of steps, to another.</p>
<div class="sourceCode" id="cb11"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>⇓<span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">→</span> <dfn>Expr</dfn> Γ ty <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- reflexivity</span></span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span>∎ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">(</span>M <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">)</span></span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> M ⇓ M</span>
<span id="cb11-5"><a href="#cb11-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- transitivity</span></span>
<span id="cb11-6"><a href="#cb11-6" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span><span class="cn">→⟨</span><span class="ot">_</span>⟩<span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">(</span>L <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">)</span> <span class="ot">{</span>M N <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb11-7"><a href="#cb11-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> L ↓ M</span>
<span id="cb11-8"><a href="#cb11-8" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> M ⇓ N</span>
<span id="cb11-9"><a href="#cb11-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> L ⇓ N</span></code></pre></div>
<p>For later use we note, as one might expect, that values, as we have
defined them, only reduce to themselves:</p>
<div class="sourceCode" id="cb12"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true" tabindex="-1"></a>⇓-val <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ a<span class="ot">}</span> <span class="ot">{</span>e e&#39; <span class="ot">:</span> <dfn>Expr</dfn> Γ a<span class="ot">}</span> <span class="ot">→</span> <dfn>Value</dfn> e <span class="ot">→</span> e ⇓ e&#39; <span class="ot">→</span> e&#39; ≡ e</span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true" tabindex="-1"></a>⇓-val val   <span class="ot">(_</span> ∎<span class="ot">)</span> <span class="ot">=</span> refl</span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- In the cases where we have a transitive step Agda will</span></span>
<span id="cb12-4"><a href="#cb12-4" aria-hidden="true" tabindex="-1"></a><span class="co">-- produce an absurd pattern as none of the reduction</span></span>
<span id="cb12-5"><a href="#cb12-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- rules apply.</span></span>
<span id="cb12-6"><a href="#cb12-6" aria-hidden="true" tabindex="-1"></a>⇓-val V-↦  <span class="ot">(_</span> <span class="cn">→⟨</span> <span class="ot">()</span> ⟩ st<span class="ot">)</span></span>
<span id="cb12-7"><a href="#cb12-7" aria-hidden="true" tabindex="-1"></a>⇓-val <span class="cn">V-tt</span> <span class="ot">(_</span> <span class="cn">→⟨</span> <span class="ot">()</span> ⟩ st<span class="ot">)</span></span>
<span id="cb12-8"><a href="#cb12-8" aria-hidden="true" tabindex="-1"></a>⇓-val <span class="cn">V-ff</span> <span class="ot">(_</span> <span class="cn">→⟨</span> <span class="ot">()</span> ⟩ st<span class="ot">)</span></span></code></pre></div>
<p>Now let us think about our <span
class="math inline">\(\mathbf{HALT}\)</span> term from last time. We
define the notion of halting by stating the existence of both a value
and a series of reduction steps to that value. We encode that as
follows:</p>
<div class="sourceCode" id="cb13"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>Halt</dfn> <span class="ot">{</span>Γ a<span class="ot">}</span> <span class="ot">(</span>e <span class="ot">:</span>  <dfn>Expr</dfn> Γ a<span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">halts</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>v <span class="ot">:</span> <dfn>Expr</dfn> Γ a<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span><dfn>Value</dfn> v<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>e ⇓ v<span class="ot">)</span> <span class="ot">→</span> <dfn>Halt</dfn> e</span></code></pre></div>
<p>We are now in the position to postulate the existence of a
<code>halt</code> function with the expected properties:</p>
<pre><code>postulate
  halt     : ∀ {Γ} {a} → Expr Γ (a ⇒ 𝔹)
-- halt is a closed term
  halt-sub :
    ∀ {Γ Δ} {a}
    → (ρ : ∀ {ty} → ty ∈ Γ → Expr Δ ty)
    → subst {Γ} {Δ} ρ (halt {Γ} {a}) ≡ halt {Δ}
  halt-ret :
    ∀ {Γ} {ty}
    (e : Expr Γ ty) → app halt e ⇓ tt ⊎ app halt e ⇓ ff
  halt-tt  :
    ∀ {Γ ty}
    (e : Expr Γ ty) → app halt e ⇓ tt → Halt e
  halt-ff :
    ∀ {Γ ty}
    (e : Expr Γ ty) → app halt e ⇓ ff → ¬ Halt e</code></pre>
<p>We assume we have a function <code>halt</code> that takes an argument
of any type (in the meta-language, agda, since our language doesn’t
itself have polymorphism) and returns a bool. That this function applies
to terms of any type is irrelevant to the argument we give here. This
would work just as well were we to simply use <code>𝔹</code>.</p>
<p>We also assume that it is decidable that halt always returns
<code>tt</code> or <code>ff</code>. Furthermore, the terms
<code>halt-tt</code> and <code>halt-ff</code> encode our assumptions
regarding applying the <code>halt</code> function - if it returns
<code>tt</code>, then the term is normalizing and conversely, if it
returns <code>ff</code>, then it is non-normalizing.</p>
<p>We can now define our three terms from last time:</p>
<div class="sourceCode" id="cb15"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb15-1"><a href="#cb15-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- Since <span class="cn">fix</span> takes a binding term we write <span class="cn">fix</span> (<span class="cn">var</span> <span class="cn">z</span>)</span></span>
<span id="cb15-2"><a href="#cb15-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- instead of the term <span class="cn">fix</span> (<span class="cn">lam</span> <span class="cn">z</span>) we used last time.</span></span>
<span id="cb15-3"><a href="#cb15-3" aria-hidden="true" tabindex="-1"></a><dfn>bot</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>ty Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ ty</span>
<span id="cb15-4"><a href="#cb15-4" aria-hidden="true" tabindex="-1"></a><dfn>bot</dfn> <span class="ot">=</span> <span class="cn">fix</span> <span class="ot">(</span><span class="cn">var</span> <span class="cn">z</span><span class="ot">)</span></span>
<span id="cb15-5"><a href="#cb15-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb15-6"><a href="#cb15-6" aria-hidden="true" tabindex="-1"></a><dfn>problem</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> <span class="ot">(</span>Γ <dfn>,</dfn> <span class="cn">𝔹</span><span class="ot">)</span> <span class="cn">𝔹</span></span>
<span id="cb15-7"><a href="#cb15-7" aria-hidden="true" tabindex="-1"></a><dfn>problem</dfn> <span class="ot">=</span> <span class="cn">bool</span> <span class="ot">(</span><span class="cn">app</span> halt <span class="ot">(</span><span class="cn">var</span> <span class="cn">z</span><span class="ot">))</span> <dfn>bot</dfn> <span class="cn">tt</span></span>
<span id="cb15-8"><a href="#cb15-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb15-9"><a href="#cb15-9" aria-hidden="true" tabindex="-1"></a><dfn>fix-problem</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span></span>
<span id="cb15-10"><a href="#cb15-10" aria-hidden="true" tabindex="-1"></a><dfn>fix-problem</dfn> <span class="ot">=</span> <span class="cn">fix</span> <dfn>problem</dfn></span></code></pre></div>
<p>At this point we would like to use <code>halt-ret</code> on
<code>fix-problem</code> but upon reflection we see that last time’s
argument was a little loose. We showed that if
<code>halt fix-problem</code> is <code>true</code> then
<code>fix-problem</code> reduces to <code>bot</code> but this actually
isn’t enough, by itself, to get a contradiction. What we need to know is
that if a term reduces to <code>bot</code> then no other reduction
sequence halts.</p>
<p>Stated as a general lemma:</p>
<div class="sourceCode" id="cb16"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb16-1"><a href="#cb16-1" aria-hidden="true" tabindex="-1"></a><dfn>halt-⊥</dfn> <span class="ot">:</span></span>
<span id="cb16-2"><a href="#cb16-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>e1 e2 <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb16-3"><a href="#cb16-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> e1 ⇓ e2 <span class="ot">→</span> ¬ <span class="ot">(</span><dfn>Halt</dfn> e2<span class="ot">)</span> <span class="ot">→</span> ¬ <span class="ot">(</span><dfn>Halt</dfn> e1<span class="ot">)</span></span></code></pre></div>
<p>In order to prove this, we would like to use the following: if
<code>e1</code> steps to a value then there exists some reduction
sequence where <code>e2</code> also steps to that same value. This
follows from the more general property of confluence:</p>
<p><strong>Definition [Confluence]</strong>: A reduction relation <span
class="math inline">\(\rightarrow\)</span> on a set <span
class="math inline">\(\mathcal{T}\)</span> is confluent if for any <span
class="math inline">\(e1, e2, e3 \in \mathcal{T}\)</span> there exists
an <span class="math inline">\(e4\)</span> such that the following
diagram commutes: <span class="math display">\[
    \require{AMScd}
    \begin{CD}
    e1     @&gt;&gt;&gt;  e2\\
    @VVV        @VV*V\\
    e3     @&gt;*&gt;&gt;  e4
    \end{CD}
\]</span> Here <span class="math inline">\(\xrightarrow{*}\)</span>
denotes the reflective, transitive closure of <span
class="math inline">\(\rightarrow\)</span>.</p>
<p>It would be outside of the scope of this post to prove confluence but
it is a well-known result (and one which I will blog about in the
future) that the lambda calculus is confluent. As such, we allow
ourselves to assume it as a postulate:</p>
<div class="sourceCode" id="cb17"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb17-1"><a href="#cb17-1" aria-hidden="true" tabindex="-1"></a><span class="kw">postulate</span></span>
<span id="cb17-2"><a href="#cb17-2" aria-hidden="true" tabindex="-1"></a>  confluence</span>
<span id="cb17-3"><a href="#cb17-3" aria-hidden="true" tabindex="-1"></a>    <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>a<span class="ot">}</span></span>
<span id="cb17-4"><a href="#cb17-4" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <span class="ot">{</span>e e1 e2 <span class="ot">:</span> <dfn>Expr</dfn> Γ a<span class="ot">}</span></span>
<span id="cb17-5"><a href="#cb17-5" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> e ⇓ e1 <span class="ot">→</span> e ⇓ e2</span>
<span id="cb17-6"><a href="#cb17-6" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> Σ[ e3 ∈ <dfn>Expr</dfn> Γ a ] <span class="ot">(</span>e1 ⇓ e3<span class="ot">)</span> × <span class="ot">(</span>e2 ⇓ e3<span class="ot">)</span></span></code></pre></div>
<p>In the above we make use of Agda’s sigma syntax. A term of the form
<code>Σ[ x ∈ A ] P</code> is a convenient syntax agda offers for the
dependent sum, traditionally written something like <span
class="math inline">\(\Sigma_{x \in A} P\)</span>. Using confluence, it
is now easy to prove that if a term halts at a value then no matter
which reduction steps we take to some other term we will still be able
to reduce to the same value:</p>
<div class="sourceCode" id="cb18"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb18-1"><a href="#cb18-1" aria-hidden="true" tabindex="-1"></a>⇓-val-uniq</span>
<span id="cb18-2"><a href="#cb18-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>e e&#39; v <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb18-3"><a href="#cb18-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <dfn>Value</dfn> v <span class="ot">→</span> e ⇓ v <span class="ot">→</span> e ⇓ e&#39; <span class="ot">→</span> e&#39; ⇓ v</span>
<span id="cb18-4"><a href="#cb18-4" aria-hidden="true" tabindex="-1"></a>⇓-val-uniq pf e⇓v e⇓e&#39; <span class="kw">with</span> confluence e⇓v e⇓e&#39;</span>
<span id="cb18-5"><a href="#cb18-5" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> Sg e3 <span class="ot">(</span>Sg v⇓e3 e&#39;⇓e3<span class="ot">)</span> <span class="kw">with</span> ⇓-val pf v⇓e3</span>
<span id="cb18-6"><a href="#cb18-6" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> refl <span class="ot">=</span> e&#39;⇓e3</span></code></pre></div>
<p>From this, we can conclude the “head-expansion” property we wanted of
non-termination:</p>
<div class="sourceCode" id="cb19"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb19-1"><a href="#cb19-1" aria-hidden="true" tabindex="-1"></a><dfn>halt-⊥</dfn></span>
<span id="cb19-2"><a href="#cb19-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>e1 e2 <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">}</span></span>
<span id="cb19-3"><a href="#cb19-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> e1 ⇓ e2 <span class="ot">→</span> ¬ <span class="ot">(</span><dfn>Halt</dfn> e2<span class="ot">)</span> <span class="ot">→</span> ¬ <span class="ot">(</span><dfn>Halt</dfn> e1<span class="ot">)</span></span>
<span id="cb19-4"><a href="#cb19-4" aria-hidden="true" tabindex="-1"></a><dfn>halt-⊥</dfn> e1⇓e2 e2-⊥ <span class="ot">(</span><span class="cn">halts</span> v-e1 st<span class="ot">)</span> <span class="kw">with</span> ⇓-val-uniq v-e1 st e1⇓e2</span>
<span id="cb19-5"><a href="#cb19-5" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> e2⇓v <span class="ot">=</span> e2-⊥ <span class="ot">(</span><span class="cn">halts</span> v-e1 e2⇓v<span class="ot">)</span></span></code></pre></div>
<p>First, it is easy for us to show (recursively) that <code>bot</code>
does not halt:</p>
<div class="sourceCode" id="cb20"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb20-1"><a href="#cb20-1" aria-hidden="true" tabindex="-1"></a><dfn>bot-non-term</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">→</span>  ¬ <span class="ot">(</span><dfn>Halt</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>ty<span class="ot">}</span> <dfn>bot</dfn><span class="ot">)</span></span>
<span id="cb20-2"><a href="#cb20-2" aria-hidden="true" tabindex="-1"></a><dfn>bot-non-term</dfn> <span class="ot">(</span><span class="cn">halts</span> v <span class="ot">(.(</span><span class="cn">fix</span> <span class="ot">(</span><span class="cn">var</span> <span class="cn">z</span><span class="ot">))</span> <span class="cn">→⟨</span> fix-↓ ⟩ st<span class="ot">))</span></span>
<span id="cb20-3"><a href="#cb20-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <dfn>bot-non-term</dfn> <span class="ot">(</span><span class="cn">halts</span> v st<span class="ot">)</span></span></code></pre></div>
<p>We can then put together <code>halt-⊥</code> and
<code>bot-non-term</code> to show that any term that steps to
<code>bot</code> cannot terminate:</p>
<div class="sourceCode" id="cb21"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb21-1"><a href="#cb21-1" aria-hidden="true" tabindex="-1"></a>⇓-bot-⊥ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>e <span class="ot">:</span> <dfn>Expr</dfn> Γ ty<span class="ot">)</span> <span class="ot">→</span> e ⇓ <dfn>bot</dfn> <span class="ot">→</span> ¬ <dfn>Halt</dfn> e</span>
<span id="cb21-2"><a href="#cb21-2" aria-hidden="true" tabindex="-1"></a>⇓-bot-⊥ e st <span class="ot">=</span> <dfn>halt-⊥</dfn> st <dfn>bot-non-term</dfn></span></code></pre></div>
<p>Now, we are better placed to show that if
<code>halt fix-problem</code> reduces to <code>tt</code> then
<code>fix-problem</code> reduces to <code>bot</code> and thus we get a
contradiction. The final general result we will need is one that
connects the big step operational semantics of Booleans to that of our
conditional function, <code>bool</code>.</p>
<div class="sourceCode" id="cb22"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb22-1"><a href="#cb22-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- In both cases if there is no reduction then we directly step.</span></span>
<span id="cb22-2"><a href="#cb22-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- Otherwise we reduce the conditional and recurse on the result.</span></span>
<span id="cb22-3"><a href="#cb22-3" aria-hidden="true" tabindex="-1"></a>bool-stepper-tt</span>
<span id="cb22-4"><a href="#cb22-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>th el<span class="ot">}</span> <span class="ot">(</span>b <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span><span class="ot">)</span></span>
<span id="cb22-5"><a href="#cb22-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> b ⇓ <span class="cn">tt</span> <span class="ot">→</span> <span class="ot">(</span><span class="cn">bool</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> b th el<span class="ot">)</span> ⇓ th</span>
<span id="cb22-6"><a href="#cb22-6" aria-hidden="true" tabindex="-1"></a>bool-stepper-tt <span class="ot">{_}</span> <span class="ot">{</span>th<span class="ot">}</span> <span class="ot">{</span>el<span class="ot">}</span> <span class="ot">.</span><span class="cn">tt</span> <span class="ot">(.</span><span class="cn">tt</span> ∎<span class="ot">)</span></span>
<span id="cb22-7"><a href="#cb22-7" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="cn">bool</span> <span class="cn">tt</span> th el <span class="cn">→⟨</span> if-tt-↓ ⟩ <span class="ot">(</span>th ∎<span class="ot">)</span></span>
<span id="cb22-8"><a href="#cb22-8" aria-hidden="true" tabindex="-1"></a>bool-stepper-tt <span class="ot">{_}</span> <span class="ot">{</span>th<span class="ot">}</span> <span class="ot">{</span>el<span class="ot">}</span> b <span class="ot">(_</span><span class="cn">→⟨</span><span class="ot">_</span>⟩<span class="ot">_</span> <span class="ot">.</span>b <span class="ot">{</span>M<span class="ot">}</span> x st<span class="ot">)</span></span>
<span id="cb22-9"><a href="#cb22-9" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="ot">_</span><span class="cn">→⟨</span><span class="ot">_</span>⟩<span class="ot">_</span> <span class="ot">(</span><span class="cn">bool</span> b th el<span class="ot">)</span> <span class="ot">(</span>if-↓ x<span class="ot">)</span> <span class="ot">(</span>bool-stepper-tt M st<span class="ot">)</span></span>
<span id="cb22-10"><a href="#cb22-10" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb22-11"><a href="#cb22-11" aria-hidden="true" tabindex="-1"></a>bool-stepper-ff</span>
<span id="cb22-12"><a href="#cb22-12" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>th el<span class="ot">}</span> <span class="ot">(</span>b <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span><span class="ot">)</span></span>
<span id="cb22-13"><a href="#cb22-13" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> b ⇓ <span class="cn">ff</span> <span class="ot">→</span> <span class="ot">(</span><span class="cn">bool</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> b th el<span class="ot">)</span> ⇓ el</span>
<span id="cb22-14"><a href="#cb22-14" aria-hidden="true" tabindex="-1"></a>bool-stepper-ff <span class="ot">{_}</span> <span class="ot">{</span>th<span class="ot">}</span> <span class="ot">{</span>el<span class="ot">}</span> <span class="ot">.</span><span class="cn">ff</span> <span class="ot">(.</span><span class="cn">ff</span> ∎<span class="ot">)</span></span>
<span id="cb22-15"><a href="#cb22-15" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="cn">bool</span> <span class="cn">ff</span> th el <span class="cn">→⟨</span> if-ff-↓ ⟩ <span class="ot">(</span>el ∎<span class="ot">)</span></span>
<span id="cb22-16"><a href="#cb22-16" aria-hidden="true" tabindex="-1"></a>bool-stepper-ff <span class="ot">{_}</span> <span class="ot">{</span>th<span class="ot">}</span> <span class="ot">{</span>el<span class="ot">}</span> b <span class="ot">(_</span><span class="cn">→⟨</span><span class="ot">_</span>⟩<span class="ot">_</span> <span class="ot">.</span>b <span class="ot">{</span>M<span class="ot">}</span> x st<span class="ot">)</span></span>
<span id="cb22-17"><a href="#cb22-17" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="ot">_</span><span class="cn">→⟨</span><span class="ot">_</span>⟩<span class="ot">_</span> <span class="ot">(</span><span class="cn">bool</span> b th el<span class="ot">)</span> <span class="ot">(</span>if-↓ x<span class="ot">)</span> <span class="ot">(</span>bool-stepper-ff M st<span class="ot">)</span></span></code></pre></div>
<p>We are now in a position to show that
<code>halt (fix-problem) ⇓ tt</code> gives rise to a contradiction which
we do in a number of simple steps:</p>
<div class="sourceCode" id="cb23"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb23-1"><a href="#cb23-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- First, we show that there is only a single way to reduce</span></span>
<span id="cb23-2"><a href="#cb23-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- <dfn>fix-problem</dfn> since only the <span class="cn">fix</span> rule applies.</span></span>
<span id="cb23-3"><a href="#cb23-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- In order to reduce <dfn>fix-problem</dfn> to what we want</span></span>
<span id="cb23-4"><a href="#cb23-4" aria-hidden="true" tabindex="-1"></a><span class="co">-- we have to know that:</span></span>
<span id="cb23-5"><a href="#cb23-5" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb23-6"><a href="#cb23-6" aria-hidden="true" tabindex="-1"></a><span class="co">--   <dfn>problem</dfn> <dfn>[</dfn> <dfn>fix-problem</dfn> ] ≡ <span class="cn">bool</span> (<span class="cn">app</span> halt <dfn>fix-problem</dfn>) <dfn>bot</dfn> <span class="cn">tt</span></span></span>
<span id="cb23-7"><a href="#cb23-7" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb23-8"><a href="#cb23-8" aria-hidden="true" tabindex="-1"></a><span class="co">-- In order for this to be the case we need that:</span></span>
<span id="cb23-9"><a href="#cb23-9" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb23-10"><a href="#cb23-10" aria-hidden="true" tabindex="-1"></a><span class="co">--     <span class="cn">app</span> (halt <dfn>[</dfn> <dfn>fix-problem</dfn> ]) (<dfn>sub</dfn> <dfn>fix-problem</dfn> <span class="cn">z</span>)</span></span>
<span id="cb23-11"><a href="#cb23-11" aria-hidden="true" tabindex="-1"></a><span class="co">--   ≡ <span class="cn">app</span> halt <dfn>fix-problem</dfn></span></span>
<span id="cb23-12"><a href="#cb23-12" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb23-13"><a href="#cb23-13" aria-hidden="true" tabindex="-1"></a><span class="co">-- and so we need that halt is a closed term. That is precisely what</span></span>
<span id="cb23-14"><a href="#cb23-14" aria-hidden="true" tabindex="-1"></a><span class="co">-- halt-sub gives us:</span></span>
<span id="cb23-15"><a href="#cb23-15" aria-hidden="true" tabindex="-1"></a>fp-step1</span>
<span id="cb23-16"><a href="#cb23-16" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>e <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span><span class="ot">}</span></span>
<span id="cb23-17"><a href="#cb23-17" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><dfn>fix-problem</dfn> <span class="ot">{</span>Γ<span class="ot">})</span> ↓ e</span>
<span id="cb23-18"><a href="#cb23-18" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> e ≡ <span class="cn">bool</span> <span class="ot">(</span><span class="cn">app</span> halt <dfn>fix-problem</dfn><span class="ot">)</span> <dfn>bot</dfn> <span class="cn">tt</span></span>
<span id="cb23-19"><a href="#cb23-19" aria-hidden="true" tabindex="-1"></a>fp-step1 <span class="ot">{</span>Γ<span class="ot">}</span> fix-↓</span>
<span id="cb23-20"><a href="#cb23-20" aria-hidden="true" tabindex="-1"></a>  <span class="kw">rewrite</span> <span class="ot">(</span>halt-sub <span class="ot">{</span>Γ <dfn>,</dfn> <span class="cn">𝔹</span><span class="ot">}</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> <span class="ot">(</span><dfn>sub</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <dfn>fix-problem</dfn><span class="ot">))</span></span>
<span id="cb23-21"><a href="#cb23-21" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> refl</span>
<span id="cb23-22"><a href="#cb23-22" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb23-23"><a href="#cb23-23" aria-hidden="true" tabindex="-1"></a><span class="co">-- Here we have a small lemma that we can replace</span></span>
<span id="cb23-24"><a href="#cb23-24" aria-hidden="true" tabindex="-1"></a><span class="co">-- equal values in the stepping relation</span></span>
<span id="cb23-25"><a href="#cb23-25" aria-hidden="true" tabindex="-1"></a>≡-↓</span>
<span id="cb23-26"><a href="#cb23-26" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>e e&#39; e&#39;&#39; <span class="ot">:</span> <dfn>Expr</dfn> Γ <span class="cn">𝔹</span><span class="ot">}</span></span>
<span id="cb23-27"><a href="#cb23-27" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> e ↓ e&#39;</span>
<span id="cb23-28"><a href="#cb23-28" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> e&#39; ≡ e&#39;&#39;</span>
<span id="cb23-29"><a href="#cb23-29" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> e ↓ e&#39;&#39;</span>
<span id="cb23-30"><a href="#cb23-30" aria-hidden="true" tabindex="-1"></a>≡-↓ e↓e&#39; refl <span class="ot">=</span> e↓e&#39;</span>
<span id="cb23-31"><a href="#cb23-31" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb23-32"><a href="#cb23-32" aria-hidden="true" tabindex="-1"></a><span class="co">-- We then make use of this fact and step 1 to show</span></span>
<span id="cb23-33"><a href="#cb23-33" aria-hidden="true" tabindex="-1"></a><span class="co">-- that <dfn>fix-problem</dfn> steps as we expect:</span></span>
<span id="cb23-34"><a href="#cb23-34" aria-hidden="true" tabindex="-1"></a>fp-step2</span>
<span id="cb23-35"><a href="#cb23-35" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb23-36"><a href="#cb23-36" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><dfn>fix-problem</dfn> <span class="ot">{</span>Γ<span class="ot">})</span> ↓ <span class="ot">(</span><span class="cn">bool</span> <span class="ot">(</span><span class="cn">app</span> halt <span class="ot">(</span><dfn>fix-problem</dfn><span class="ot">))</span> <dfn>bot</dfn> <span class="cn">tt</span><span class="ot">)</span></span>
<span id="cb23-37"><a href="#cb23-37" aria-hidden="true" tabindex="-1"></a>fp-step2 <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">=</span></span>
<span id="cb23-38"><a href="#cb23-38" aria-hidden="true" tabindex="-1"></a>  ≡-↓ <span class="ot">(</span>fix-↓ <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> <span class="ot">{</span><dfn>problem</dfn><span class="ot">})</span> <span class="ot">(</span>fp-step1 <span class="ot">(</span>fix-↓ <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span><span class="cn">𝔹</span><span class="ot">}</span> <span class="ot">{</span><dfn>problem</dfn><span class="ot">}))</span></span>
<span id="cb23-39"><a href="#cb23-39" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb23-40"><a href="#cb23-40" aria-hidden="true" tabindex="-1"></a><span class="co">-- In the next two steps we then use our assumption and the</span></span>
<span id="cb23-41"><a href="#cb23-41" aria-hidden="true" tabindex="-1"></a><span class="co">-- big step lemma to derive a contradiction:</span></span>
<span id="cb23-42"><a href="#cb23-42" aria-hidden="true" tabindex="-1"></a>fp-step3</span>
<span id="cb23-43"><a href="#cb23-43" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb23-44"><a href="#cb23-44" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">tt</span></span>
<span id="cb23-45"><a href="#cb23-45" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">bool</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> <dfn>bot</dfn> <span class="cn">tt</span><span class="ot">)</span> ⇓ <dfn>bot</dfn></span>
<span id="cb23-46"><a href="#cb23-46" aria-hidden="true" tabindex="-1"></a>fp-step3 ⇓-tt <span class="ot">=</span> bool-stepper-tt <span class="ot">_</span>  ⇓-tt</span>
<span id="cb23-47"><a href="#cb23-47" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb23-48"><a href="#cb23-48" aria-hidden="true" tabindex="-1"></a>fp-step4</span>
<span id="cb23-49"><a href="#cb23-49" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb23-50"><a href="#cb23-50" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">tt</span></span>
<span id="cb23-51"><a href="#cb23-51" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><dfn>fix-problem</dfn> <span class="ot">{</span>Γ<span class="ot">})</span> ⇓ <dfn>bot</dfn></span>
<span id="cb23-52"><a href="#cb23-52" aria-hidden="true" tabindex="-1"></a>fp-step4 <span class="ot">{</span>Γ<span class="ot">}</span> ⇓-tt <span class="ot">=</span> <dfn>fix-problem</dfn> <span class="cn">→⟨</span> fp-step2 ⟩ fp-step3 ⇓-tt</span></code></pre></div>
<p>The other half of the argument, assuming
<code>halt (fix-problem) ⇓ ff</code>, is quite a bit simpler. To prove
halting, we only need to exhibit some particular sequence of
reductions:</p>
<div class="sourceCode" id="cb24"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb24-1"><a href="#cb24-1" aria-hidden="true" tabindex="-1"></a>fp-step5</span>
<span id="cb24-2"><a href="#cb24-2" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb24-3"><a href="#cb24-3" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">ff</span></span>
<span id="cb24-4"><a href="#cb24-4" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">bool</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> <dfn>bot</dfn> <span class="cn">tt</span><span class="ot">)</span> ⇓ <span class="cn">tt</span></span>
<span id="cb24-5"><a href="#cb24-5" aria-hidden="true" tabindex="-1"></a>fp-step5 ⇓-ff <span class="ot">=</span> bool-stepper-ff <span class="ot">_</span> ⇓-ff</span>
<span id="cb24-6"><a href="#cb24-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb24-7"><a href="#cb24-7" aria-hidden="true" tabindex="-1"></a><span class="co">-- We make use of our substition lemma (fp-step2) to</span></span>
<span id="cb24-8"><a href="#cb24-8" aria-hidden="true" tabindex="-1"></a><span class="co">-- show under this assumption that <dfn>fix-problem</dfn> big</span></span>
<span id="cb24-9"><a href="#cb24-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- steps to <span class="cn">tt</span>.</span></span>
<span id="cb24-10"><a href="#cb24-10" aria-hidden="true" tabindex="-1"></a>fp-step6</span>
<span id="cb24-11"><a href="#cb24-11" aria-hidden="true" tabindex="-1"></a>   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb24-12"><a href="#cb24-12" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">ff</span></span>
<span id="cb24-13"><a href="#cb24-13" aria-hidden="true" tabindex="-1"></a>   <span class="ot">→</span> <dfn>fix-problem</dfn> ⇓ <span class="cn">tt</span></span>
<span id="cb24-14"><a href="#cb24-14" aria-hidden="true" tabindex="-1"></a>fp-step6 ⇓-ff <span class="ot">=</span> <dfn>fix-problem</dfn> <span class="cn">→⟨</span> fp-step2 ⟩ fp-step5 ⇓-ff</span></code></pre></div>
<p>Finally, let’s package up these results into their respective
contradictions:</p>
<div class="sourceCode" id="cb25"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb25-1"><a href="#cb25-1" aria-hidden="true" tabindex="-1"></a>fix-problem-tt</span>
<span id="cb25-2"><a href="#cb25-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb25-3"><a href="#cb25-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">tt</span></span>
<span id="cb25-4"><a href="#cb25-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <dfn>Halt</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <dfn>fix-problem</dfn> <span class="ot">→</span> ⊥</span>
<span id="cb25-5"><a href="#cb25-5" aria-hidden="true" tabindex="-1"></a>fix-problem-tt ⇓-tt h <span class="ot">=</span> ⇓-bot-⊥ <span class="ot">_</span> <span class="ot">(</span>fp-step4 ⇓-tt<span class="ot">)</span> h</span>
<span id="cb25-6"><a href="#cb25-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb25-7"><a href="#cb25-7" aria-hidden="true" tabindex="-1"></a>fix-problem-ff</span>
<span id="cb25-8"><a href="#cb25-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span></span>
<span id="cb25-9"><a href="#cb25-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <span class="ot">(</span><span class="cn">app</span> <span class="ot">(</span>halt <span class="ot">{</span>Γ<span class="ot">})</span> <dfn>fix-problem</dfn><span class="ot">)</span> ⇓ <span class="cn">ff</span></span>
<span id="cb25-10"><a href="#cb25-10" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <span class="ot">(</span>¬ <dfn>Halt</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <dfn>fix-problem</dfn><span class="ot">)</span> <span class="ot">→</span> ⊥</span>
<span id="cb25-11"><a href="#cb25-11" aria-hidden="true" tabindex="-1"></a>fix-problem-ff ⇓-ff ¬h <span class="ot">=</span> ¬h <span class="ot">(</span><span class="cn">halts</span> <span class="cn">V-tt</span> <span class="ot">(</span>fp-step6 ⇓-ff<span class="ot">))</span></span></code></pre></div>
<p>and put everything together:</p>
<div class="sourceCode" id="cb26"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb26-1"><a href="#cb26-1" aria-hidden="true" tabindex="-1"></a><dfn>halting</dfn> <span class="ot">:</span> ⊥</span>
<span id="cb26-2"><a href="#cb26-2" aria-hidden="true" tabindex="-1"></a><dfn>halting</dfn> <span class="kw">with</span> halt-ret <span class="ot">{</span>nil<span class="ot">}</span> <dfn>fix-problem</dfn></span>
<span id="cb26-3"><a href="#cb26-3" aria-hidden="true" tabindex="-1"></a><dfn>halting</dfn> <span class="ot">|</span> inj₁ ⇓tt  <span class="ot">=</span> fix-problem-tt ⇓tt <span class="ot">(</span>halt-tt <dfn>fix-problem</dfn> ⇓tt<span class="ot">)</span></span>
<span id="cb26-4"><a href="#cb26-4" aria-hidden="true" tabindex="-1"></a><dfn>halting</dfn> <span class="ot">|</span> inj₂ ⇓ff  <span class="ot">=</span> fix-problem-ff ⇓ff <span class="ot">(</span>halt-ff <dfn>fix-problem</dfn> ⇓ff<span class="ot">)</span></span></code></pre></div>
<p>Hopefully this post has given an approachable account of formalising
one of the central results in computability theory. We hope to have also
demonstrated, from a programming language theory perspective, some of
the advantages of the lambda calculus as a foundational theory of
computation (over, for example, Turing machines). Thank you for reading!
The full code for this proof is available <a
href="https://github.com/Boarders/agda-halting">here</a>.</p>
<p><i>With warmest thanks to Alixandra Prybyla and Sam Derbyshire for
their valuable feedback.</i></p>]]></content>
  </entry>
  <entry>
      <title>Locally Nameless</title>
      <link href="https://boarders.github.ioposts/locally-nameless.html"/>
      <id>https://boarders.github.ioposts/locally-nameless.html</id>
      <updated>2019-10-27T00:00:SZ</updated>
      <category term="Type theory"/>
      <category term="Lambda Calculus"/>
      <summary>The locally nameless approach to substitution.</summary>
      <content type="html"><![CDATA[<p><em>Note: the original version of this post unfortunately had
name-capturing errors, and perhaps has some still remaining after
fixing. Alas, I console myself that this particular misfortune is one
that has befallen many of my betters.</em></p>
The untyped lambda calculus has a very simple grammar with just three
term formers:
<div style="display:none">
()
</div>
<p><span class="math display">\[ \mathrm{term}
\mathrel{\vcenter{\hbox{::}}{=}} v \mspace{5mu}
  | \mspace{5mu}\lambda \mspace{5mu}v \mspace{5mu}.
\mspace{5mu}\mathrm{term} \mspace{5mu}
  | \mspace{5mu}\mathrm{term} \mspace{5mu}\mathrm{term} \mspace{5mu}
  \]</span></p>
<p>Or in Haskell:</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Lam</span> a <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a>  <span class="dt">Var</span><span class="ot"> ::</span> a <span class="ot">-&gt;</span> <span class="dt">Lam</span> a</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a>  <span class="dt">Lam</span><span class="ot"> ::</span> a <span class="ot">-&gt;</span> <span class="dt">Lam</span> a <span class="ot">-&gt;</span> <span class="dt">Lam</span> a</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  <span class="dt">App</span><span class="ot"> ::</span> <span class="dt">Lam</span> a <span class="ot">-&gt;</span> <span class="dt">Lam</span> a <span class="ot">-&gt;</span> <span class="dt">Lam</span> a</span></code></pre></div>
<p>In order that this work as a theory of computation, we need some
notion of evaluation and this is driven by <span
class="math inline">\(\beta\)</span>-reduction. The standard jargon in
lambda caluclus is to say that a <span
class="math inline">\(\beta\)</span>-redex is any subterm of the form
<span class="math inline">\((\lambda \mathrm{x} \mspace{5mu}.
\mspace{5mu}\mathrm{f}) \mspace{5mu}\mathrm{arg}\)</span>. On such a
<span class="math inline">\(\beta\)</span>-redex we can then step via
(capture-avoiding) substitution:</p>
<p><span class="math display">\[ (\lambda \mathrm{x} \mspace{5mu}.
\mspace{5mu}\mathrm{f}) \mspace{5mu}\mathrm{arg}
    \rightsquigarrow
   f\mspace{5mu}[x  \mathrel{\vcenter{\hbox{:}}{=}} \mathrm{arg}]
  \]</span></p>
<p>Formally we then extend this to a congruence relation on Lambda terms
via rules of the form:</p>
<p><span class="math display">\[
  M \rightsquigarrow M&#39; \Rightarrow M N \rightsquigarrow M&#39; N
\]</span> <span class="math display">\[
  N \rightsquigarrow N&#39; \Rightarrow M N \rightsquigarrow M  N&#39;
  \]</span></p>
<p>Precisely how we choose to set up these rules to give a computational
semantics to our language corresponds algorithmically to a choice of
evaluation strategy. As such, this opens up myriad interesting questions
related to order of evaluation and normal forms and so on. Instead we
will largely bypass these considerations and concentrate on the more
humdrum (but no less vital) matter of the practicalities of performing
such capture-avoiding substitutions. The basic problem with too naive an
approach is as follows:</p>
<p><span class="math display">\[ (\lambda \mspace{5mu}\mathrm{x}
\mspace{5mu}. \mspace{5mu}\lambda \mspace{5mu}y \mspace{5mu}.
\mspace{5mu}x) \mspace{5mu}\mathrm{y}
    \rightsquigarrow
   (\lambda \mathrm{y} \mspace{5mu}. \mspace{5mu}y)
  \]</span></p>
<p>Here we have substituted the <em>free</em> variable y into our lambda
term and it has become <em>bound</em>. This is semantically incorrect:
the names of free variables are meaningful because, in spirit, they
refer to names we have defined elsewhere (that is, they can be looked up
within a context, or, in other words, they are <em>open</em> for further
substitution). Conversely, the names of bound variables are,
computationally speaking, unimportant. In fact, it is usual to refer to
the grammar we have introduced earlier as <em>pre-lambda terms</em> and
to take lambda terms as referring to the equivalence classes under <span
class="math inline">\(\alpha\)</span>-equivalence. This refers to the
(equivalence) relation whereby two terms are equivalent if we can
consistently rename the bound variables of one to obtain the other (here
too we need to take care, <span
class="math inline">\(\alpha\)</span>-renaming <span
class="math inline">\(\mathrm{x}\)</span> to <span
class="math inline">\(\mathrm{y}\)</span> in the above term would lead
to a different sort of variable capture). Most accounts of <span
class="math inline">\(\alpha\)</span>-equivalence are themselves
intimiately tied up with the question of how to perform substitution
(and locally nameless is no different in this respect).</p>
<p>In practice this means that in order to compute <span
class="math inline">\((\lambda \mathrm{x} \mspace{5mu}.
\mspace{5mu}\mathrm{f}) \mspace{5mu}\mathrm{arg}\)</span> we would first
<span class="math inline">\(\alpha\)</span>-rename <span
class="math inline">\(\mathrm{x}\)</span> to a variable that is neither
already named within <span class="math inline">\(\mathrm{f}\)</span>,
nor appears free within <span
class="math inline">\(\mathrm{arg}\)</span>. Carrying out such a
procedure by brute force is workable, but tends to be rather
error-prone. A straightforward approach along these lines is described
in <a
href="http://augustss.blogspot.com/2007/10/simpler-easier-in-recent-paper-simply.html">this
excellent post</a> by Lennart Augustsson.</p>
<p>There are a <a
href="https://www.schoolofhaskell.com/user/edwardk/bound">whole host</a>
of more sophisticated methods for dealing with the problem of
capture-avoiding substitution. Perhaps one of the best known is to use
De-Bruijn indices. The idea here is to replace all bound variables by a
natural number. This indicates the variable’s distance from its binding
site. All free variables are then represented by distinct natural
numbers greater than the maximum depth of any binding site in the term.
We then keep track of these variables within the environment under which
computation is performed. For instance, the following is how one might
translate a typical term into De-Bruijn indices:</p>
<p><span class="math display">\[ (\lambda \mspace{5mu}\mathrm{x}
\mspace{5mu}. \mspace{5mu}\lambda \mspace{5mu}y \mspace{5mu}.
\mspace{5mu}x \mspace{5mu}z)
    \longrightarrow
   (\lambda \mspace{5mu}\lambda \mspace{5mu}. \mspace{5mu}1
\mspace{5mu}3 )
   \]</span> <span class="math display">\[
   [z \mapsto 3]
  \]</span></p>
<p>Here we keep both the translated lambda term but also the context for
how to read free variables.</p>
<p>This approach offers two key advantages:</p>
<ul>
<li>Capture avoiding substitution becomes a matter of keeping binding
distance arithmetic in check.</li>
<li>The De-Bruijn representation gives canonical representatives for
<span class="math inline">\(\alpha\)</span>-equivalence classes, thus
allowing us to test for <span
class="math inline">\(\alpha\)</span>-equivalence via syntactic equality
of terms.</li>
</ul>
<p>On the other hand, Bob Atkey has, rather aptly, referred to the
ability to read terms written with DeBruijn indices as a “cylon
detector”. What we gain in ease of implementation we give up in much
worse readability.</p>
<p>Instead we turn to the hybrid approach in the paper <a
href="http://www.cs.ru.nl/~james/RESEARCH/haskell2004.pdf">I Am Not a
Number – I am a Free Variable</a>. Let us keep free variables free and
use De-Bruijn indices only for bound variables:</p>
<div class="sourceCode" id="cb2"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- Our variable type keeps the old free variables and uses</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a><span class="co">--integers to represent bound variables.</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Var</span> a</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="dt">F</span> a</span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="dt">B</span> <span class="dt">Int</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true" tabindex="-1"></a><span class="co">-- Locally nameless terms will be the same lambda terms with</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true" tabindex="-1"></a><span class="co">-- variables now labelled either bound or free.</span></span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true" tabindex="-1"></a><span class="kw">type</span> <span class="dt">LocallyNameless</span> a <span class="ot">=</span> <span class="dt">Lam</span> (<span class="dt">Var</span> a)</span></code></pre></div>
<p>Notice how, because we use the same lambda terms with this
representation, we still have names at binders. This is useful as we can
recover the named term we started out with, carrying along all such
names as we perform work. Here is how we convert between the two
representations:</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="ot">{-# LANGUAGE LambdaCase          #-}</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a><span class="ot">{-# LANGUAGE ScopedTypeVariables #-}</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Data.Map</span> <span class="kw">as</span> <span class="dt">M</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Data.Set</span> (notMember)</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true" tabindex="-1"></a><span class="co">{-</span></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true" tabindex="-1"></a><span class="co">[...]</span></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true" tabindex="-1"></a><span class="co">-}</span></span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true" tabindex="-1"></a><span class="ot">toLocallyNameless ::</span> <span class="kw">forall</span> a <span class="op">.</span> (<span class="dt">Ord</span> a) <span class="ot">=&gt;</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true" tabindex="-1"></a>toLocallyNameless <span class="ot">=</span> go <span class="fu">mempty</span></span>
<span id="cb3-10"><a href="#cb3-10" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb3-11"><a href="#cb3-11" aria-hidden="true" tabindex="-1"></a><span class="ot">    go ::</span> <span class="dt">Map</span> a <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb3-12"><a href="#cb3-12" aria-hidden="true" tabindex="-1"></a>    go env <span class="ot">=</span> \<span class="kw">case</span></span>
<span id="cb3-13"><a href="#cb3-13" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Var</span> a  <span class="ot">-&gt;</span></span>
<span id="cb3-14"><a href="#cb3-14" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- we check if our variable has been bound elsewhere</span></span>
<span id="cb3-15"><a href="#cb3-15" aria-hidden="true" tabindex="-1"></a>        <span class="kw">case</span> a <span class="ot">`lookup`</span> env <span class="kw">of</span></span>
<span id="cb3-16"><a href="#cb3-16" aria-hidden="true" tabindex="-1"></a>          <span class="dt">Just</span> bv <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">B</span> bv)</span>
<span id="cb3-17"><a href="#cb3-17" aria-hidden="true" tabindex="-1"></a>          <span class="dt">Nothing</span> <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">F</span> a)</span>
<span id="cb3-18"><a href="#cb3-18" aria-hidden="true" tabindex="-1"></a>      <span class="dt">App</span> l r <span class="ot">-&gt;</span> <span class="dt">App</span> (go env l) (go env r)</span>
<span id="cb3-19"><a href="#cb3-19" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Lam</span> n e <span class="ot">-&gt;</span></span>
<span id="cb3-20"><a href="#cb3-20" aria-hidden="true" tabindex="-1"></a>        <span class="kw">let</span></span>
<span id="cb3-21"><a href="#cb3-21" aria-hidden="true" tabindex="-1"></a>       <span class="co">-- As we have gone under a binder we bump each variable</span></span>
<span id="cb3-22"><a href="#cb3-22" aria-hidden="true" tabindex="-1"></a>       <span class="co">-- by 1, inserting our newly bound variable at 0.</span></span>
<span id="cb3-23"><a href="#cb3-23" aria-hidden="true" tabindex="-1"></a>          env&#39; <span class="ot">=</span> insert n <span class="dv">0</span> (M.map (<span class="op">+</span> <span class="dv">1</span>) env)</span>
<span id="cb3-24"><a href="#cb3-24" aria-hidden="true" tabindex="-1"></a>        <span class="kw">in</span></span>
<span id="cb3-25"><a href="#cb3-25" aria-hidden="true" tabindex="-1"></a>          <span class="dt">Lam</span> (<span class="dt">F</span> n) (go env&#39; e)</span></code></pre></div>
<p>When converting back from locally nameless we need to be careful
about name capture: if the binder name already appears free in the body,
naively reusing it would capture those free occurrences. We need a way
to generate a fresh name and a way to collect free variables in locally
nameless terms:</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- A class for generating fresh names by incrementing:</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- for Text this appends &quot;&#39;&quot;, so &quot;x&quot; becomes &quot;x&#39;&quot;, &quot;x&#39;&quot; becomes &quot;x&#39;&#39;&quot;, etc.</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true" tabindex="-1"></a><span class="kw">class</span> <span class="dt">Increment</span> a <span class="kw">where</span></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true" tabindex="-1"></a><span class="ot">  incr ::</span> a <span class="ot">-&gt;</span> a</span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> (<span class="dt">IsString</span> t, <span class="dt">Semigroup</span> t) <span class="ot">=&gt;</span> <span class="dt">Increment</span> t <span class="kw">where</span></span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true" tabindex="-1"></a>  incr <span class="ot">=</span> (<span class="op">&lt;&gt;</span> <span class="st">&quot;&#39;&quot;</span>)</span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true" tabindex="-1"></a><span class="ot">freeVarsLN ::</span> (<span class="dt">Ord</span> a) <span class="ot">=&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Set</span> a</span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true" tabindex="-1"></a>freeVarsLN (<span class="dt">Var</span> v) <span class="ot">=</span> <span class="kw">case</span> v <span class="kw">of</span></span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true" tabindex="-1"></a>  <span class="dt">F</span> a  <span class="ot">-&gt;</span> singleton a</span>
<span id="cb4-12"><a href="#cb4-12" aria-hidden="true" tabindex="-1"></a>  <span class="dt">B</span> _  <span class="ot">-&gt;</span> <span class="fu">mempty</span></span>
<span id="cb4-13"><a href="#cb4-13" aria-hidden="true" tabindex="-1"></a>freeVarsLN (<span class="dt">App</span> l r) <span class="ot">=</span> freeVarsLN l <span class="op">&lt;&gt;</span> freeVarsLN r</span>
<span id="cb4-14"><a href="#cb4-14" aria-hidden="true" tabindex="-1"></a>freeVarsLN (<span class="dt">Lam</span> _ e) <span class="ot">=</span> freeVarsLN e</span>
<span id="cb4-15"><a href="#cb4-15" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb4-16"><a href="#cb4-16" aria-hidden="true" tabindex="-1"></a><span class="co">-- Keep incrementing the candidate name until it is not in the conflict set.</span></span>
<span id="cb4-17"><a href="#cb4-17" aria-hidden="true" tabindex="-1"></a><span class="ot">freshName ::</span> (<span class="dt">Ord</span> a, <span class="dt">Increment</span> a) <span class="ot">=&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Set</span> a <span class="ot">-&gt;</span> a</span>
<span id="cb4-18"><a href="#cb4-18" aria-hidden="true" tabindex="-1"></a>freshName candidate conflicts</span>
<span id="cb4-19"><a href="#cb4-19" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> candidate <span class="ot">`notMember`</span> conflicts <span class="ot">=</span> candidate</span>
<span id="cb4-20"><a href="#cb4-20" aria-hidden="true" tabindex="-1"></a>  <span class="op">|</span> <span class="fu">otherwise</span> <span class="ot">=</span> freshName (incr candidate) conflicts</span></code></pre></div>
<div class="sourceCode" id="cb5"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true" tabindex="-1"></a><span class="ot">fromLocallyNameless ::</span> <span class="kw">forall</span> a <span class="op">.</span> (<span class="dt">Ord</span> a, <span class="dt">Increment</span> a) <span class="ot">=&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> a</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true" tabindex="-1"></a>fromLocallyNameless <span class="ot">=</span> go <span class="fu">mempty</span></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true" tabindex="-1"></a><span class="ot">    go ::</span> <span class="dt">Map</span> <span class="dt">Int</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> a</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true" tabindex="-1"></a>    go env <span class="ot">=</span> \<span class="kw">case</span></span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Var</span> v <span class="ot">-&gt;</span></span>
<span id="cb5-7"><a href="#cb5-7" aria-hidden="true" tabindex="-1"></a>        <span class="kw">case</span> v <span class="kw">of</span></span>
<span id="cb5-8"><a href="#cb5-8" aria-hidden="true" tabindex="-1"></a>          <span class="dt">F</span> a  <span class="ot">-&gt;</span> <span class="dt">Var</span> a</span>
<span id="cb5-9"><a href="#cb5-9" aria-hidden="true" tabindex="-1"></a>               <span class="co">-- we look up our bound variable with the name we collected from</span></span>
<span id="cb5-10"><a href="#cb5-10" aria-hidden="true" tabindex="-1"></a>               <span class="co">-- its binder</span></span>
<span id="cb5-11"><a href="#cb5-11" aria-hidden="true" tabindex="-1"></a>          <span class="dt">B</span> bv <span class="ot">-&gt;</span> <span class="kw">case</span> bv <span class="ot">`lookup`</span> env <span class="kw">of</span></span>
<span id="cb5-12"><a href="#cb5-12" aria-hidden="true" tabindex="-1"></a>            <span class="dt">Just</span> name <span class="ot">-&gt;</span> <span class="dt">Var</span> name</span>
<span id="cb5-13"><a href="#cb5-13" aria-hidden="true" tabindex="-1"></a>            <span class="dt">Nothing</span>   <span class="ot">-&gt;</span> <span class="fu">error</span> <span class="op">$</span></span>
<span id="cb5-14"><a href="#cb5-14" aria-hidden="true" tabindex="-1"></a>              <span class="st">&quot;Found bound variable :&quot;</span> <span class="op">&lt;&gt;</span> <span class="fu">show</span> bv <span class="op">&lt;&gt;</span> <span class="st">&quot; without binder.&quot;</span></span>
<span id="cb5-15"><a href="#cb5-15" aria-hidden="true" tabindex="-1"></a>      <span class="dt">App</span> l r <span class="ot">-&gt;</span> <span class="dt">App</span> (go env l) (go env r)</span>
<span id="cb5-16"><a href="#cb5-16" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Lam</span> n e <span class="ot">-&gt;</span></span>
<span id="cb5-17"><a href="#cb5-17" aria-hidden="true" tabindex="-1"></a>        <span class="kw">case</span> n <span class="kw">of</span></span>
<span id="cb5-18"><a href="#cb5-18" aria-hidden="true" tabindex="-1"></a>       <span class="co">-- if our lambda term has a Bound variable at a binding site something</span></span>
<span id="cb5-19"><a href="#cb5-19" aria-hidden="true" tabindex="-1"></a>       <span class="co">-- has gone horribly wrong</span></span>
<span id="cb5-20"><a href="#cb5-20" aria-hidden="true" tabindex="-1"></a>          <span class="dt">B</span> bv <span class="ot">-&gt;</span> <span class="fu">error</span> <span class="op">$</span> <span class="st">&quot;Found unnamed variable at binding site :&quot;</span> <span class="op">&lt;&gt;</span> <span class="fu">show</span> bv</span>
<span id="cb5-21"><a href="#cb5-21" aria-hidden="true" tabindex="-1"></a>          <span class="dt">F</span> v  <span class="ot">-&gt;</span></span>
<span id="cb5-22"><a href="#cb5-22" aria-hidden="true" tabindex="-1"></a>            <span class="kw">let</span></span>
<span id="cb5-23"><a href="#cb5-23" aria-hidden="true" tabindex="-1"></a>           <span class="co">-- Choose a fresh name that does not appear free in the body,</span></span>
<span id="cb5-24"><a href="#cb5-24" aria-hidden="true" tabindex="-1"></a>           <span class="co">-- to avoid the binder capturing free occurrences of the same name.</span></span>
<span id="cb5-25"><a href="#cb5-25" aria-hidden="true" tabindex="-1"></a>              fvs  <span class="ot">=</span> freeVarsLN e</span>
<span id="cb5-26"><a href="#cb5-26" aria-hidden="true" tabindex="-1"></a>              v&#39;   <span class="ot">=</span> freshName v fvs</span>
<span id="cb5-27"><a href="#cb5-27" aria-hidden="true" tabindex="-1"></a>              env&#39; <span class="ot">=</span> insert <span class="dv">0</span> v&#39; (mapKeysMonotonic (<span class="op">+</span> <span class="dv">1</span>) env)</span>
<span id="cb5-28"><a href="#cb5-28" aria-hidden="true" tabindex="-1"></a>            <span class="kw">in</span></span>
<span id="cb5-29"><a href="#cb5-29" aria-hidden="true" tabindex="-1"></a>              <span class="dt">Lam</span> v&#39; (go env&#39; e)</span></code></pre></div>
<p>Now let’s see that this works as expected (this is, for me, a
worrying amount of bookeeping to leave to “Looks good!”). Let us use
quickcheck to see that <strong>fromLocallyNameless</strong> is a left
inverse to <strong>toLocallyNameless</strong>:</p>
<div class="sourceCode" id="cb6"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Test.Tasty.QuickCheck</span> <span class="kw">as</span> <span class="dt">QC</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true" tabindex="-1"></a><span class="co">{-</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true" tabindex="-1"></a><span class="co">[...]</span></span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true" tabindex="-1"></a><span class="co">-}</span></span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Arbitrary</span> a <span class="ot">=&gt;</span> <span class="dt">Arbitrary</span> (<span class="dt">Lam</span> a) <span class="kw">where</span></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true" tabindex="-1"></a>  arbitrary <span class="ot">=</span> sized <span class="op">$</span> \n <span class="ot">-&gt;</span> genTerm n []</span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true" tabindex="-1"></a><span class="ot">      genTerm ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> [a] <span class="ot">-&gt;</span> <span class="dt">Gen</span> (<span class="dt">Term</span> a)</span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true" tabindex="-1"></a>      genTerm size scope</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true" tabindex="-1"></a>        <span class="op">|</span> size <span class="op">&lt;=</span> <span class="dv">0</span> <span class="ot">=</span> genVar scope</span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true" tabindex="-1"></a>        <span class="op">|</span> <span class="fu">otherwise</span> <span class="ot">=</span> frequency</span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true" tabindex="-1"></a>            [ (<span class="dv">3</span>, genVar scope)</span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true" tabindex="-1"></a>            , (<span class="dv">2</span>, genLam size scope)</span>
<span id="cb6-14"><a href="#cb6-14" aria-hidden="true" tabindex="-1"></a>            , (<span class="dv">1</span>, genApp size scope)</span>
<span id="cb6-15"><a href="#cb6-15" aria-hidden="true" tabindex="-1"></a>            ]</span>
<span id="cb6-16"><a href="#cb6-16" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb6-17"><a href="#cb6-17" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Pick either a variable in scope or a fresh arbitrary one.</span></span>
<span id="cb6-18"><a href="#cb6-18" aria-hidden="true" tabindex="-1"></a><span class="ot">      genVar ::</span> [a] <span class="ot">-&gt;</span> <span class="dt">Gen</span> (<span class="dt">Term</span> a)</span>
<span id="cb6-19"><a href="#cb6-19" aria-hidden="true" tabindex="-1"></a>      genVar [] <span class="ot">=</span> <span class="dt">Var</span> <span class="op">&lt;$&gt;</span> arbitrary</span>
<span id="cb6-20"><a href="#cb6-20" aria-hidden="true" tabindex="-1"></a>      genVar scope <span class="ot">=</span> oneof</span>
<span id="cb6-21"><a href="#cb6-21" aria-hidden="true" tabindex="-1"></a>        [ <span class="dt">Var</span> <span class="op">&lt;$&gt;</span> elements scope</span>
<span id="cb6-22"><a href="#cb6-22" aria-hidden="true" tabindex="-1"></a>        , <span class="dt">Var</span> <span class="op">&lt;$&gt;</span> arbitrary</span>
<span id="cb6-23"><a href="#cb6-23" aria-hidden="true" tabindex="-1"></a>        ]</span>
<span id="cb6-24"><a href="#cb6-24" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb6-25"><a href="#cb6-25" aria-hidden="true" tabindex="-1"></a><span class="ot">      genLam ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> [a] <span class="ot">-&gt;</span> <span class="dt">Gen</span> (<span class="dt">Term</span> a)</span>
<span id="cb6-26"><a href="#cb6-26" aria-hidden="true" tabindex="-1"></a>      genLam size scope <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb6-27"><a href="#cb6-27" aria-hidden="true" tabindex="-1"></a>        binder <span class="ot">&lt;-</span> arbitrary</span>
<span id="cb6-28"><a href="#cb6-28" aria-hidden="true" tabindex="-1"></a>        body   <span class="ot">&lt;-</span> genTerm (size <span class="ot">`div`</span> <span class="dv">2</span>) (binder <span class="op">:</span> scope)</span>
<span id="cb6-29"><a href="#cb6-29" aria-hidden="true" tabindex="-1"></a>        <span class="fu">pure</span> <span class="op">$</span> <span class="dt">Lam</span> binder body</span>
<span id="cb6-30"><a href="#cb6-30" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb6-31"><a href="#cb6-31" aria-hidden="true" tabindex="-1"></a><span class="ot">      genApp ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> [a] <span class="ot">-&gt;</span> <span class="dt">Gen</span> (<span class="dt">Term</span> a)</span>
<span id="cb6-32"><a href="#cb6-32" aria-hidden="true" tabindex="-1"></a>      genApp size scope <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb6-33"><a href="#cb6-33" aria-hidden="true" tabindex="-1"></a>        <span class="kw">let</span> size&#39; <span class="ot">=</span> size <span class="ot">`div`</span> <span class="dv">2</span></span>
<span id="cb6-34"><a href="#cb6-34" aria-hidden="true" tabindex="-1"></a>        <span class="dt">App</span> <span class="op">&lt;$&gt;</span> genTerm size&#39; scope <span class="op">&lt;*&gt;</span> genTerm size&#39; scope</span>
<span id="cb6-35"><a href="#cb6-35" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb6-36"><a href="#cb6-36" aria-hidden="true" tabindex="-1"></a><span class="co">-- |</span></span>
<span id="cb6-37"><a href="#cb6-37" aria-hidden="true" tabindex="-1"></a><span class="co">-- fromLocalyNameless is a left inverse to toLocallyNameless.</span></span>
<span id="cb6-38"><a href="#cb6-38" aria-hidden="true" tabindex="-1"></a><span class="ot">fromLocallyNamelessLeftInverse ::</span> (<span class="dt">Ord</span> a, <span class="dt">Show</span> a) <span class="ot">=&gt;</span> <span class="dt">Lam</span> a <span class="ot">-&gt;</span> <span class="dt">Property</span></span>
<span id="cb6-39"><a href="#cb6-39" aria-hidden="true" tabindex="-1"></a>fromLocallyNamelessLeftInverse e <span class="ot">=</span></span>
<span id="cb6-40"><a href="#cb6-40" aria-hidden="true" tabindex="-1"></a>  (fromLocallyNameless <span class="op">.</span> toLocallyNameless) e <span class="op">===</span> e</span></code></pre></div>
<p>Thankfully, this does indeed work as expected:</p>
<pre class="shell"><code>Tests
  Property Tests
    fromLocallyNameless ∘ toLocallyNameless == id: OK (0.35s)
      +++ OK, passed 1000 tests.</code></pre>
<p>Now that we have terms in locally nameless representation, we can
perform substitution in a fairly straightforward manner. In the
McBride–McKinna (MM) paper, they refer to this operation as
<strong>“instantiate”</strong>. It is also common in the locally
nameless literature to call the operation <strong>“opening”</strong> or
<strong>“open”</strong> because it involves opening the body of a term
to substitute for its outermost bound variable. As this accords with our
intuitions on the meaning of substitution of locally nameless terms, we
will follow this convention.</p>
<p>Note that in the code below, we follow (at least in spirit) the MM
approach of using a scope type to denote a term that is only legal as
the body of an expression (i.e. a term which may have bound variables
referring to a non-existant outer binder such as <span
class="math inline">\(\lambda \mspace{5mu}. \mspace{5mu}1\)</span>). In
our case, we only use a type synonym; however, in a more substantial
implementation, one should use a newtype to get the type safety that
such a measure confers.</p>
<div class="sourceCode" id="cb8"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true" tabindex="-1"></a><span class="kw">type</span> <span class="dt">Scope</span> f x <span class="ot">=</span> f x</span></code></pre></div>
<p>Before defining <code>open</code> we need a helper <code>shift</code>
that adjusts bound variable indices when a term is moved to a different
binding depth. When we substitute <code>image</code> at depth
<code>outer</code>, any bound variables in <code>image</code> that refer
to binders <em>outside</em> it need their indices incremented by
<code>outer</code> to remain valid in the new context:</p>
<div class="sourceCode" id="cb9"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- Shift all bound variable indices &gt;= cutoff by `amount`.</span></span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true" tabindex="-1"></a><span class="ot">shift ::</span> <span class="kw">forall</span> a <span class="op">.</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true" tabindex="-1"></a>shift amount <span class="ot">=</span> shiftFrom <span class="dv">0</span></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true" tabindex="-1"></a><span class="ot">    shiftFrom ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true" tabindex="-1"></a>    shiftFrom cutoff <span class="ot">=</span> \<span class="kw">case</span></span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Var</span> v <span class="ot">-&gt;</span> <span class="kw">case</span> v <span class="kw">of</span></span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true" tabindex="-1"></a>        <span class="dt">B</span> bv <span class="op">|</span> bv <span class="op">&gt;=</span> cutoff <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">B</span> (bv <span class="op">+</span> amount))</span>
<span id="cb9-9"><a href="#cb9-9" aria-hidden="true" tabindex="-1"></a>             <span class="op">|</span> <span class="fu">otherwise</span>    <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">B</span> bv)</span>
<span id="cb9-10"><a href="#cb9-10" aria-hidden="true" tabindex="-1"></a>        <span class="dt">F</span> fv <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">F</span> fv)</span>
<span id="cb9-11"><a href="#cb9-11" aria-hidden="true" tabindex="-1"></a>      <span class="dt">App</span> l r <span class="ot">-&gt;</span> <span class="dt">App</span> (shiftFrom cutoff l) (shiftFrom cutoff r)</span>
<span id="cb9-12"><a href="#cb9-12" aria-hidden="true" tabindex="-1"></a>      <span class="dt">Lam</span> n b <span class="ot">-&gt;</span> <span class="dt">Lam</span> n (shiftFrom (cutoff <span class="op">+</span> <span class="dv">1</span>) b)</span></code></pre></div>
<div class="sourceCode" id="cb10"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true" tabindex="-1"></a>                    <span class="co">-- ┌─── term we are substituting</span></span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true" tabindex="-1"></a>                    <span class="co">-- │</span></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true" tabindex="-1"></a>                    <span class="co">-- │                 ┌─── body we are substituting into</span></span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true" tabindex="-1"></a>                    <span class="co">-- │                 │</span></span>
<span id="cb10-5"><a href="#cb10-5" aria-hidden="true" tabindex="-1"></a>                    <span class="co">-- │                 │</span></span>
<span id="cb10-6"><a href="#cb10-6" aria-hidden="true" tabindex="-1"></a><span class="ot">open ::</span> <span class="kw">forall</span> a <span class="op">.</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Scope</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb10-7"><a href="#cb10-7" aria-hidden="true" tabindex="-1"></a>open image <span class="ot">=</span> go <span class="dv">0</span> <span class="co">-- the bound variable begins at 0</span></span>
<span id="cb10-8"><a href="#cb10-8" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb10-9"><a href="#cb10-9" aria-hidden="true" tabindex="-1"></a><span class="ot">    go ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb10-10"><a href="#cb10-10" aria-hidden="true" tabindex="-1"></a>    go outer <span class="ot">=</span></span>
<span id="cb10-11"><a href="#cb10-11" aria-hidden="true" tabindex="-1"></a>      \<span class="kw">case</span></span>
<span id="cb10-12"><a href="#cb10-12" aria-hidden="true" tabindex="-1"></a>        <span class="dt">Var</span> fbv <span class="ot">-&gt;</span></span>
<span id="cb10-13"><a href="#cb10-13" aria-hidden="true" tabindex="-1"></a>          <span class="kw">case</span> fbv <span class="kw">of</span></span>
<span id="cb10-14"><a href="#cb10-14" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- if the bound variable refers to the outer binder of the body</span></span>
<span id="cb10-15"><a href="#cb10-15" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- of the term then we substitute the image, shifting its indices</span></span>
<span id="cb10-16"><a href="#cb10-16" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- to account for the binding depth at the substitution site.</span></span>
<span id="cb10-17"><a href="#cb10-17" aria-hidden="true" tabindex="-1"></a>            <span class="dt">B</span> bv <span class="op">|</span> bv <span class="op">==</span> outer <span class="ot">-&gt;</span> shift outer image</span>
<span id="cb10-18"><a href="#cb10-18" aria-hidden="true" tabindex="-1"></a>                 <span class="co">-- variables above the substituted binder move one step closer</span></span>
<span id="cb10-19"><a href="#cb10-19" aria-hidden="true" tabindex="-1"></a>                 <span class="op">|</span> bv <span class="op">&gt;</span> outer  <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">B</span> (bv <span class="op">-</span> <span class="dv">1</span>))</span>
<span id="cb10-20"><a href="#cb10-20" aria-hidden="true" tabindex="-1"></a>                 <span class="op">|</span> <span class="fu">otherwise</span>   <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">B</span> bv)</span>
<span id="cb10-21"><a href="#cb10-21" aria-hidden="true" tabindex="-1"></a>            <span class="dt">F</span> fv <span class="ot">-&gt;</span> <span class="dt">Var</span> (<span class="dt">F</span> fv)</span>
<span id="cb10-22"><a href="#cb10-22" aria-hidden="true" tabindex="-1"></a>        <span class="dt">App</span> l r <span class="ot">-&gt;</span> <span class="dt">App</span> (go outer l) (go outer r)</span>
<span id="cb10-23"><a href="#cb10-23" aria-hidden="true" tabindex="-1"></a>                <span class="co">-- Note that as we have gone under another binder we must</span></span>
<span id="cb10-24"><a href="#cb10-24" aria-hidden="true" tabindex="-1"></a>                <span class="co">-- in turn bump the binding variable we substitute for</span></span>
<span id="cb10-25"><a href="#cb10-25" aria-hidden="true" tabindex="-1"></a>                <span class="co">-- so that it still refers to the outermost binder.</span></span>
<span id="cb10-26"><a href="#cb10-26" aria-hidden="true" tabindex="-1"></a>        <span class="dt">Lam</span> n b <span class="ot">-&gt;</span> <span class="dt">Lam</span> n (go (outer <span class="op">+</span> <span class="dv">1</span>) b)</span></code></pre></div>
<p>From here it is easy for us to implement reduction to both normal
form and weak-head normal form (where we use call-by-name semantics). We
will, in both cases, write a function that does all of the work using
locally nameless terms and functions that make use of that work on named
lambda terms via the previously defined conversion functions:</p>
<div class="sourceCode" id="cb11"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true" tabindex="-1"></a><span class="ot">whnfLN ::</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true" tabindex="-1"></a>whnfLN term <span class="ot">=</span> go term []</span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- ┌─── current leftmost lambda term</span></span>
<span id="cb11-5"><a href="#cb11-5" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- │</span></span>
<span id="cb11-6"><a href="#cb11-6" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- │             ┌─── list of collected arguments</span></span>
<span id="cb11-7"><a href="#cb11-7" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- │             │</span></span>
<span id="cb11-8"><a href="#cb11-8" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- │             │</span></span>
<span id="cb11-9"><a href="#cb11-9" aria-hidden="true" tabindex="-1"></a><span class="ot">    go ::</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> [<span class="dt">Term</span> (<span class="dt">Var</span> a)] <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb11-10"><a href="#cb11-10" aria-hidden="true" tabindex="-1"></a>    go t as <span class="ot">=</span></span>
<span id="cb11-11"><a href="#cb11-11" aria-hidden="true" tabindex="-1"></a>      <span class="kw">case</span> (t, as) <span class="kw">of</span></span>
<span id="cb11-12"><a href="#cb11-12" aria-hidden="true" tabindex="-1"></a>        ((<span class="dt">App</span> l r), args)</span>
<span id="cb11-13"><a href="#cb11-13" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- if we encounter an application then we collect the</span></span>
<span id="cb11-14"><a href="#cb11-14" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- argument on the right and recurse into the left term</span></span>
<span id="cb11-15"><a href="#cb11-15" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> go l (r <span class="op">:</span> args)</span>
<span id="cb11-16"><a href="#cb11-16" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- We only perform substitution if we have both</span></span>
<span id="cb11-17"><a href="#cb11-17" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- a non-empty list of arguments to substitute and</span></span>
<span id="cb11-18"><a href="#cb11-18" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- a leftmost lambda term.</span></span>
<span id="cb11-19"><a href="#cb11-19" aria-hidden="true" tabindex="-1"></a>        ((<span class="dt">Lam</span> _ body) , a<span class="op">:</span>args)</span>
<span id="cb11-20"><a href="#cb11-20" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- Note that we substitute the body before evaluation</span></span>
<span id="cb11-21"><a href="#cb11-21" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- and hence we follow call-by-name semantics.</span></span>
<span id="cb11-22"><a href="#cb11-22" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> go (substitute a body) args</span>
<span id="cb11-23"><a href="#cb11-23" aria-hidden="true" tabindex="-1"></a>        _</span>
<span id="cb11-24"><a href="#cb11-24" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- otherwise we encountered no further leftmost</span></span>
<span id="cb11-25"><a href="#cb11-25" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- lambda terms and so we re-apply App to the</span></span>
<span id="cb11-26"><a href="#cb11-26" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- built-up list of arguments</span></span>
<span id="cb11-27"><a href="#cb11-27" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> foldl&#39; <span class="dt">App</span> t as</span>
<span id="cb11-28"><a href="#cb11-28" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-29"><a href="#cb11-29" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-30"><a href="#cb11-30" aria-hidden="true" tabindex="-1"></a><span class="ot">whnf ::</span> (<span class="dt">Ord</span> a, <span class="dt">Increment</span> a) <span class="ot">=&gt;</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> a</span>
<span id="cb11-31"><a href="#cb11-31" aria-hidden="true" tabindex="-1"></a>    <span class="co">-- defer the work to the locally nameless terms</span></span>
<span id="cb11-32"><a href="#cb11-32" aria-hidden="true" tabindex="-1"></a>whnf <span class="ot">=</span> fromLocallyNameless <span class="op">.</span> whnfLN <span class="op">.</span> toLocallyNameless</span>
<span id="cb11-33"><a href="#cb11-33" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-34"><a href="#cb11-34" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-35"><a href="#cb11-35" aria-hidden="true" tabindex="-1"></a><span class="ot">nfLN ::</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb11-36"><a href="#cb11-36" aria-hidden="true" tabindex="-1"></a>nfLN term <span class="ot">=</span> go term []</span>
<span id="cb11-37"><a href="#cb11-37" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb11-38"><a href="#cb11-38" aria-hidden="true" tabindex="-1"></a><span class="ot">    go ::</span> <span class="dt">Term</span> (<span class="dt">Var</span> a) <span class="ot">-&gt;</span> [<span class="dt">Term</span> (<span class="dt">Var</span> a)] <span class="ot">-&gt;</span> <span class="dt">Term</span> (<span class="dt">Var</span> a)</span>
<span id="cb11-39"><a href="#cb11-39" aria-hidden="true" tabindex="-1"></a>    go t as <span class="ot">=</span></span>
<span id="cb11-40"><a href="#cb11-40" aria-hidden="true" tabindex="-1"></a>      <span class="kw">case</span> (t, as) <span class="kw">of</span></span>
<span id="cb11-41"><a href="#cb11-41" aria-hidden="true" tabindex="-1"></a>        ((<span class="dt">App</span> l r), args)</span>
<span id="cb11-42"><a href="#cb11-42" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- the same as above we collect right arguments in a list.</span></span>
<span id="cb11-43"><a href="#cb11-43" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> go l (r <span class="op">:</span> args)</span>
<span id="cb11-44"><a href="#cb11-44" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- If we have no arguments to apply to a lambda then we</span></span>
<span id="cb11-45"><a href="#cb11-45" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- recurse into the body (this is the difference between</span></span>
<span id="cb11-46"><a href="#cb11-46" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- normal form and weak head normal form).</span></span>
<span id="cb11-47"><a href="#cb11-47" aria-hidden="true" tabindex="-1"></a>        ((<span class="dt">Lam</span> n body) , [])</span>
<span id="cb11-48"><a href="#cb11-48" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> (<span class="dt">Lam</span> n (nfLN body))</span>
<span id="cb11-49"><a href="#cb11-49" aria-hidden="true" tabindex="-1"></a>        ((<span class="dt">Lam</span> _ body) , a<span class="op">:</span>args)</span>
<span id="cb11-50"><a href="#cb11-50" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> go (substitute a body) args</span>
<span id="cb11-51"><a href="#cb11-51" aria-hidden="true" tabindex="-1"></a>        _</span>
<span id="cb11-52"><a href="#cb11-52" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- If we encounter no further lambdas then we reduce </span></span>
<span id="cb11-53"><a href="#cb11-53" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- each of our built-up arguments before re-applying App.</span></span>
<span id="cb11-54"><a href="#cb11-54" aria-hidden="true" tabindex="-1"></a>          <span class="ot">-&gt;</span> foldl&#39; <span class="dt">App</span> t (<span class="fu">fmap</span> nfLN as)</span>
<span id="cb11-55"><a href="#cb11-55" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-56"><a href="#cb11-56" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb11-57"><a href="#cb11-57" aria-hidden="true" tabindex="-1"></a><span class="ot">nf ::</span> (<span class="dt">Ord</span> a, <span class="dt">Increment</span> a) <span class="ot">=&gt;</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> a</span>
<span id="cb11-58"><a href="#cb11-58" aria-hidden="true" tabindex="-1"></a>  <span class="co">-- again we defer all the actual work to locally nameless terms.</span></span>
<span id="cb11-59"><a href="#cb11-59" aria-hidden="true" tabindex="-1"></a>nf <span class="ot">=</span> fromLocallyNameless <span class="op">.</span> nfLN <span class="op">.</span> toLocallyNameless</span></code></pre></div>
<p>Now that we have written this reduction code, we should test it. But
what on? A natural choice is to consider church encodings of the natural
numbers. Since all we have in the lambda calculus is a theory of
functions (with no base data types), we must encode any data in the form
of functions. Church numerals act as a prototypical example of such an
encoding:</p>
<p><span class="math display">\[ \mathrm{zero} = \lambda
\mspace{5mu}\mathrm{s} \mspace{5mu}. \mspace{5mu}\lambda
\mspace{5mu}\mathrm{z} \mspace{5mu}. \mspace{5mu}\mathrm{z} \]</span>
<span class="math display">\[ \mathrm{succ}\mspace{5mu}\mathrm{n} =
\lambda \mspace{5mu}\mathrm{s} \mspace{5mu}. \mspace{5mu}\lambda
\mspace{5mu}\mathrm{z} \mspace{5mu}. \mspace{5mu}\mathrm{s}
\mspace{5mu}(\mathrm{n} \mspace{5mu}\mathrm{s} \mspace{5mu}\mathrm{z})
\]</span></p>
<p>The idea here is that zero takes as arguments a step function <span
class="math inline">\(\mathrm{s}\)</span> and a starting value <span
class="math inline">\(\mathrm{z}\)</span>, returning the starting value.
A positive number n, on the other hand, takes those arguments and
applies the step function to the starting value n times. Here is what
this looks like using our named terms:</p>
<div class="sourceCode" id="cb12"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true" tabindex="-1"></a><span class="ot">{-# LANGUAGE OverloadedStrings #-}</span></span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- [...]</span></span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- We give ourselves some handy infix syntax for apply.</span></span>
<span id="cb12-4"><a href="#cb12-4" aria-hidden="true" tabindex="-1"></a><span class="kw">infixl</span> <span class="dv">5</span> <span class="op">.$</span></span>
<span id="cb12-5"><a href="#cb12-5" aria-hidden="true" tabindex="-1"></a><span class="ot">(.$) ::</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> a <span class="ot">-&gt;</span> <span class="dt">Term</span> a</span>
<span id="cb12-6"><a href="#cb12-6" aria-hidden="true" tabindex="-1"></a>(<span class="op">.$</span>) <span class="ot">=</span> <span class="dt">App</span></span>
<span id="cb12-7"><a href="#cb12-7" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb12-8"><a href="#cb12-8" aria-hidden="true" tabindex="-1"></a><span class="co">-- |</span></span>
<span id="cb12-9"><a href="#cb12-9" aria-hidden="true" tabindex="-1"></a><span class="co">-- The unary natural numbers.</span></span>
<span id="cb12-10"><a href="#cb12-10" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="dt">Nat</span> <span class="ot">=</span> <span class="dt">Z</span> <span class="op">|</span> <span class="dt">S</span> <span class="dt">Nat</span></span>
<span id="cb12-11"><a href="#cb12-11" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb12-12"><a href="#cb12-12" aria-hidden="true" tabindex="-1"></a><span class="co">-- Notice here in the inductive case we reduce to normal form. </span></span>
<span id="cb12-13"><a href="#cb12-13" aria-hidden="true" tabindex="-1"></a><span class="co">-- Not doing so leads to a subtly different term wherein we </span></span>
<span id="cb12-14"><a href="#cb12-14" aria-hidden="true" tabindex="-1"></a><span class="co">-- are applying &quot;S&quot; to a term that itself is a lambda term</span></span>
<span id="cb12-15"><a href="#cb12-15" aria-hidden="true" tabindex="-1"></a><span class="co">-- applied to two arguments but not yet β-reduced.</span></span>
<span id="cb12-16"><a href="#cb12-16" aria-hidden="true" tabindex="-1"></a><span class="ot">fromNat ::</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Term</span> <span class="dt">Text</span></span>
<span id="cb12-17"><a href="#cb12-17" aria-hidden="true" tabindex="-1"></a>fromNat <span class="dt">Z</span>     <span class="ot">=</span> <span class="dt">Lam</span> <span class="st">&quot;S&quot;</span> (<span class="dt">Lam</span> <span class="st">&quot;Z&quot;</span> <span class="st">&quot;Z&quot;</span>)</span>
<span id="cb12-18"><a href="#cb12-18" aria-hidden="true" tabindex="-1"></a>fromNat (<span class="dt">S</span> n) <span class="ot">=</span> <span class="dt">Lam</span> <span class="st">&quot;S&quot;</span> (<span class="dt">Lam</span> <span class="st">&quot;Z&quot;</span> (<span class="st">&quot;S&quot;</span> <span class="op">.$</span> (nf <span class="op">$</span> fromNat n <span class="op">.$</span> <span class="st">&quot;S&quot;</span> <span class="op">.$</span> <span class="st">&quot;Z&quot;</span>)))</span>
<span id="cb12-19"><a href="#cb12-19" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb12-20"><a href="#cb12-20" aria-hidden="true" tabindex="-1"></a><span class="co">-- Let us also give ourselves names for the first </span></span>
<span id="cb12-21"><a href="#cb12-21" aria-hidden="true" tabindex="-1"></a><span class="co">-- few church numerals for convenience:</span></span>
<span id="cb12-22"><a href="#cb12-22" aria-hidden="true" tabindex="-1"></a>cZero  <span class="ot">=</span> fromNat <span class="dt">Z</span></span>
<span id="cb12-23"><a href="#cb12-23" aria-hidden="true" tabindex="-1"></a>cOne   <span class="ot">=</span> fromNat (<span class="dt">S</span> <span class="dt">Z</span>)</span>
<span id="cb12-24"><a href="#cb12-24" aria-hidden="true" tabindex="-1"></a>cTwo   <span class="ot">=</span> fromNat (<span class="dt">S</span> (<span class="dt">S</span> <span class="dt">Z</span>))</span>
<span id="cb12-25"><a href="#cb12-25" aria-hidden="true" tabindex="-1"></a>cThree <span class="ot">=</span> fromNat (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> <span class="dt">Z</span>)))</span>
<span id="cb12-26"><a href="#cb12-26" aria-hidden="true" tabindex="-1"></a>cFour  <span class="ot">=</span> fromNat (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> <span class="dt">Z</span>))))</span>
<span id="cb12-27"><a href="#cb12-27" aria-hidden="true" tabindex="-1"></a>cFive  <span class="ot">=</span> fromNat (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> <span class="dt">Z</span>)))))</span>
<span id="cb12-28"><a href="#cb12-28" aria-hidden="true" tabindex="-1"></a>cSix   <span class="ot">=</span> fromNat (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> (<span class="dt">S</span> <span class="dt">Z</span>))))))</span></code></pre></div>
<p>Now recall that we wish to test how our code <em>evaluates</em> to
normal form and thus we should consider some functions to actually run.
Two such functions that come to mind are addition and multiplication.
But how are these defined for Church numerals? Remember that <span
class="math inline">\(\mathrm{n}\)</span> is meant to represent applying
a step function <span class="math inline">\(\mathrm{n}\)</span> times to
a value. If the value we apply to the step function is the result of
applying a step function argument <span
class="math inline">\(\mathrm{s}\)</span> to a starting value <span
class="math inline">\(\mathrm{m}\)</span> times, we see that this is
operationally the same as <span class="math inline">\(\mathrm{m} +
\mathrm{n}\)</span>. In lambda terms:</p>
<p><span class="math display">\[ \mathrm{add} := \lambda \mathrm{n}
\mspace{5mu}. \mspace{5mu}\lambda \mspace{5mu}\mathrm{m} \mspace{5mu}.
\mspace{5mu}
      \lambda \mathrm{s} \mspace{5mu}. \mspace{5mu}\lambda
\mspace{5mu}\mathrm{z} \mspace{5mu}. \mspace{5mu}
      \mathrm{n} \mspace{5mu}\mathrm{s} \mspace{5mu}(\mathrm{m}
\mspace{5mu}\mathrm{s} \mspace{5mu}\mathrm{z} )
  \]</span></p>
<p>Similarly, we can define multiplication of <span
class="math inline">\(\mathrm{m}\)</span> by <span
class="math inline">\(\mathrm{n}\)</span> by applying <span
class="math inline">\(\mathrm{n}\)</span> to the step funtion <span
class="math inline">\((\mathrm{m} \mspace{5mu}s)\)</span> (the <span
class="math inline">\(\mathrm{m}\)</span>-fold application of <span
class="math inline">\(\mathrm{s}\)</span>) and starting value <span
class="math inline">\(\mathrm{z}\)</span>:</p>
<p><span class="math display">\[ \mathrm{mult}:= \lambda \mathrm{n}
\mspace{5mu}. \mspace{5mu}\lambda \mspace{5mu}\mathrm{m} \mspace{5mu}.
\mspace{5mu}
      \lambda \mathrm{s} \mspace{5mu}. \mspace{5mu}\lambda
\mspace{5mu}\mathrm{z} \mspace{5mu}. \mspace{5mu}
      \mathrm{n} \mspace{5mu}(\mathrm{m} \mspace{5mu}\mathrm{s})
\mspace{5mu}z
  \]</span></p>
<p>Let us translate this into Haskell:</p>
<div class="sourceCode" id="cb13"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true" tabindex="-1"></a><span class="ot">churchAdd ::</span> <span class="dt">Term</span> <span class="dt">Text</span></span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true" tabindex="-1"></a>churchAdd <span class="ot">=</span></span>
<span id="cb13-3"><a href="#cb13-3" aria-hidden="true" tabindex="-1"></a>  <span class="dt">Lam</span> <span class="st">&quot;n&quot;</span> </span>
<span id="cb13-4"><a href="#cb13-4" aria-hidden="true" tabindex="-1"></a>    (<span class="dt">Lam</span> <span class="st">&quot;m&quot;</span> </span>
<span id="cb13-5"><a href="#cb13-5" aria-hidden="true" tabindex="-1"></a>      (<span class="dt">Lam</span> <span class="st">&quot;S&quot;</span> (<span class="dt">Lam</span> <span class="st">&quot;Z&quot;</span> ((<span class="dt">App</span> <span class="st">&quot;n&quot;</span> <span class="st">&quot;S&quot;</span>) <span class="op">.$</span> (<span class="st">&quot;m&quot;</span> <span class="op">.$</span> <span class="st">&quot;S&quot;</span> <span class="op">.$</span> <span class="st">&quot;Z&quot;</span>)))))</span>
<span id="cb13-6"><a href="#cb13-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb13-7"><a href="#cb13-7" aria-hidden="true" tabindex="-1"></a><span class="ot">churchMult ::</span> <span class="dt">Term</span> <span class="dt">Text</span></span>
<span id="cb13-8"><a href="#cb13-8" aria-hidden="true" tabindex="-1"></a>churchMult <span class="ot">=</span> </span>
<span id="cb13-9"><a href="#cb13-9" aria-hidden="true" tabindex="-1"></a>  <span class="dt">Lam</span> <span class="st">&quot;n&quot;</span> </span>
<span id="cb13-10"><a href="#cb13-10" aria-hidden="true" tabindex="-1"></a>    (<span class="dt">Lam</span> <span class="st">&quot;m&quot;</span> </span>
<span id="cb13-11"><a href="#cb13-11" aria-hidden="true" tabindex="-1"></a>      (<span class="dt">Lam</span> <span class="st">&quot;S&quot;</span> (<span class="dt">Lam</span> <span class="st">&quot;Z&quot;</span> (<span class="st">&quot;n&quot;</span> <span class="op">.$</span> (<span class="st">&quot;m&quot;</span> <span class="op">.$</span> <span class="st">&quot;S&quot;</span>) <span class="op">.$</span> <span class="st">&quot;Z&quot;</span>))))</span></code></pre></div>
<p>As a first check let us see that <span class="math inline">\(2 + 2
\rightsquigarrow 4\)</span> and that <span class="math inline">\(2 * 3
\rightsquigarrow 6\)</span>:</p>
<div class="sourceCode" id="cb14"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Test.Tasty.HUnit</span>      <span class="kw">as</span> <span class="dt">HU</span></span>
<span id="cb14-2"><a href="#cb14-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- [...]</span></span>
<span id="cb14-3"><a href="#cb14-3" aria-hidden="true" tabindex="-1"></a><span class="ot">unitTests ::</span> <span class="dt">TestTree</span></span>
<span id="cb14-4"><a href="#cb14-4" aria-hidden="true" tabindex="-1"></a>unitTests <span class="ot">=</span> testGroup <span class="st">&quot;Church Arithmetic Unit Tests&quot;</span></span>
<span id="cb14-5"><a href="#cb14-5" aria-hidden="true" tabindex="-1"></a>  [ HU.testCase</span>
<span id="cb14-6"><a href="#cb14-6" aria-hidden="true" tabindex="-1"></a>      <span class="st">&quot;2 + 2 ⇝ 4&quot;</span> <span class="op">$</span></span>
<span id="cb14-7"><a href="#cb14-7" aria-hidden="true" tabindex="-1"></a>      (nf <span class="op">$</span> (churchAdd <span class="op">.$</span> cTwo) <span class="op">.$</span> cTwo) <span class="op">@?=</span> cFour</span>
<span id="cb14-8"><a href="#cb14-8" aria-hidden="true" tabindex="-1"></a>  , HU.testCase</span>
<span id="cb14-9"><a href="#cb14-9" aria-hidden="true" tabindex="-1"></a>      <span class="st">&quot;2 * 3 ⇝ 6&quot;</span> <span class="op">$</span></span>
<span id="cb14-10"><a href="#cb14-10" aria-hidden="true" tabindex="-1"></a>      (nf <span class="op">$</span> churchMult <span class="op">.$</span> cTwo <span class="op">.$</span> cThree) <span class="op">@?=</span> cSix</span>
<span id="cb14-11"><a href="#cb14-11" aria-hidden="true" tabindex="-1"></a>  ]</span></code></pre></div>
<p>This is fortunately the case:</p>
<div class="sourceCode" id="cb15"><pre
class="sourceCode bash"><code class="sourceCode bash"><span id="cb15-1"><a href="#cb15-1" aria-hidden="true" tabindex="-1"></a><span class="ex">Church</span> Arithmetic Unit Tests</span>
<span id="cb15-2"><a href="#cb15-2" aria-hidden="true" tabindex="-1"></a>    <span class="ex">2</span> + 2 ⇝ 4:                         OK</span>
<span id="cb15-3"><a href="#cb15-3" aria-hidden="true" tabindex="-1"></a>    <span class="ex">2</span> <span class="pp">*</span> 3 ⇝ 6:                         OK</span></code></pre></div>
<p>For a slightly more robust test, we can also write property tests to
check that addition and multiplication are each commutative. First we
will want to have an arbitrary instance for our definition of the unary
natural numbers:</p>
<div class="sourceCode" id="cb16"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb16-1"><a href="#cb16-1" aria-hidden="true" tabindex="-1"></a><span class="fu">fromInt</span><span class="ot"> ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Nat</span></span>
<span id="cb16-2"><a href="#cb16-2" aria-hidden="true" tabindex="-1"></a><span class="fu">fromInt</span> <span class="dv">0</span> <span class="ot">=</span> <span class="dt">Z</span></span>
<span id="cb16-3"><a href="#cb16-3" aria-hidden="true" tabindex="-1"></a><span class="fu">fromInt</span> n <span class="ot">=</span> <span class="dt">S</span> (<span class="fu">fromInt</span> (n <span class="op">-</span> <span class="dv">1</span>))</span>
<span id="cb16-4"><a href="#cb16-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb16-5"><a href="#cb16-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- We convert from a randomly generated integer</span></span>
<span id="cb16-6"><a href="#cb16-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- between 0 and 50.</span></span>
<span id="cb16-7"><a href="#cb16-7" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span> <span class="dt">Arbitrary</span> <span class="dt">Nat</span> <span class="kw">where</span></span>
<span id="cb16-8"><a href="#cb16-8" aria-hidden="true" tabindex="-1"></a>    arbitrary <span class="ot">=</span></span>
<span id="cb16-9"><a href="#cb16-9" aria-hidden="true" tabindex="-1"></a>      <span class="kw">do</span></span>
<span id="cb16-10"><a href="#cb16-10" aria-hidden="true" tabindex="-1"></a>        i <span class="ot">&lt;-</span> choose (<span class="dv">0</span>,<span class="dv">50</span>)</span>
<span id="cb16-11"><a href="#cb16-11" aria-hidden="true" tabindex="-1"></a>        <span class="fu">pure</span> <span class="op">$</span> <span class="fu">fromInt</span> i</span></code></pre></div>
<p>Now we can write our properties as follows:</p>
<div class="sourceCode" id="cb17"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb17-1"><a href="#cb17-1" aria-hidden="true" tabindex="-1"></a><span class="kw">import</span> <span class="dt">Test.Tasty.QuickCheck</span> <span class="kw">as</span> <span class="dt">QC</span></span>
<span id="cb17-2"><a href="#cb17-2" aria-hidden="true" tabindex="-1"></a><span class="co">{-</span></span>
<span id="cb17-3"><a href="#cb17-3" aria-hidden="true" tabindex="-1"></a><span class="co">[...]</span></span>
<span id="cb17-4"><a href="#cb17-4" aria-hidden="true" tabindex="-1"></a><span class="co">-}</span></span>
<span id="cb17-5"><a href="#cb17-5" aria-hidden="true" tabindex="-1"></a><span class="ot">additionIsCommutative ::</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Property</span></span>
<span id="cb17-6"><a href="#cb17-6" aria-hidden="true" tabindex="-1"></a>additionIsCommutative n m <span class="ot">=</span></span>
<span id="cb17-7"><a href="#cb17-7" aria-hidden="true" tabindex="-1"></a>      nf (churchAdd <span class="op">.$</span> fromNat n <span class="op">.$</span> fromNat m)</span>
<span id="cb17-8"><a href="#cb17-8" aria-hidden="true" tabindex="-1"></a>  <span class="op">===</span> nf (churchAdd <span class="op">.$</span> fromNat m <span class="op">.$</span> fromNat n)</span>
<span id="cb17-9"><a href="#cb17-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb17-10"><a href="#cb17-10" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb17-11"><a href="#cb17-11" aria-hidden="true" tabindex="-1"></a><span class="ot">multiplicationIsCommutative ::</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Nat</span> <span class="ot">-&gt;</span> <span class="dt">Property</span></span>
<span id="cb17-12"><a href="#cb17-12" aria-hidden="true" tabindex="-1"></a>multiplicationIsCommutative n m <span class="ot">=</span></span>
<span id="cb17-13"><a href="#cb17-13" aria-hidden="true" tabindex="-1"></a>      nf (churchMult <span class="op">.$</span> fromNat n <span class="op">.$</span> fromNat m)</span>
<span id="cb17-14"><a href="#cb17-14" aria-hidden="true" tabindex="-1"></a>  <span class="op">===</span> nf (churchMult <span class="op">.$</span> fromNat m <span class="op">.$</span> fromNat n)</span>
<span id="cb17-15"><a href="#cb17-15" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb17-16"><a href="#cb17-16" aria-hidden="true" tabindex="-1"></a><span class="ot">churchProperties ::</span> [<span class="dt">TestTree</span>]</span>
<span id="cb17-17"><a href="#cb17-17" aria-hidden="true" tabindex="-1"></a>churchProperties <span class="ot">=</span></span>
<span id="cb17-18"><a href="#cb17-18" aria-hidden="true" tabindex="-1"></a>  [ QC.testProperty</span>
<span id="cb17-19"><a href="#cb17-19" aria-hidden="true" tabindex="-1"></a>      <span class="st">&quot;Addition of Church numerals is commutative&quot;</span></span>
<span id="cb17-20"><a href="#cb17-20" aria-hidden="true" tabindex="-1"></a>     (withMaxSuccess <span class="dv">100</span> additionIsCommutative)</span>
<span id="cb17-21"><a href="#cb17-21" aria-hidden="true" tabindex="-1"></a>  , QC.testProperty</span>
<span id="cb17-22"><a href="#cb17-22" aria-hidden="true" tabindex="-1"></a>      <span class="st">&quot;Multiplication of Church numerals is commutative&quot;</span></span>
<span id="cb17-23"><a href="#cb17-23" aria-hidden="true" tabindex="-1"></a>     (withMaxSuccess <span class="dv">100</span> multiplicationIsCommutative)</span>
<span id="cb17-24"><a href="#cb17-24" aria-hidden="true" tabindex="-1"></a>  ]</span></code></pre></div>
<p>Running this gives us the following:</p>
<div class="sourceCode" id="cb18"><pre
class="sourceCode bash"><code class="sourceCode bash"><span id="cb18-1"><a href="#cb18-1" aria-hidden="true" tabindex="-1"></a>    <span class="ex">Addition</span> of Church numerals is commutative:       OK <span class="er">(</span><span class="ex">0.05s</span><span class="kw">)</span></span>
<span id="cb18-2"><a href="#cb18-2" aria-hidden="true" tabindex="-1"></a>      <span class="ex">+++</span> OK, passed 100 tests.</span>
<span id="cb18-3"><a href="#cb18-3" aria-hidden="true" tabindex="-1"></a>    <span class="ex">Multiplication</span> of Church numerals is commutative: OK <span class="er">(</span><span class="ex">0.07s</span><span class="kw">)</span></span>
<span id="cb18-4"><a href="#cb18-4" aria-hidden="true" tabindex="-1"></a>      <span class="ex">+++</span> OK, passed 100 tests.</span></code></pre></div>
<p>It looks like our implementation might be (close to) working as
hoped! Phew.</p>
<p>We should note that one downside to our version of locally nameless
terms is that there are syntacticaly valid terms in our grammar which,
nevertheless, do not make sense as lambda terms. For example, the
following term is perfectly valid in our grammar: <span
class="math display">\[
    \lambda \mspace{5mu}. \mspace{5mu}\lambda \mspace{5mu}.
\mspace{5mu}(1 \mspace{5mu}3)
  \]</span></p>
<p>Here there is a bound variable <span class="math inline">\(3\)</span>
but the binding depth is only 1 (counting from <span
class="math inline">\(0\)</span>). We would like, instead, to use the
type system to enforce that each of our terms is (intrinsically) a valid
lambda term. Doing this in Haskell is quite a challenge as such an
endeavour necessarily involves types which keep track of the maximum
current binding variable, and thus dependent types. In our next post we
will see how to do this in Agda and then prove various properties of our
locally nameless terms.</p>
<p>Thank you for reading! Feel free to contact me <a
href="mailto:callan.mcgill@gmail.com">here</a> with questions, thoughts,
ideas, or all of the above.</p>]]></content>
  </entry>
  <entry>
      <title>On Characterizing Nat in Agda</title>
      <link href="https://boarders.github.ioposts/peano.html"/>
      <id>https://boarders.github.ioposts/peano.html</id>
      <updated>2020-10-06T00:00:SZ</updated>
      <category term="Agda"/>
      <category term="Natural Numbers"/>
      <summary>Explorations into characterizing natural numbers</summary>
      <content type="html"><![CDATA[The 19th century brought about an unprecedented revolution in the
foundations of mathematics culminating in the Zermelo–Frankel (ZF)
axioms. The ZF axioms gave a logical basis to Cantor’s <em>set
theory</em> as a foundational theory, in which the main branches of
mathematics (analysis, topology, geometry and number theory) could be
encoded and thusly understood. It was through this newly-systematized
approach that the axiomatic method was born. This is of historical and
philosophical interest as it marks a decisive structuralist turn in
mathematical thought. It is at this time that mathematical objects begin
to be characterized not by any particular construction, but by the
collection of axioms (or laws) an object satisfies.
<div style="display:none">
()
</div>
<p>Consider a paradigmatic case: characterizing the natural numbers. In
this post we will explore two different formulations of the natural
numbers, the first of which is given by Peano in the late 19th century,
and the second of which is a more categorical approach inspired by
Lawvere. This will provide us a springboard upon which to explore
<em>how</em> we can formulate and reason about mathematical structures
in Agda. Since Agda is based on dependent type theory, our formulations
will be <a
href="https://en.wikipedia.org/wiki/Constructivism_(philosophy_of_mathematics)"><em>constructive</em></a>.
This means our constructions will be <em>proof relevant</em>. In this
setting, proofs are first-class entities and as such our algebraic
structures will encode both the operations a structure has along with
proofs of the properties that it satisfies.</p>
<p>We will assume the reader has some familiarity with Agda (with
relevant materials noted throughout) and has an interest in formalizing
mathematics.</p>
<p>Let us begin with Peano’s axioms. A set <span
class="math inline">\(\mathsf{N}\)</span> satisfies Peano’s axioms if
the following properties hold:</p>
<ul>
<li>There exist terms <span class="math inline">\(0 \in
\mathsf{N}\)</span> and <span class="math inline">\(\mathrm{s} :
\mathsf{N}\rightarrow \mathsf{N}\)</span>.</li>
<li><span class="math inline">\(\mathsf{N}\)</span> carries an
equivalence relation <span
class="math inline">\(\simeq\;\subset\mathsf{N}\times\mathsf{N}\)</span>
(to be explained below).</li>
<li><span class="math inline">\(\mathrm{s}\)</span> reflects <span
class="math inline">\(\simeq\)</span>-equivalence: <span
class="math display">\[ \forall \; \mathrm{n}, \mathrm{m} \in
\mathsf{N}\; . \; \mathrm{s} (\mathrm{n}) \simeq \mathrm{s} (\mathrm{m})
\Rightarrow \mathrm{m} \simeq \mathrm{n} \]</span></li>
<li><span class="math inline">\(0\)</span> is never (equivalent to) the
successor of any number: <span class="math display">\[ \nexists \;
\mathrm{n} \in \mathbb{N} \; . \; \mathrm{s}(n) \simeq 0 \]</span></li>
<li><span class="math inline">\(\mathsf{N}\)</span> satisfies induction:
if <span class="math inline">\(\phi : \mathsf{N}\rightarrow
\mathbb{B}\)</span> is a predicate and the following conditions hold:
<ul>
<li><span class="math inline">\(\phi(0)\)</span> is true.</li>
<li><span class="math inline">\(\forall \; \mathrm{n} \in \mathbb{N} \;
. \; \phi(n) \Rightarrow \phi(\mathrm{s}(\mathrm{n}))\)</span></li>
</ul>
then <span class="math inline">\(\phi\)</span> is true for all <span
class="math inline">\(\mathrm{n} \in \mathsf{N}\)</span>.</li>
</ul>
<p>How can we prove that this uniquely determines the natural numbers?
Our strategy would go roughly as follows:</p>
<ul>
<li>Give a particular construction of <span
class="math inline">\(\mathbb{N}\)</span> showing it satisfies the
axioms.</li>
<li>For any set <span class="math inline">\(\mathsf{N}\)</span>
satisfying the axioms, construct maps: <span
class="math display">\[\begin{aligned}
  \mathrm{from} &amp;: \mathbb{N} \rightarrow \mathsf{N} \\
  \mathrm{to}   &amp;: \mathsf{N} \rightarrow \mathbb{N} \\
\end{aligned}\]</span></li>
<li>Use induction with the following predicates: <span
class="math display">\[\begin{aligned}
  \phi_{\mathbb{N}}(n) &amp;= n \simeq_\mathbb{N} \mathrm{to} \circ
\mathrm{from}(n) \\
  \phi_{\mathsf{N}}(n) &amp;= n \simeq_\mathsf{N} \mathrm{to} \circ
\mathrm{from} (n)
\end{aligned}\]</span> to show the these maps form an equivalence.</li>
</ul>
<p>Let’s try to formalise this constructively in Agda. We start with a
few imports we will need later:</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> <dfn>Peano</dfn> <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Relation<span class="ot">.</span>Binary<span class="ot">.</span>PropositionalEquality</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(_</span>≡<span class="ot">_;</span> <span class="cn">refl</span><span class="ot">;</span> <span class="ot">_</span>≢<span class="ot">_;</span> cong<span class="ot">;</span> <dfn>trans</dfn><span class="ot">;</span> sym<span class="ot">)</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Function</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">using</span> <span class="ot">(_</span>∘<span class="ot">_)</span></span></code></pre></div>
<p>Typically, we define the (unary) natural numbers as follows:</p>
<div class="sourceCode" id="cb2"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>ℕ</dfn> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">Zero</span> <span class="ot">:</span> <dfn>ℕ</dfn></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a>  <span class="cn">Succ</span> <span class="ot">:</span> <dfn>ℕ</dfn> <span class="ot">-&gt;</span> <dfn>ℕ</dfn></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true" tabindex="-1"></a><span class="pp">{-# BUILTIN NATURAL <dfn>ℕ</dfn> #-}</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- This allows us to use numeric literals.</span></span></code></pre></div>
<p>Our first port of call is to formulate equivalence relations. In Agda
we usually encode algebraic structures as <a
href="https://agda.readthedocs.io/en/v2.6.1.1/language/record-types.html">records</a>.
As mentioned in the introduction, since proofs are first-class, we carry
around not just the structure of the binary relation but also the
properties it satisfies:</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>EqRel</dfn> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set₁</span> <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a>    <span class="ot">_</span><dfn>≃</dfn><span class="ot">_</span> <span class="ot">:</span> A <span class="ot">→</span> A <span class="ot">→</span> <span class="dt">Set</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true" tabindex="-1"></a>    <dfn>reflexivity</dfn>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a <span class="ot">:</span> A<span class="ot">}</span>     <span class="ot">→</span> a <dfn>≃</dfn> a</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true" tabindex="-1"></a>    <span class="cn">symmetry</span>     <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b <span class="ot">:</span> A<span class="ot">}</span>   <span class="ot">→</span> a <dfn>≃</dfn> b <span class="ot">→</span> b <dfn>≃</dfn> a</span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true" tabindex="-1"></a>    <span class="cn">transitivity</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b c <span class="ot">:</span> A<span class="ot">}</span> <span class="ot">→</span> a <dfn>≃</dfn> b <span class="ot">→</span> b <dfn>≃</dfn> c <span class="ot">→</span> a <dfn>≃</dfn> c</span></code></pre></div>
<p>We note that this definition is slightly different from what is
typical in mathematics. Rather than a <em>subset</em> of the diagonal,
we make use of a two-argument dependent type <span
class="math inline">\(\_ \simeq \_\)</span>. Given <span
class="math inline">\(\mathrm{a}, \mathrm{b} : \mathrm{A}\)</span>, the
type <span class="math inline">\(\mathrm{a}\;\simeq\;\mathrm{b}\)</span>
gives the collection of <em>evidence</em> that <span
class="math inline">\(\mathrm{a}\)</span> and <span
class="math inline">\(\mathrm{b}\)</span> are equal. The axioms this
satisfies are reflexivity (that any element is equal to itself) symmetry
(that we can freely reverse equalities) and transitivity (that we can
compose equalities).</p>
<p>Let’s show that Agda’s built-in equality type, <span
class="math inline">\(\equiv\)</span>, is an equivalence relation on
<span class="math inline">\(\mathbb{N}\)</span>. As a brief reminder,
here is how the equality type is defined, if we ignore <a
href="https://agda.readthedocs.io/en/v2.6.1.1/language/universe-levels.html">level
polymorphism</a>:</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>≡<span class="ot">_</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">(</span>x <span class="ot">:</span> A<span class="ot">)</span> <span class="ot">:</span> A <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">refl</span> <span class="ot">:</span> x ≡ x</span></code></pre></div>
<p>That is to say, we can give a term <span
class="math inline">\(\mathrm{refl}\)</span> of type <span
class="math inline">\(\mathrm{a} \equiv \mathrm{b}\)</span> so long as
Agda can directly compute that <span
class="math inline">\(\mathrm{a}\)</span> and <span
class="math inline">\(\mathrm{b}\)</span> are equal within the
particular context. For example, if we define addition:</p>
<div class="sourceCode" id="cb5"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>+</dfn><span class="ot">_</span> <span class="ot">:</span> <dfn>ℕ</dfn> <span class="ot">-&gt;</span> <dfn>ℕ</dfn> <span class="ot">-&gt;</span> <dfn>ℕ</dfn></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>+</dfn><span class="ot">_</span> <span class="cn">Zero</span>     m <span class="ot">=</span> m</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>+</dfn><span class="ot">_</span> <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> m <span class="ot">=</span> <span class="cn">Succ</span> <span class="ot">(</span>n <dfn>+</dfn> m<span class="ot">)</span></span></code></pre></div>
<p>then we can give the following (unnamed) definition:</p>
<div class="sourceCode" id="cb6"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span> <span class="ot">:</span> <span class="dv">2</span> <dfn>+</dfn> <span class="dv">2</span> ≡ <span class="dv">4</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span> <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>as Agda can compute that both sides are equal.</p>
<p>Coming back to <span class="math inline">\(\mathbb{N}\)</span>, let’s
show how <span class="math inline">\(\equiv\)</span> satisfies the
properties of an equivalence relation:</p>
<div class="sourceCode" id="cb7"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-refl</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>n <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">}</span> <span class="ot">→</span> n ≡ n</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-refl</dfn> <span class="ot">=</span> <span class="cn">refl</span></span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-symm</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>n m <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">}</span> <span class="ot">→</span> n ≡ m <span class="ot">→</span> m ≡ n</span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-symm</dfn> n≡m <span class="kw">rewrite</span> n≡m <span class="ot">=</span> <span class="cn">refl</span></span>
<span id="cb7-6"><a href="#cb7-6" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb7-7"><a href="#cb7-7" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-trans</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>m n r <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">}</span> <span class="ot">→</span> m ≡ n <span class="ot">→</span> n ≡ r <span class="ot">→</span> m ≡ r</span>
<span id="cb7-8"><a href="#cb7-8" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-trans</dfn>  m≡n n≡r <span class="kw">rewrite</span> m≡n <span class="ot">|</span> n≡r <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>Here we make use of Agda’s <a
href="https://agda.readthedocs.io/en/v2.6.1/language/with-abstraction.html#with-rewrite">rewrite
construction</a>. By providing an equality proof of the form <span
class="math inline">\(\mathrm{a} \equiv \mathrm{b}\)</span>, the rewrite
construction will replace subexpressions in the goal of the form <span
class="math inline">\(\mathrm{a}\)</span> with <span
class="math inline">\(\mathrm{b}\)</span>. For example, in <span
class="math inline">\(\mathbb{N}\mathrm{-symm}\)</span>, we use the
equality term we are given, <span class="math inline">\(\mathrm{n}
\equiv \mathrm{m}\)</span>, as an argument to rewrite so that each
appearance of <span class="math inline">\(\mathrm{n}\)</span> is
replaced with <span class="math inline">\(\mathrm{m}\)</span>. At this
point we may fill the hole with <span
class="math inline">\(\mathrm{refl} : \mathrm{m} \equiv
\mathrm{m}\)</span>.</p>
<p>It is worth noting that we haven’t used anything special about <span
class="math inline">\(\mathbb{N}\)</span> and these same definitions
would work to prove that <em>any</em> set forms an equivalence relation
under <span class="math inline">\(\equiv\)</span>.</p>
<p>Now we can write an instance of EqRel for <span
class="math inline">\(\mathbb{N}\)</span>:</p>
<div class="sourceCode" id="cb8"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <dfn>EqRel</dfn> <span class="ot">{{...}}</span> <span class="kw">public</span></span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span></span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true" tabindex="-1"></a>  <dfn>≡-Nat</dfn> <span class="ot">:</span> <dfn>EqRel</dfn> <dfn>ℕ</dfn></span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true" tabindex="-1"></a>  <dfn>≡-Nat</dfn> <span class="ot">=</span></span>
<span id="cb8-6"><a href="#cb8-6" aria-hidden="true" tabindex="-1"></a>    <span class="kw">record</span></span>
<span id="cb8-7"><a href="#cb8-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">{</span> <span class="ot">_</span><dfn>≃</dfn><span class="ot">_</span>          <span class="ot">=</span> <span class="ot">_</span>≡<span class="ot">_</span></span>
<span id="cb8-8"><a href="#cb8-8" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <dfn>reflexivity</dfn>  <span class="ot">=</span> <dfn>ℕ-refl</dfn></span>
<span id="cb8-9"><a href="#cb8-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="cn">symmetry</span>     <span class="ot">=</span> <dfn>ℕ-symm</dfn></span>
<span id="cb8-10"><a href="#cb8-10" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="cn">transitivity</span> <span class="ot">=</span> <dfn>ℕ-trans</dfn></span>
<span id="cb8-11"><a href="#cb8-11" aria-hidden="true" tabindex="-1"></a>    <span class="ot">}</span></span></code></pre></div>
<p>Here we use Agda’s <a
href="https://agda.readthedocs.io/en/v2.6.1.1/language/instance-arguments.html">instance
arguments</a> mechanism, the analog to Haskell’s typeclass instances. We
start by bringing the fields of EqRel into scope for those instances
which can be resolved. This is essentially equivalent to us defining
top-level functions of the form:</p>
<div class="sourceCode" id="cb9"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≃</dfn><span class="ot">_</span>         <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> A<span class="ot">}}</span> <span class="ot">→</span> A <span class="ot">→</span> A <span class="ot">→</span> <span class="dt">Set</span></span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true" tabindex="-1"></a><dfn>reflexivity</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> A<span class="ot">}}</span> <span class="ot">{</span>a b <span class="ot">:</span> A<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>a <dfn>≃</dfn> a<span class="ot">)</span></span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- etc.</span></span></code></pre></div>
<p>Implicit arguments <span class="math inline">\(\{\{\_ :
\mathrm{EqRel}\; \mathrm{A}\}\}\)</span> are resolved by searching for
instances we have available in scope. In particular, we define an
instance of EqRel for <span class="math inline">\(\mathbb{N}\)</span>
which means that we may use these methods on <span
class="math inline">\(\mathbb{N}\)</span> and Agda will infer the
instance we have provided.</p>
<p>Now we are in a position to formalise the Peano axioms. In much the
same way as we have done with equivalence relations, we again use
records to encode algebraic structure:</p>
<div class="sourceCode" id="cb10"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>Peano</dfn> <span class="ot">(</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">{{</span>rel <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">:</span> <span class="dt">Set₁</span> <span class="kw">where</span></span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true" tabindex="-1"></a>    <span class="cn">zero</span>        <span class="ot">:</span> N</span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true" tabindex="-1"></a>    <span class="cn">succ</span>        <span class="ot">:</span> N <span class="ot">→</span> N</span>
<span id="cb10-5"><a href="#cb10-5" aria-hidden="true" tabindex="-1"></a>    <span class="cn">s-injective</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b <span class="ot">:</span> N<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span><span class="cn">succ</span> a<span class="ot">)</span> <dfn>≃</dfn> <span class="ot">(</span><span class="cn">succ</span> b<span class="ot">)</span> <span class="ot">→</span> a <dfn>≃</dfn> b</span>
<span id="cb10-6"><a href="#cb10-6" aria-hidden="true" tabindex="-1"></a>    zero≠succ   <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">(</span>a <span class="ot">:</span> N<span class="ot">)</span> <span class="ot">→</span> <span class="cn">zero</span> <dfn>≃</dfn> <span class="ot">(</span><span class="cn">succ</span> a<span class="ot">)</span> <span class="ot">→</span> ⊥</span>
<span id="cb10-7"><a href="#cb10-7" aria-hidden="true" tabindex="-1"></a>    <span class="cn">induction</span>   <span class="ot">:</span></span>
<span id="cb10-8"><a href="#cb10-8" aria-hidden="true" tabindex="-1"></a>       <span class="ot">∀</span> <span class="ot">(</span>P <span class="ot">:</span> N <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>a <span class="ot">:</span> N<span class="ot">)</span></span>
<span id="cb10-9"><a href="#cb10-9" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>z <span class="ot">:</span> P <span class="cn">zero</span><span class="ot">)</span></span>
<span id="cb10-10"><a href="#cb10-10" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>s <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>b <span class="ot">:</span> N<span class="ot">}</span> <span class="ot">→</span> P b <span class="ot">→</span> P <span class="ot">(</span><span class="cn">succ</span> b<span class="ot">))</span></span>
<span id="cb10-11"><a href="#cb10-11" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> P a</span>
<span id="cb10-12"><a href="#cb10-12" aria-hidden="true" tabindex="-1"></a>    <span class="cn">induction-zero</span> <span class="ot">:</span></span>
<span id="cb10-13"><a href="#cb10-13" aria-hidden="true" tabindex="-1"></a>       <span class="ot">∀</span> <span class="ot">(</span>P <span class="ot">:</span> N <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span></span>
<span id="cb10-14"><a href="#cb10-14" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>z <span class="ot">:</span> P <span class="cn">zero</span><span class="ot">)</span></span>
<span id="cb10-15"><a href="#cb10-15" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>s <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>b <span class="ot">:</span> N<span class="ot">}</span> <span class="ot">→</span> P b <span class="ot">→</span> P <span class="ot">(</span><span class="cn">succ</span> b<span class="ot">))</span></span>
<span id="cb10-16"><a href="#cb10-16" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span><span class="cn">induction</span> P <span class="cn">zero</span> z s ≡ z<span class="ot">)</span></span>
<span id="cb10-17"><a href="#cb10-17" aria-hidden="true" tabindex="-1"></a>    <span class="cn">induction-succ</span> <span class="ot">:</span></span>
<span id="cb10-18"><a href="#cb10-18" aria-hidden="true" tabindex="-1"></a>       <span class="ot">∀</span> <span class="ot">(</span>P <span class="ot">:</span> N <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>a <span class="ot">:</span> N<span class="ot">)</span></span>
<span id="cb10-19"><a href="#cb10-19" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>z <span class="ot">:</span> <span class="ot">(</span>P <span class="cn">zero</span><span class="ot">))</span></span>
<span id="cb10-20"><a href="#cb10-20" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span>s <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>b <span class="ot">:</span> N<span class="ot">}</span> <span class="ot">→</span> P b <span class="ot">→</span> P <span class="ot">(</span><span class="cn">succ</span> b<span class="ot">))</span></span>
<span id="cb10-21"><a href="#cb10-21" aria-hidden="true" tabindex="-1"></a>       <span class="ot">→</span> <span class="ot">(</span><span class="cn">induction</span> P <span class="ot">(</span><span class="cn">succ</span> a<span class="ot">)</span> z s ≡ s <span class="ot">(</span><span class="cn">induction</span> P a z s<span class="ot">))</span></span></code></pre></div>
<p>Several things are worth noting about this defintion:</p>
<ul>
<li>We again make use of instance arguments so that the input type <span
class="math inline">\(\mathsf{N}\)</span> has the structure of an
equivalence relation. This is somewhat similar to a typeclass extension
definition in Haskell:</li>
</ul>
<div class="sourceCode" id="cb11"><pre
class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true" tabindex="-1"></a>    <span class="kw">class</span> <span class="dt">Eq</span> a <span class="ot">=&gt;</span> <span class="dt">Ord</span> a</span></code></pre></div>
<ul>
<li>We upgrade induction from a unary predicate <span
class="math inline">\(\phi : \mathsf{N}\rightarrow \mathbb{B}\)</span>
to a dependent type <span class="math inline">\(\mathrm{P} :
\mathsf{N}\rightarrow \mathrm{Set}\)</span>.</li>
<li>As we will see later, we would like this upgraded principle to be
able to <em>compute</em>. As such we add two laws that dictate how
computation should unfold.</li>
</ul>
<p>Let’s now prove that <span class="math inline">\(\mathbb{N}\)</span>
satisfies induction and injectivity of <span
class="math inline">\(\mathrm{Succ}\)</span>:</p>
<div class="sourceCode" id="cb12"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-induction</dfn> <span class="ot">:</span></span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">(</span>P <span class="ot">:</span> <dfn>ℕ</dfn> <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>a <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">)</span></span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>P <span class="cn">Zero</span><span class="ot">)</span></span>
<span id="cb12-4"><a href="#cb12-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">{</span>b <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>P b<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>P <span class="ot">(</span><span class="cn">Succ</span> b<span class="ot">)))</span></span>
<span id="cb12-5"><a href="#cb12-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>P a<span class="ot">)</span></span>
<span id="cb12-6"><a href="#cb12-6" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-induction</dfn> P <span class="cn">Zero</span> p[<span class="cn">zero</span>] p[<span class="cn">succ</span>] <span class="ot">=</span> p[<span class="cn">zero</span>]</span>
<span id="cb12-7"><a href="#cb12-7" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-induction</dfn> P <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> p[<span class="cn">zero</span>] p[<span class="cn">succ</span>]</span>
<span id="cb12-8"><a href="#cb12-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> p[<span class="cn">succ</span>] <span class="ot">(</span><dfn>ℕ-induction</dfn> P n p[<span class="cn">zero</span>] p[<span class="cn">succ</span>]<span class="ot">)</span></span>
<span id="cb12-9"><a href="#cb12-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb12-10"><a href="#cb12-10" aria-hidden="true" tabindex="-1"></a><dfn>Succ-inj</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>n m <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> ≡ <span class="ot">(</span><span class="cn">Succ</span> m<span class="ot">)</span> <span class="ot">→</span> n ≡ m</span>
<span id="cb12-11"><a href="#cb12-11" aria-hidden="true" tabindex="-1"></a><dfn>Succ-inj</dfn> <span class="cn">refl</span> <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>This is much as we might expect, induction is identical to the
recursion principle for <span class="math inline">\(\mathbb{N}\)</span>
and <span class="math inline">\(\mathrm{Succ}\)</span>-<span
class="math inline">\(\mathrm{inj}\)</span> follows definitionally after
we case match on equality. We can now make <span
class="math inline">\(\mathbb{N}\)</span> an instance of <span
class="math inline">\(\mathrm{Peano}\)</span>:</p>
<pre><code>instance
  ℕ-Peano : Peano ℕ
  ℕ-Peano =
    record
      { zero           = Zero
      ; succ           = Succ
      ; s-injective    = Succ-inj
      ; zero≠succ      = λ n ()
      ; induction      = ℕ-induction
      ; induction-zero = λ P z s   → refl
      ; induction-succ = λ P a z s → refl
      }</code></pre>
<p>In the last two cases, the <span
class="math inline">\(\mathrm{induction}\)</span> laws hold
definitionally from how we have defined <span
class="math inline">\(\mathbb{N}\)</span>-<span
class="math inline">\(\mathrm{induction}\)</span>.</p>
<p>Now, suppose <span class="math inline">\(\mathsf{N}\)</span> is a set
satisfying the Peano axioms, we want to then define functions to and
from <span class="math inline">\(\mathbb{N}\)</span>:</p>
<div class="sourceCode" id="cb14"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb14-2"><a href="#cb14-2" aria-hidden="true" tabindex="-1"></a><dfn>from-ℕ</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{</span> <span class="ot">_</span> <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span> <span class="ot">→</span> <dfn>ℕ</dfn> <span class="ot">→</span> N</span>
<span id="cb14-3"><a href="#cb14-3" aria-hidden="true" tabindex="-1"></a><dfn>from-ℕ</dfn> <span class="ot">{</span>N<span class="ot">}</span> n <span class="ot">=</span> <span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">-&gt;</span> N<span class="ot">)</span> n <span class="cn">zero</span> <span class="cn">succ</span></span>
<span id="cb14-4"><a href="#cb14-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb14-5"><a href="#cb14-5" aria-hidden="true" tabindex="-1"></a><dfn>to-ℕ</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span> <span class="ot">→</span> N <span class="ot">→</span> <dfn>ℕ</dfn></span>
<span id="cb14-6"><a href="#cb14-6" aria-hidden="true" tabindex="-1"></a><dfn>to-ℕ</dfn> n <span class="ot">=</span> <span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> n <span class="cn">zero</span> <span class="cn">succ</span></span></code></pre></div>
<p>Pleasantly both definitions are essentially identical, using instance
resolution to determine the relevant induction principle and values to
use. Here we see the power of constructive induction. We don’t use
induction to prove a <em>property</em> per se, but to compute. Since the
dependent types in question are constant, induction simply <em>is</em>
recursion!</p>
<p>Now we can show these maps form equivalences. To get a flavour let us
step through the development for the first equality:</p>
<div class="sourceCode" id="cb15"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb15-1"><a href="#cb15-1" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="ot">:</span></span>
<span id="cb15-2"><a href="#cb15-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{</span> <span class="ot">_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N <span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span></span>
<span id="cb15-3"><a href="#cb15-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>n <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">→</span> <dfn>to-ℕ</dfn> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(</span><dfn>from-ℕ</dfn> n<span class="ot">)</span> ≡ n</span>
<span id="cb15-4"><a href="#cb15-4" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> n <span class="ot">=</span> <span class="ot">{!!}</span></span></code></pre></div>
<p>Asking Agda for the goal gives us:</p>
<pre class="terminal"><code>Goal: Peano.induction peano (λ _ → ℕ)
      (ℕ-induction (λ _ → N) n (Peano.zero peano) (Peano.succ peano)) 0
      Succ
      ≡ n
————————————————————————————————————————————————————————————
n     : ℕ
peano : Peano N  (not in scope, instance)
_     : EqRel N    (instance)
N     : Set      (not in scope)
</code></pre>
<p>We can see that in order for <span
class="math inline">\(\mathbb{N}\)</span>-<span
class="math inline">\(\mathrm{induction}\)</span> to make progress we
need to split on <span class="math inline">\(\mathrm{n}\)</span>:</p>
<div class="sourceCode" id="cb17"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb17-1"><a href="#cb17-1" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="ot">:</span></span>
<span id="cb17-2"><a href="#cb17-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{</span> <span class="ot">_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N <span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span></span>
<span id="cb17-3"><a href="#cb17-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>n <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">→</span> <dfn>to-ℕ</dfn> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(</span><dfn>from-ℕ</dfn> n<span class="ot">)</span> ≡ n</span>
<span id="cb17-4"><a href="#cb17-4" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="cn">Zero</span>     <span class="ot">=</span> <span class="ot">{!!}</span></span>
<span id="cb17-5"><a href="#cb17-5" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> <span class="ot">=</span> <span class="ot">{!!}</span></span></code></pre></div>
<p>The new goal for <span class="math inline">\(\mathrm{Zero}\)</span>
is:</p>
<pre class="terminal"><code>Goal: Peano.induction peano (λ _ → ℕ) (Peano.zero peano) 0 Succ ≡ 0</code></pre>
<p>But this is precisely our <span
class="math inline">\(\mathrm{induction}\)</span>-<span
class="math inline">\(\mathrm{zero}\)</span> principle! Similarly we can
now use the <span
class="math inline">\(\mathrm{induction}\)</span>-<span
class="math inline">\(\mathrm{succ}\)</span> principle in the second
case and then recurse giving us:</p>
<div class="sourceCode" id="cb19"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb19-1"><a href="#cb19-1" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="ot">:</span></span>
<span id="cb19-2"><a href="#cb19-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{</span> <span class="ot">_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N <span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span></span>
<span id="cb19-3"><a href="#cb19-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>n <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">→</span> <dfn>to-ℕ</dfn> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(</span><dfn>from-ℕ</dfn> n<span class="ot">)</span> ≡ n</span>
<span id="cb19-4"><a href="#cb19-4" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="cn">Zero</span> <span class="ot">=</span>  <span class="ot">(</span><span class="cn">induction-zero</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span></span>
<span id="cb19-5"><a href="#cb19-5" aria-hidden="true" tabindex="-1"></a><dfn>to∘from</dfn> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">{{_}}</span> <span class="ot">{{</span>peano<span class="ot">}}</span> <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span></span>
<span id="cb19-6"><a href="#cb19-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">rewrite</span></span>
<span id="cb19-7"><a href="#cb19-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span><span class="cn">induction-succ</span></span>
<span id="cb19-8"><a href="#cb19-8" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span></span>
<span id="cb19-9"><a href="#cb19-9" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span><dfn>ℕ-induction</dfn> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> N<span class="ot">)</span> n <span class="ot">(</span><dfn>Peano</dfn><span class="ot">.</span><span class="cn">zero</span> peano<span class="ot">)</span> <span class="ot">(</span><dfn>Peano</dfn><span class="ot">.</span><span class="cn">succ</span> peano<span class="ot">))</span></span>
<span id="cb19-10"><a href="#cb19-10" aria-hidden="true" tabindex="-1"></a>      <span class="cn">Zero</span></span>
<span id="cb19-11"><a href="#cb19-11" aria-hidden="true" tabindex="-1"></a>      <span class="cn">Succ</span><span class="ot">)</span></span>
<span id="cb19-12"><a href="#cb19-12" aria-hidden="true" tabindex="-1"></a>  <span class="ot">|</span> <dfn>to∘from</dfn> <span class="ot">{</span>N<span class="ot">}</span> n</span>
<span id="cb19-13"><a href="#cb19-13" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>The slightly gnarly explicit arguments in the second case are to help
along the rewrite construction as it didn’t seem to cooperate with a
less verbose alternative.</p>
<p>The other proof is similarly a case of following our nose (or rather
following the typechecker). We first remind ourselves of some equality
principles we have imported above (again simplifying away level
polymorphism):</p>
<div class="sourceCode" id="cb20"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb20-1"><a href="#cb20-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- Funtions preserve equality:</span></span>
<span id="cb20-2"><a href="#cb20-2" aria-hidden="true" tabindex="-1"></a>cong</span>
<span id="cb20-3"><a href="#cb20-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{</span>B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">(</span>f <span class="ot">:</span> A <span class="ot">→</span> B<span class="ot">)</span> <span class="ot">{</span>x y <span class="ot">:</span> A<span class="ot">}</span></span>
<span id="cb20-4"><a href="#cb20-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> x ≡ y <span class="ot">→</span> f x ≡ f y</span>
<span id="cb20-5"><a href="#cb20-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb20-6"><a href="#cb20-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- The <span class="cn">transitivity</span> principle for ≡</span></span>
<span id="cb20-7"><a href="#cb20-7" aria-hidden="true" tabindex="-1"></a><dfn>trans</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b c <span class="ot">:</span> A<span class="ot">}</span> <span class="ot">→</span> a ≡ b <span class="ot">→</span> b ≡ c <span class="ot">→</span> a ≡ c</span></code></pre></div>
<p>We also will need the fact that we can lift any propositional
equality into an equivalence relation:</p>
<div class="sourceCode" id="cb21"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb21-1"><a href="#cb21-1" aria-hidden="true" tabindex="-1"></a><dfn>liftEq</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span>  <span class="ot">{{</span>r <span class="ot">:</span> <dfn>EqRel</dfn> A<span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{</span>a b <span class="ot">:</span> A<span class="ot">}</span> <span class="ot">→</span> a ≡ b <span class="ot">→</span> <span class="ot">(</span>a <dfn>≃</dfn> b<span class="ot">)</span></span>
<span id="cb21-2"><a href="#cb21-2" aria-hidden="true" tabindex="-1"></a><dfn>liftEq</dfn> <span class="cn">refl</span> <span class="ot">=</span> <dfn>reflexivity</dfn></span></code></pre></div>
<p>With these can now give the proof:</p>
<div class="sourceCode" id="cb22"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb22-1"><a href="#cb22-1" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb22-2"><a href="#cb22-2" aria-hidden="true" tabindex="-1"></a><dfn>from∘to</dfn> <span class="ot">:</span></span>
<span id="cb22-3"><a href="#cb22-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>N <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}{{</span> rel <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">→</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span> <span class="ot">→</span> <span class="ot">(</span>n <span class="ot">:</span> N<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb22-4"><a href="#cb22-4" aria-hidden="true" tabindex="-1"></a>  <dfn>from-ℕ</dfn> <span class="ot">(</span><dfn>to-ℕ</dfn> n<span class="ot">)</span> <dfn>≃</dfn> n</span>
<span id="cb22-5"><a href="#cb22-5" aria-hidden="true" tabindex="-1"></a><dfn>from∘to</dfn> <span class="ot">{</span>N<span class="ot">}</span> n <span class="ot">=</span> <dfn>liftEq</dfn> <span class="ot">(</span>prop-eq <span class="ot">{</span>N<span class="ot">})</span></span>
<span id="cb22-6"><a href="#cb22-6" aria-hidden="true" tabindex="-1"></a>             <span class="co">-- We make use of <dfn>liftEq</dfn> as we prove the</span></span>
<span id="cb22-7"><a href="#cb22-7" aria-hidden="true" tabindex="-1"></a>             <span class="co">-- stronger claim that <dfn>from-ℕ</dfn> (<dfn>to-ℕ</dfn> n) ≡ n</span></span>
<span id="cb22-8"><a href="#cb22-8" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb22-9"><a href="#cb22-9" aria-hidden="true" tabindex="-1"></a>  <span class="co">-- In the <span class="cn">zero</span> case we apply <span class="cn">induction-zero</span> underneath <dfn>from-ℕ</dfn></span></span>
<span id="cb22-10"><a href="#cb22-10" aria-hidden="true" tabindex="-1"></a>  <span class="co">-- and then use the definition of <dfn>from-ℕ</dfn>.</span></span>
<span id="cb22-11"><a href="#cb22-11" aria-hidden="true" tabindex="-1"></a>  zero-lem</span>
<span id="cb22-12"><a href="#cb22-12" aria-hidden="true" tabindex="-1"></a>    <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span></span>
<span id="cb22-13"><a href="#cb22-13" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <dfn>from-ℕ</dfn> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="cn">zero</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span> ≡ <span class="cn">zero</span></span>
<span id="cb22-14"><a href="#cb22-14" aria-hidden="true" tabindex="-1"></a>  zero-lem <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">=</span></span>
<span id="cb22-15"><a href="#cb22-15" aria-hidden="true" tabindex="-1"></a>    <span class="kw">let</span></span>
<span id="cb22-16"><a href="#cb22-16" aria-hidden="true" tabindex="-1"></a>      <dfn>pf1</dfn> <span class="ot">:</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="cn">zero</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span> ≡ <dfn>from-ℕ</dfn> <span class="cn">Zero</span></span>
<span id="cb22-17"><a href="#cb22-17" aria-hidden="true" tabindex="-1"></a>      <dfn>pf1</dfn> <span class="ot">=</span> cong <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction-zero</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span></span>
<span id="cb22-18"><a href="#cb22-18" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb22-19"><a href="#cb22-19" aria-hidden="true" tabindex="-1"></a>      <dfn>pf2</dfn> <span class="ot">:</span> <dfn>from-ℕ</dfn> <span class="cn">Zero</span> ≡ <span class="cn">zero</span></span>
<span id="cb22-20"><a href="#cb22-20" aria-hidden="true" tabindex="-1"></a>      <dfn>pf2</dfn> <span class="ot">=</span> <span class="cn">refl</span></span>
<span id="cb22-21"><a href="#cb22-21" aria-hidden="true" tabindex="-1"></a>    <span class="kw">in</span></span>
<span id="cb22-22"><a href="#cb22-22" aria-hidden="true" tabindex="-1"></a>      <dfn>trans</dfn> <dfn>pf1</dfn> <dfn>pf2</dfn></span>
<span id="cb22-23"><a href="#cb22-23" aria-hidden="true" tabindex="-1"></a>  <span class="co">-- In the successor case we similarly apply <span class="cn">induction-succ</span></span></span>
<span id="cb22-24"><a href="#cb22-24" aria-hidden="true" tabindex="-1"></a>  <span class="co">-- underneath <dfn>from-ℕ</dfn> and then recurse on the previous proof.</span></span>
<span id="cb22-25"><a href="#cb22-25" aria-hidden="true" tabindex="-1"></a>  succ-lem</span>
<span id="cb22-26"><a href="#cb22-26" aria-hidden="true" tabindex="-1"></a>    <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span> <span class="ot">(</span>prev <span class="ot">:</span> N<span class="ot">)</span></span>
<span id="cb22-27"><a href="#cb22-27" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> prev <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span> ≡ prev</span>
<span id="cb22-28"><a href="#cb22-28" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">(</span><span class="cn">succ</span> prev<span class="ot">)</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span> ≡ <span class="cn">succ</span> prev</span>
<span id="cb22-29"><a href="#cb22-29" aria-hidden="true" tabindex="-1"></a>  succ-lem prev pf <span class="ot">=</span></span>
<span id="cb22-30"><a href="#cb22-30" aria-hidden="true" tabindex="-1"></a>    <span class="kw">let</span></span>
<span id="cb22-31"><a href="#cb22-31" aria-hidden="true" tabindex="-1"></a>      <dfn>pf1</dfn> <span class="ot">:</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">(</span><span class="cn">succ</span> prev<span class="ot">)</span> <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span></span>
<span id="cb22-32"><a href="#cb22-32" aria-hidden="true" tabindex="-1"></a>          ≡ <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">Succ</span> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> prev <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">))</span></span>
<span id="cb22-33"><a href="#cb22-33" aria-hidden="true" tabindex="-1"></a>      <dfn>pf1</dfn> <span class="ot">=</span> cong <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">induction-succ</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> prev <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">)</span></span>
<span id="cb22-34"><a href="#cb22-34" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb22-35"><a href="#cb22-35" aria-hidden="true" tabindex="-1"></a>      <dfn>pf2</dfn> <span class="ot">:</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><span class="cn">Succ</span> <span class="ot">(</span><span class="cn">induction</span> <span class="ot">(λ</span> <span class="ot">_</span> <span class="ot">→</span> <dfn>ℕ</dfn><span class="ot">)</span> prev <span class="cn">Zero</span> <span class="cn">Succ</span><span class="ot">))</span> ≡ <span class="ot">(</span><span class="cn">succ</span> prev<span class="ot">)</span></span>
<span id="cb22-36"><a href="#cb22-36" aria-hidden="true" tabindex="-1"></a>      <dfn>pf2</dfn> <span class="ot">=</span> cong <span class="cn">succ</span> pf</span>
<span id="cb22-37"><a href="#cb22-37" aria-hidden="true" tabindex="-1"></a>    <span class="kw">in</span></span>
<span id="cb22-38"><a href="#cb22-38" aria-hidden="true" tabindex="-1"></a>      <dfn>trans</dfn> <dfn>pf1</dfn> <dfn>pf2</dfn></span>
<span id="cb22-39"><a href="#cb22-39" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb22-40"><a href="#cb22-40" aria-hidden="true" tabindex="-1"></a>  prop-eq</span>
<span id="cb22-41"><a href="#cb22-41" aria-hidden="true" tabindex="-1"></a>    <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>N<span class="ot">}</span> <span class="ot">{{_</span> <span class="ot">:</span> <dfn>EqRel</dfn> N<span class="ot">}}</span> <span class="ot">{{</span>peano <span class="ot">:</span> <dfn>Peano</dfn> N<span class="ot">}}</span></span>
<span id="cb22-42"><a href="#cb22-42" aria-hidden="true" tabindex="-1"></a>    <span class="ot">→</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><dfn>to-ℕ</dfn> n<span class="ot">)</span> ≡ n</span>
<span id="cb22-43"><a href="#cb22-43" aria-hidden="true" tabindex="-1"></a>  prop-eq <span class="ot">=</span></span>
<span id="cb22-44"><a href="#cb22-44" aria-hidden="true" tabindex="-1"></a>      <span class="cn">induction</span></span>
<span id="cb22-45"><a href="#cb22-45" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- we use <span class="cn">induction</span> on the principle</span></span>
<span id="cb22-46"><a href="#cb22-46" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- we are trying to show with the above</span></span>
<span id="cb22-47"><a href="#cb22-47" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- lemmas.</span></span>
<span id="cb22-48"><a href="#cb22-48" aria-hidden="true" tabindex="-1"></a>        <span class="ot">(λ</span> n <span class="ot">→</span> <dfn>from-ℕ</dfn> <span class="ot">(</span><dfn>to-ℕ</dfn>  n<span class="ot">)</span> ≡ n<span class="ot">)</span></span>
<span id="cb22-49"><a href="#cb22-49" aria-hidden="true" tabindex="-1"></a>        n</span>
<span id="cb22-50"><a href="#cb22-50" aria-hidden="true" tabindex="-1"></a>        zero-lem</span>
<span id="cb22-51"><a href="#cb22-51" aria-hidden="true" tabindex="-1"></a>        <span class="ot">(λ</span> <span class="ot">{</span>prev<span class="ot">}</span> <span class="ot">→</span> succ-lem prev<span class="ot">)</span></span></code></pre></div>
<p>This shows that any two types which satisfy the Peano axioms are
equivalent in the sense that there are maps between them which form an
isomorphism up to equivalence.</p>
<p>This is quite interesting as it stands, but we might wonder if there
is a more direct characterization of the natural numbers. After all, our
original definition as a recursive algebraic data type seems to give a
perfectly good specification of what the natural numbers <em>are</em>.
Let us turn to a characterization of <span
class="math inline">\(\mathbb{N}\)</span> given by Lawvere.</p>
<p>We define the category of discrete dynamical systems, whose:</p>
<ul>
<li>Objects are sets <span class="math inline">\(X\)</span> equipped
with a starting point <span class="math inline">\(x_0 \in X\)</span> and
a self-map <span class="math inline">\(f : X \rightarrow
X\)</span>.</li>
<li>Morphisms are functions <span class="math inline">\(\phi : X
\rightarrow Y\)</span> which take basepoint to basepoint and which
commute with the self-map: <span class="math display">\[
\begin{array}{lll}
X          &amp; \xrightarrow{\phi} &amp; Y      \\
\downarrow &amp;             &amp; \downarrow    \\
X          &amp; \xrightarrow{\phi} &amp; Y      \\
\end{array}
\]</span></li>
</ul>
<p>Lawvere then observed that the natural numbers are the <a
href="https://ncatlab.org/nlab/show/initial+object">initial object</a>
in the category of discrete dynamical systems. In other words, every
other dynamical system receives a <em>unique</em> map from the discrete
dynamical system <span class="math inline">\(\left( \mathbb{N}\; , \; 0
: \mathbb{N}\; ,\; \mathrm{s}\; : \; \mathbb{N} \rightarrow
\mathbb{N}\right)\)</span>.</p>
<p>Let us phrase this idea in terms of language more familiar to
functional programmers. First define a “pattern functor” for <span
class="math inline">\(\mathbb{N}\)</span>:</p>
<div class="sourceCode" id="cb23"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb23-1"><a href="#cb23-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>NatP</dfn> <span class="ot">(</span>r <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb23-2"><a href="#cb23-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ZeroP</span> <span class="ot">:</span> <dfn>NatP</dfn> r</span>
<span id="cb23-3"><a href="#cb23-3" aria-hidden="true" tabindex="-1"></a>  <span class="cn">SuccP</span> <span class="ot">:</span> r <span class="ot">→</span> <dfn>NatP</dfn> r</span></code></pre></div>
<p>This has the same shape as <span
class="math inline">\(\mathbb{N}\)</span> but we leave the recursion
open (this same pattern of open recursion is the animating idea behind
<a
href="https://hackage.haskell.org/package/recursion-schemes">recursion
schemes</a>). We can show that this is a functor (we don’t worry about
the functor laws):</p>
<div class="sourceCode" id="cb24"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb24-1"><a href="#cb24-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>Functor</dfn> <span class="ot">(</span>F <span class="ot">:</span> <span class="dt">Set</span> <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set₁</span> <span class="kw">where</span></span>
<span id="cb24-2"><a href="#cb24-2" aria-hidden="true" tabindex="-1"></a>  <span class="kw">constructor</span> Func</span>
<span id="cb24-3"><a href="#cb24-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb24-4"><a href="#cb24-4" aria-hidden="true" tabindex="-1"></a>    <span class="cn">Arr</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>f <span class="ot">:</span> A <span class="ot">→</span> B<span class="ot">)</span> <span class="ot">→</span> F A <span class="ot">→</span> F B</span>
<span id="cb24-5"><a href="#cb24-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb24-6"><a href="#cb24-6" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <dfn>Functor</dfn> <span class="ot">{{...}}</span> <span class="kw">public</span></span>
<span id="cb24-7"><a href="#cb24-7" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb24-8"><a href="#cb24-8" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span></span>
<span id="cb24-9"><a href="#cb24-9" aria-hidden="true" tabindex="-1"></a>  <span class="cn">NatP-Functor</span> <span class="ot">:</span> <dfn>Functor</dfn> <dfn>NatP</dfn></span>
<span id="cb24-10"><a href="#cb24-10" aria-hidden="true" tabindex="-1"></a>  <span class="cn">NatP-Functor</span> <span class="ot">=</span> Func <span class="cn">map</span></span>
<span id="cb24-11"><a href="#cb24-11" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb24-12"><a href="#cb24-12" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>A <span class="ot">→</span> B<span class="ot">)</span> <span class="ot">→</span> <dfn>NatP</dfn> A <span class="ot">→</span> <dfn>NatP</dfn> B</span>
<span id="cb24-13"><a href="#cb24-13" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map</span> f <span class="cn">ZeroP</span>     <span class="ot">=</span> <span class="cn">ZeroP</span></span>
<span id="cb24-14"><a href="#cb24-14" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map</span> f <span class="ot">(</span><span class="cn">SuccP</span> x<span class="ot">)</span> <span class="ot">=</span> <span class="cn">SuccP</span> <span class="ot">(</span>f x<span class="ot">)</span></span></code></pre></div>
<p>Functional programmers might recognise that the discrete dynamical
systems discussed above are in fact <a
href="https://en.wikipedia.org/wiki/F-algebra"><span
class="math inline">\(\mathrm{F}\)</span>-algebras</a> for this pattern
functor, which we define as follows:</p>
<div class="sourceCode" id="cb25"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb25-1"><a href="#cb25-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>Alg</dfn> <span class="ot">(</span>F <span class="ot">:</span> <span class="dt">Set</span> <span class="ot">→</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb25-2"><a href="#cb25-2" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb25-3"><a href="#cb25-3" aria-hidden="true" tabindex="-1"></a>    <span class="cn">μ</span> <span class="ot">:</span> F A <span class="ot">→</span> A</span>
<span id="cb25-4"><a href="#cb25-4" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <dfn>Alg</dfn> <span class="ot">{{...}}</span> <span class="kw">public</span></span></code></pre></div>
<p>In particular if we define discrete dynamical systems:</p>
<div class="sourceCode" id="cb26"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb26-1"><a href="#cb26-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>Dyn</dfn> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb26-2"><a href="#cb26-2" aria-hidden="true" tabindex="-1"></a>  <span class="kw">constructor</span> D</span>
<span id="cb26-3"><a href="#cb26-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb26-4"><a href="#cb26-4" aria-hidden="true" tabindex="-1"></a>    <span class="cn">basepoint</span> <span class="ot">:</span> A</span>
<span id="cb26-5"><a href="#cb26-5" aria-hidden="true" tabindex="-1"></a>    <span class="cn">self-map</span>  <span class="ot">:</span> A <span class="ot">→</span> A</span></code></pre></div>
<p>then we can show then any dynamical system gives rise to an
algebra:</p>
<div class="sourceCode" id="cb27"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb27-1"><a href="#cb27-1" aria-hidden="true" tabindex="-1"></a><dfn>from-Dyn</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">→</span> <dfn>Dyn</dfn> A <span class="ot">→</span> <dfn>Alg</dfn> <dfn>NatP</dfn> A</span>
<span id="cb27-2"><a href="#cb27-2" aria-hidden="true" tabindex="-1"></a><dfn>from-Dyn</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">(</span>D <span class="cn">basepoint</span> <span class="cn">self-map</span><span class="ot">)</span> <span class="ot">=</span> <span class="kw">record</span> <span class="ot">{</span> <span class="cn">μ</span> <span class="ot">=</span> <span class="cn">alg</span> <span class="ot">}</span></span>
<span id="cb27-3"><a href="#cb27-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb27-4"><a href="#cb27-4" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="ot">:</span> <dfn>NatP</dfn> A <span class="ot">→</span> A</span>
<span id="cb27-5"><a href="#cb27-5" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="cn">ZeroP</span>     <span class="ot">=</span> <span class="cn">basepoint</span></span>
<span id="cb27-6"><a href="#cb27-6" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="ot">(</span><span class="cn">SuccP</span> a<span class="ot">)</span> <span class="ot">=</span> <span class="cn">self-map</span> a</span></code></pre></div>
<p>and we leave, as an exercise, to show that there is an isomorphism
between <span class="math inline">\(\mathrm{NatP}\)</span> algebra
structures on <span class="math inline">\(\mathrm{A}\)</span> and
discrete dynamical system structures (an observation we can upgrade to
an isomorphism between the respective categories).</p>
<p>Let’s give the algebra structure for <span
class="math inline">\(\mathbb{N}\)</span>:</p>
<div class="sourceCode" id="cb28"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb28-1"><a href="#cb28-1" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span></span>
<span id="cb28-2"><a href="#cb28-2" aria-hidden="true" tabindex="-1"></a>  <dfn>ℕ-Alg</dfn> <span class="ot">:</span> <dfn>Alg</dfn> <dfn>NatP</dfn> <dfn>ℕ</dfn></span>
<span id="cb28-3"><a href="#cb28-3" aria-hidden="true" tabindex="-1"></a>  <dfn>ℕ-Alg</dfn> <span class="ot">=</span> <span class="kw">record</span> <span class="ot">{</span> <span class="cn">μ</span> <span class="ot">=</span> <span class="cn">alg</span> <span class="ot">}</span></span>
<span id="cb28-4"><a href="#cb28-4" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb28-5"><a href="#cb28-5" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="ot">:</span> <dfn>NatP</dfn> <dfn>ℕ</dfn> <span class="ot">→</span> <dfn>ℕ</dfn></span>
<span id="cb28-6"><a href="#cb28-6" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="cn">ZeroP</span>     <span class="ot">=</span> <span class="cn">Zero</span></span>
<span id="cb28-7"><a href="#cb28-7" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="ot">(</span><span class="cn">SuccP</span> x<span class="ot">)</span> <span class="ot">=</span> <span class="cn">Succ</span> x</span></code></pre></div>
<p>Just as in Lawvere’s characterization, we now wish to show that this
algebra is initial. For that, we first have to define maps between
algebras. Suppose <span class="math inline">\(\mathrm{A}\)</span> and
<span class="math inline">\(\mathrm{B}\)</span> are <span
class="math inline">\(\mathrm{F}\)</span>-algebras. We then say a map
<span class="math inline">\(m : \mathrm{A} \rightarrow
\mathrm{B}\)</span> is an <span
class="math inline">\(\mathrm{F}\)</span>-algebra homomorphism when the
following diagram commutes (where the downward arrows are the algebra
maps):</p>
<p><span class="math display">\[
\begin{array}{lll}
F A          &amp; \xrightarrow{F m} &amp; F B  \\
\downarrow &amp;             &amp; \downarrow   \\
A          &amp; \xrightarrow{m} &amp; B        \\
\end{array}
\]</span></p>
<p>In other words, the algebra map commutes with the map in question, or
in equations: <span class="math display">\[
f \circ \mu_{A} \equiv \mu_{B} \circ (F f)
\]</span></p>
<div class="sourceCode" id="cb29"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb29-1"><a href="#cb29-1" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> Alg-Homo <span class="ot">(</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">{</span>F <span class="ot">:</span> <span class="dt">Set</span> <span class="ot">→</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">{{</span>f <span class="ot">:</span> <dfn>Functor</dfn> F<span class="ot">}}</span></span>
<span id="cb29-2"><a href="#cb29-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>FA <span class="ot">:</span> <dfn>Alg</dfn> F A<span class="ot">)</span> <span class="ot">(</span>FB <span class="ot">:</span> <dfn>Alg</dfn> F B<span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set₁</span> <span class="kw">where</span></span>
<span id="cb29-3"><a href="#cb29-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">constructor</span> AlgHom</span>
<span id="cb29-4"><a href="#cb29-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb29-5"><a href="#cb29-5" aria-hidden="true" tabindex="-1"></a>    →-fun  <span class="ot">:</span> A <span class="ot">→</span> B</span>
<span id="cb29-6"><a href="#cb29-6" aria-hidden="true" tabindex="-1"></a>    μ-comm</span>
<span id="cb29-7"><a href="#cb29-7" aria-hidden="true" tabindex="-1"></a>      <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">(</span>fa <span class="ot">:</span> F A<span class="ot">)</span></span>
<span id="cb29-8"><a href="#cb29-8" aria-hidden="true" tabindex="-1"></a>      <span class="ot">→</span> →-fun <span class="ot">(</span><dfn>Alg</dfn><span class="ot">.</span><span class="cn">μ</span> FA fa<span class="ot">)</span> ≡ <span class="ot">(</span><dfn>Alg</dfn><span class="ot">.</span><span class="cn">μ</span> FB<span class="ot">)</span> <span class="ot">(</span><span class="cn">Arr</span> →-fun fa<span class="ot">)</span></span></code></pre></div>
<p>Now we can try to prove that the algebra structure on <span
class="math inline">\(\mathbb{N}\)</span> is initial. We first show
there is an induced map to every other <span
class="math inline">\(\mathrm{F}\)</span>-<span
class="math inline">\(\mathrm{algebra}\)</span>:</p>
<div class="sourceCode" id="cb30"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb30-1"><a href="#cb30-1" aria-hidden="true" tabindex="-1"></a>ℕ-weakly-initial</span>
<span id="cb30-2"><a href="#cb30-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span></span>
<span id="cb30-3"><a href="#cb30-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">{{</span>FA <span class="ot">:</span> <dfn>Alg</dfn> <dfn>NatP</dfn> A<span class="ot">}}</span></span>
<span id="cb30-4"><a href="#cb30-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> Alg-Homo <dfn>ℕ</dfn> A <dfn>ℕ-Alg</dfn> FA</span>
<span id="cb30-5"><a href="#cb30-5" aria-hidden="true" tabindex="-1"></a>ℕ-weakly-initial <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">=</span> AlgH <dfn>init-ℕ</dfn> <span class="cn">law</span></span>
<span id="cb30-6"><a href="#cb30-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb30-7"><a href="#cb30-7" aria-hidden="true" tabindex="-1"></a>  <dfn>init-ℕ</dfn> <span class="ot">:</span> <dfn>ℕ</dfn> <span class="ot">→</span> A</span>
<span id="cb30-8"><a href="#cb30-8" aria-hidden="true" tabindex="-1"></a>  <dfn>init-ℕ</dfn> <span class="cn">Zero</span>     <span class="ot">=</span> <span class="cn">μ</span> <span class="cn">ZeroP</span></span>
<span id="cb30-9"><a href="#cb30-9" aria-hidden="true" tabindex="-1"></a>  <dfn>init-ℕ</dfn> <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> <span class="ot">=</span> <span class="cn">μ</span> <span class="ot">(</span><span class="cn">SuccP</span> <span class="ot">(</span><dfn>init-ℕ</dfn> n<span class="ot">))</span></span>
<span id="cb30-10"><a href="#cb30-10" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb30-11"><a href="#cb30-11" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="ot">:</span> <span class="ot">(</span>nP <span class="ot">:</span> <dfn>NatP</dfn> <dfn>ℕ</dfn><span class="ot">)</span> <span class="ot">→</span> <dfn>init-ℕ</dfn> <span class="ot">(</span><span class="cn">μ</span> nP<span class="ot">)</span> ≡ <span class="cn">μ</span> <span class="ot">(</span><span class="cn">Arr</span> <dfn>init-ℕ</dfn> nP<span class="ot">)</span></span>
<span id="cb30-12"><a href="#cb30-12" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="cn">ZeroP</span>     <span class="ot">=</span> <span class="cn">refl</span></span>
<span id="cb30-13"><a href="#cb30-13" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="ot">(</span><span class="cn">SuccP</span> x<span class="ot">)</span> <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>We define the map via the structure of the algebra. <span
class="math inline">\(\mathrm{Zero}\)</span> maps to the basepoint of
<span class="math inline">\(\mathrm{A}\)</span> and for the successor we
apply the self-map and then recurse. For the <span
class="math inline">\(\mu\)</span>-<span
class="math inline">\(\mathrm{law}\)</span> we first case split as this
is how <span class="math inline">\(\mathrm{init}\)</span>-<span
class="math inline">\(\mathbb{N}\)</span> is defined. At this point we
can see that the laws hold definitionally from how we have defined the
map.</p>
<p>We can then show uniqueness:</p>
<div class="sourceCode" id="cb31"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb31-1"><a href="#cb31-1" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-init-uniq</dfn> <span class="ot">:</span></span>
<span id="cb31-2"><a href="#cb31-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span></span>
<span id="cb31-3"><a href="#cb31-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">{{</span>FA <span class="ot">:</span> <dfn>Alg</dfn> <dfn>NatP</dfn> A<span class="ot">}}</span></span>
<span id="cb31-4"><a href="#cb31-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>alg-hom <span class="ot">:</span> Alg-Homo <dfn>ℕ</dfn> A <dfn>ℕ-Alg</dfn> FA<span class="ot">)</span></span>
<span id="cb31-5"><a href="#cb31-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>n <span class="ot">:</span> <dfn>ℕ</dfn><span class="ot">)</span></span>
<span id="cb31-6"><a href="#cb31-6" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>Alg-Homo<span class="ot">.</span>→-fun alg-hom n<span class="ot">)</span></span>
<span id="cb31-7"><a href="#cb31-7" aria-hidden="true" tabindex="-1"></a>  ≡ <span class="ot">(</span>Alg-Homo<span class="ot">.</span>→-fun <span class="ot">(</span>ℕ-weakly-initial <span class="ot">{{</span>FA<span class="ot">}})</span> n<span class="ot">)</span></span>
<span id="cb31-8"><a href="#cb31-8" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-init-uniq</dfn> alg-hom <span class="cn">Zero</span> <span class="ot">=</span> μ-comm <span class="cn">ZeroP</span></span>
<span id="cb31-9"><a href="#cb31-9" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb31-10"><a href="#cb31-10" aria-hidden="true" tabindex="-1"></a>    <span class="kw">open</span> Alg-Homo alg-hom <span class="kw">public</span></span>
<span id="cb31-11"><a href="#cb31-11" aria-hidden="true" tabindex="-1"></a><dfn>ℕ-init-uniq</dfn> <span class="ot">{{</span>FA<span class="ot">}}</span> alg-hom <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> <span class="ot">=</span></span>
<span id="cb31-12"><a href="#cb31-12" aria-hidden="true" tabindex="-1"></a>  <span class="kw">let</span></span>
<span id="cb31-13"><a href="#cb31-13" aria-hidden="true" tabindex="-1"></a>    <dfn>pf1</dfn> <span class="ot">:</span>  →-fun <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> ≡ <span class="cn">μ</span> <span class="ot">(</span><span class="cn">SuccP</span> <span class="ot">(</span>→-fun n<span class="ot">))</span></span>
<span id="cb31-14"><a href="#cb31-14" aria-hidden="true" tabindex="-1"></a>    <dfn>pf1</dfn> <span class="ot">=</span> μ-comm <span class="ot">(</span><span class="cn">SuccP</span> n<span class="ot">)</span></span>
<span id="cb31-15"><a href="#cb31-15" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb31-16"><a href="#cb31-16" aria-hidden="true" tabindex="-1"></a>    <dfn>pf2</dfn> <span class="ot">:</span>  <span class="cn">μ</span> <span class="ot">(</span><span class="cn">SuccP</span> <span class="ot">(</span>→-fun n<span class="ot">))</span> ≡ <span class="cn">μ</span> <span class="ot">(</span><span class="cn">SuccP</span> <span class="ot">(</span>Alg-Homo<span class="ot">.</span>→-fun ℕ-weakly-initial n<span class="ot">))</span></span>
<span id="cb31-17"><a href="#cb31-17" aria-hidden="true" tabindex="-1"></a>    <dfn>pf2</dfn> <span class="ot">=</span> cong <span class="ot">(</span><span class="cn">μ</span> ∘ <span class="cn">SuccP</span><span class="ot">)</span> <span class="ot">(</span><dfn>ℕ-init-uniq</dfn> alg-hom n<span class="ot">)</span></span>
<span id="cb31-18"><a href="#cb31-18" aria-hidden="true" tabindex="-1"></a>  <span class="kw">in</span></span>
<span id="cb31-19"><a href="#cb31-19" aria-hidden="true" tabindex="-1"></a>    <dfn>trans</dfn> <dfn>pf1</dfn> <dfn>pf2</dfn></span>
<span id="cb31-20"><a href="#cb31-20" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb31-21"><a href="#cb31-21" aria-hidden="true" tabindex="-1"></a>    <span class="kw">open</span> Alg-Homo alg-hom <span class="kw">public</span></span></code></pre></div>
<p>We don’t infer algebra homomorphisms and so we need to pass them as
arguments and open the various records to bring their fields into scope.
In the first case we have the following goal:</p>
<div class="sourceCode" id="cb32"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb32-1"><a href="#cb32-1" aria-hidden="true" tabindex="-1"></a>→-fun <span class="dv">0</span> ≡ <span class="cn">μ</span> <span class="cn">ZeroP</span></span></code></pre></div>
<p>We note that this is definitionally equivalent to showing:</p>
<div class="sourceCode" id="cb33"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb33-1"><a href="#cb33-1" aria-hidden="true" tabindex="-1"></a>→-fun <span class="ot">(</span><span class="cn">μ</span> <span class="cn">ZeroP</span><span class="ot">)</span> ≡ <span class="cn">μ</span> <span class="ot">(</span><span class="cn">Arr</span> →-fun <span class="cn">ZeroP</span><span class="ot">)</span></span></code></pre></div>
<p>and this is the <span class="math inline">\(\mu\)</span>-<span
class="math inline">\(\mathrm{comm}\)</span> law!</p>
<p>In the successor case we have to prove:</p>
<div class="sourceCode" id="cb34"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb34-1"><a href="#cb34-1" aria-hidden="true" tabindex="-1"></a>→-fun <span class="ot">(</span><span class="cn">Succ</span> n<span class="ot">)</span> ≡ <span class="cn">μ</span> <span class="ot">(</span><span class="cn">SuccP</span> <span class="ot">(</span>Alg-Homo<span class="ot">.</span>→-fun ℕ-weakly-initial n<span class="ot">))</span></span></code></pre></div>
<p>We use a similar observation as above to first rewrite the
left-hand-side using the <span class="math inline">\(\mu\)</span>-<span
class="math inline">\(\mathrm{comm}\)</span> law. At that point (as
before) we can rewrite the inner part by recursion.</p>
<p>One nice thing about this characterization (beyond being much simpler
to reason about!) is that the same idea of initial algebra semantics
works just as well for any algebraic data type. For example we can do
exactly the same for lists as we have for <span
class="math inline">\(\mathbb{N}\)</span>:</p>
<div class="sourceCode" id="cb35"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb35-1"><a href="#cb35-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>ListP</dfn> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>r <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb35-2"><a href="#cb35-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">NilP</span>  <span class="ot">:</span> <dfn>ListP</dfn> A r</span>
<span id="cb35-3"><a href="#cb35-3" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ConsP</span> <span class="ot">:</span> A <span class="ot">→</span> r <span class="ot">→</span> <dfn>ListP</dfn> A r</span>
<span id="cb35-4"><a href="#cb35-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-5"><a href="#cb35-5" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>List</dfn> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb35-6"><a href="#cb35-6" aria-hidden="true" tabindex="-1"></a>  <span class="cn">Nil</span>  <span class="ot">:</span> <dfn>List</dfn> A</span>
<span id="cb35-7"><a href="#cb35-7" aria-hidden="true" tabindex="-1"></a>  <span class="cn">Cons</span> <span class="ot">:</span> A <span class="ot">→</span> <dfn>List</dfn> A <span class="ot">→</span> <dfn>List</dfn> A</span>
<span id="cb35-8"><a href="#cb35-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-9"><a href="#cb35-9" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span></span>
<span id="cb35-10"><a href="#cb35-10" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ListP-Functor</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">→</span>  <dfn>Functor</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span></span>
<span id="cb35-11"><a href="#cb35-11" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ListP-Functor</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">=</span> Func <span class="cn">map-L</span></span>
<span id="cb35-12"><a href="#cb35-12" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb35-13"><a href="#cb35-13" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map-L</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>B <span class="ot">→</span> C<span class="ot">)</span> <span class="ot">→</span> <dfn>ListP</dfn> A B <span class="ot">→</span> <dfn>ListP</dfn> A C</span>
<span id="cb35-14"><a href="#cb35-14" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map-L</span> f <span class="cn">NilP</span> <span class="ot">=</span> <span class="cn">NilP</span></span>
<span id="cb35-15"><a href="#cb35-15" aria-hidden="true" tabindex="-1"></a>    <span class="cn">map-L</span> f <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">)</span> <span class="ot">=</span> <span class="cn">ConsP</span> a <span class="ot">(</span>f b<span class="ot">)</span></span>
<span id="cb35-16"><a href="#cb35-16" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-17"><a href="#cb35-17" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-18"><a href="#cb35-18" aria-hidden="true" tabindex="-1"></a><span class="kw">record</span> <dfn>HasFold</dfn> <span class="ot">(</span>A <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">(</span>B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">)</span> <span class="ot">:</span> <span class="dt">Set₁</span> <span class="kw">where</span></span>
<span id="cb35-19"><a href="#cb35-19" aria-hidden="true" tabindex="-1"></a>  <span class="kw">constructor</span> F</span>
<span id="cb35-20"><a href="#cb35-20" aria-hidden="true" tabindex="-1"></a>  <span class="kw">field</span></span>
<span id="cb35-21"><a href="#cb35-21" aria-hidden="true" tabindex="-1"></a>    <span class="cn">initial</span>  <span class="ot">:</span> B</span>
<span id="cb35-22"><a href="#cb35-22" aria-hidden="true" tabindex="-1"></a>    <span class="cn">operator</span> <span class="ot">:</span> A <span class="ot">→</span> B <span class="ot">→</span> B</span>
<span id="cb35-23"><a href="#cb35-23" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-24"><a href="#cb35-24" aria-hidden="true" tabindex="-1"></a><span class="cn">fromHasFold</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">→</span>  <dfn>HasFold</dfn> A B <span class="ot">→</span> <dfn>Alg</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span> B</span>
<span id="cb35-25"><a href="#cb35-25" aria-hidden="true" tabindex="-1"></a><span class="cn">fromHasFold</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">(</span>F <span class="cn">initial</span> <span class="cn">operator</span><span class="ot">)</span> <span class="ot">=</span> <span class="kw">record</span> <span class="ot">{</span> <span class="cn">μ</span> <span class="ot">=</span> <span class="cn">alg</span> <span class="ot">}</span></span>
<span id="cb35-26"><a href="#cb35-26" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb35-27"><a href="#cb35-27" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="ot">:</span> <dfn>ListP</dfn> A B <span class="ot">→</span> B</span>
<span id="cb35-28"><a href="#cb35-28" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="cn">NilP</span> <span class="ot">=</span> <span class="cn">initial</span></span>
<span id="cb35-29"><a href="#cb35-29" aria-hidden="true" tabindex="-1"></a>  <span class="cn">alg</span> <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">)</span> <span class="ot">=</span> <span class="cn">operator</span> a b</span>
<span id="cb35-30"><a href="#cb35-30" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-31"><a href="#cb35-31" aria-hidden="true" tabindex="-1"></a><span class="cn">toHasFold</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">→</span> <dfn>Alg</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span> B <span class="ot">→</span> <dfn>HasFold</dfn> A B</span>
<span id="cb35-32"><a href="#cb35-32" aria-hidden="true" tabindex="-1"></a><span class="cn">toHasFold</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="kw">record</span> <span class="ot">{</span> <span class="cn">μ</span> <span class="ot">=</span> <span class="cn">μ</span> <span class="ot">}</span> <span class="ot">=</span> F <span class="cn">init</span> <span class="cn">op</span></span>
<span id="cb35-33"><a href="#cb35-33" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb35-34"><a href="#cb35-34" aria-hidden="true" tabindex="-1"></a>  <span class="cn">init</span> <span class="ot">:</span> B</span>
<span id="cb35-35"><a href="#cb35-35" aria-hidden="true" tabindex="-1"></a>  <span class="cn">init</span> <span class="ot">=</span> <span class="cn">μ</span> <span class="cn">NilP</span></span>
<span id="cb35-36"><a href="#cb35-36" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-37"><a href="#cb35-37" aria-hidden="true" tabindex="-1"></a>  <span class="cn">op</span> <span class="ot">:</span> A <span class="ot">→</span> B <span class="ot">→</span> B</span>
<span id="cb35-38"><a href="#cb35-38" aria-hidden="true" tabindex="-1"></a>  <span class="cn">op</span> a b <span class="ot">=</span> <span class="cn">μ</span> <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">)</span></span>
<span id="cb35-39"><a href="#cb35-39" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-40"><a href="#cb35-40" aria-hidden="true" tabindex="-1"></a><span class="kw">instance</span></span>
<span id="cb35-41"><a href="#cb35-41" aria-hidden="true" tabindex="-1"></a>  <span class="cn">List-Alg</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">→</span> <dfn>Alg</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span> <span class="ot">(</span><dfn>List</dfn> A<span class="ot">)</span></span>
<span id="cb35-42"><a href="#cb35-42" aria-hidden="true" tabindex="-1"></a>  <span class="cn">List-Alg</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">=</span> <span class="kw">record</span> <span class="ot">{</span> <span class="cn">μ</span> <span class="ot">=</span> <span class="cn">alg</span> <span class="ot">}</span></span>
<span id="cb35-43"><a href="#cb35-43" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb35-44"><a href="#cb35-44" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="ot">:</span> <dfn>ListP</dfn> A <span class="ot">(</span><dfn>List</dfn> A<span class="ot">)</span> <span class="ot">→</span> <dfn>List</dfn> A</span>
<span id="cb35-45"><a href="#cb35-45" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="cn">NilP</span> <span class="ot">=</span> <span class="cn">Nil</span></span>
<span id="cb35-46"><a href="#cb35-46" aria-hidden="true" tabindex="-1"></a>    <span class="cn">alg</span> <span class="ot">(</span><span class="cn">ConsP</span> a as<span class="ot">)</span> <span class="ot">=</span> <span class="cn">Cons</span> a as</span>
<span id="cb35-47"><a href="#cb35-47" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-48"><a href="#cb35-48" aria-hidden="true" tabindex="-1"></a><span class="cn">foldr</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>A <span class="ot">→</span> B <span class="ot">→</span> B<span class="ot">)</span> <span class="ot">→</span> B <span class="ot">→</span> <dfn>List</dfn> A <span class="ot">→</span> B</span>
<span id="cb35-49"><a href="#cb35-49" aria-hidden="true" tabindex="-1"></a><span class="cn">foldr</span> <span class="cn">op</span> <span class="cn">init</span> <span class="cn">Nil</span>         <span class="ot">=</span> <span class="cn">init</span></span>
<span id="cb35-50"><a href="#cb35-50" aria-hidden="true" tabindex="-1"></a><span class="cn">foldr</span> <span class="cn">op</span> <span class="cn">init</span> <span class="ot">(</span><span class="cn">Cons</span> a as<span class="ot">)</span> <span class="ot">=</span> <span class="cn">op</span> a <span class="ot">(</span><span class="cn">foldr</span> <span class="cn">op</span> <span class="cn">init</span> as<span class="ot">)</span></span>
<span id="cb35-51"><a href="#cb35-51" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-52"><a href="#cb35-52" aria-hidden="true" tabindex="-1"></a><span class="cn">foldr-Alg</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span></span>
<span id="cb35-53"><a href="#cb35-53" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">{{</span>FA <span class="ot">:</span> <dfn>Alg</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span> B<span class="ot">}}</span></span>
<span id="cb35-54"><a href="#cb35-54" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <dfn>List</dfn> A <span class="ot">→</span> B</span>
<span id="cb35-55"><a href="#cb35-55" aria-hidden="true" tabindex="-1"></a><span class="cn">foldr-Alg</span> <span class="ot">=</span> <span class="cn">foldr</span> <span class="ot">(λ</span> a b <span class="ot">→</span> <span class="cn">μ</span> <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">))</span> <span class="ot">(</span><span class="cn">μ</span> <span class="cn">NilP</span><span class="ot">)</span></span>
<span id="cb35-56"><a href="#cb35-56" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-57"><a href="#cb35-57" aria-hidden="true" tabindex="-1"></a>List-weakly-initial</span>
<span id="cb35-58"><a href="#cb35-58" aria-hidden="true" tabindex="-1"></a>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="dt">Set</span><span class="ot">}</span></span>
<span id="cb35-59"><a href="#cb35-59" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">{{</span>FA <span class="ot">:</span> <dfn>Alg</dfn> <span class="ot">(</span><dfn>ListP</dfn> A<span class="ot">)</span> B<span class="ot">}}</span></span>
<span id="cb35-60"><a href="#cb35-60" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> Alg-Homo <span class="ot">(</span><dfn>List</dfn> A<span class="ot">)</span> B <span class="ot">(</span><span class="cn">List-Alg</span> <span class="ot">{</span>A<span class="ot">})</span> FA</span>
<span id="cb35-61"><a href="#cb35-61" aria-hidden="true" tabindex="-1"></a>List-weakly-initial <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">=</span> AlgHom <span class="cn">foldr-Alg</span> <span class="cn">law</span></span>
<span id="cb35-62"><a href="#cb35-62" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb35-63"><a href="#cb35-63" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb35-64"><a href="#cb35-64" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="ot">:</span> <span class="ot">(</span>fa <span class="ot">:</span> <dfn>ListP</dfn> A <span class="ot">(</span><dfn>List</dfn> A<span class="ot">))</span> <span class="ot">→</span></span>
<span id="cb35-65"><a href="#cb35-65" aria-hidden="true" tabindex="-1"></a>    <span class="cn">foldr</span> <span class="ot">(λ</span> a b <span class="ot">→</span> <span class="cn">μ</span> <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">))</span> <span class="ot">(</span><span class="cn">μ</span> <span class="cn">NilP</span><span class="ot">)</span> <span class="ot">(</span><span class="cn">μ</span> fa<span class="ot">)</span></span>
<span id="cb35-66"><a href="#cb35-66" aria-hidden="true" tabindex="-1"></a>      ≡</span>
<span id="cb35-67"><a href="#cb35-67" aria-hidden="true" tabindex="-1"></a>    <span class="cn">μ</span> <span class="ot">(</span><span class="cn">Arr</span> <span class="ot">(</span><span class="cn">foldr</span> <span class="ot">(λ</span> a b <span class="ot">→</span> <span class="cn">μ</span> <span class="ot">(</span><span class="cn">ConsP</span> a b<span class="ot">))</span> <span class="ot">(</span><span class="cn">μ</span> <span class="cn">NilP</span><span class="ot">))</span> fa<span class="ot">)</span></span>
<span id="cb35-68"><a href="#cb35-68" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="cn">NilP</span>         <span class="ot">=</span> <span class="cn">refl</span></span>
<span id="cb35-69"><a href="#cb35-69" aria-hidden="true" tabindex="-1"></a>  <span class="cn">law</span> <span class="ot">(</span><span class="cn">ConsP</span> a as<span class="ot">)</span> <span class="ot">=</span> <span class="cn">refl</span></span></code></pre></div>
<p>and we see that out of initiality naturally comes <span
class="math inline">\(\mathrm{foldr}\)</span>! <span
class="math inline">\(\mathrm{F}\)</span>-algebra semantics give rise to
<em>recursion principles</em>! The essence of <span
class="math inline">\(\mathbb{N}\)</span>, from this point of view, lies
in its recursive structure. After all, in a dependently-typed setting
recursion and induction are really two sides of the same coin.</p>
<p>Thank you for reading! The full code for these examples is available
<a
href="https://github.com/Boarders/agda-peano/blob/master/Peano.agda">here</a>.
Hopefully this has given some ideas for how we can explore mathematical
ideas in Agda leveraging the typechecker to guide our proofs. Feel free
to contact me <a href="mailto:callan.mcgill@gmail.com">here</a> with
questions, thoughts, ideas, or all of the above.</p>
<p><i>With warmest thanks to Sam Derbyshire, Reed Mullanix, Alixandra
Prybyla and Shon Feder for their valuable feedback.</i></p>]]></content>
  </entry>
  <entry>
      <title>Semantics of STLC in Agda</title>
      <link href="https://boarders.github.ioposts/stlc-semantics.html"/>
      <id>https://boarders.github.ioposts/stlc-semantics.html</id>
      <updated>2026-03-20T00:00:SZ</updated>
      <category term="Agda"/>
      <category term="CCC"/>
      <category term="STLC"/>
      <category term="Category-Theory"/>
      <summary>An excursion into the semantics of STLC formalized in agda</summary>
      <content type="html"><![CDATA[<div style="display:none">
( )
</div>
<h2 id="motivation">Motivation</h2>
<p>In the study of formal languages (or other types of syntactical
gadgets) we face two fundamental questions:</p>
<ol type="1">
<li>What, if any, is the intended or conventional <em>meaning</em> of
the language in question?</li>
<li>What general class of mathematical or formal objects does the
language seek to describe?</li>
</ol>
<p>Formal languages, in this sense, arguably first arose with Frege’s
work and later that of Hilbert, Russell and Whitehead etc. Hilbert was
the first to define a formal language legible as what we would today
call first-order Peano arithmetic.</p>
<p>For such a language, the intended model is, of course, the natural
numbers; the language is precisely constructed to capture the essential
logical principles for all arithmetical truths. The second question,
that of the general class of models of first-order Peano arithmetic
seems to have been a later historical consideration. It was perhaps
thought, though unstated, that such a language is <em>categorical</em>,
that is to say: the language has a unique model up to (model)
isomorphism. Later work of Gödel, and especially Skolem, emphasized the
folly of this idea, typifying expressive first-order theories of
arithmetic as necessarily giving rise to a panoply of non-standard
models of arithmetic.</p>
<p>Whilst non-standard models in this case might strike us as especially
peculiar – models, for instance, which have some non-standard number
with Gödel number equal to a Gödel sentence of the system – they seem
much tamer to contemplate when we change our perspective to a more
modern algebraic one. For instance, let us imagine a formal language for
an algebraic theory – let us say, the language of groups – with the
intended models of the theory precisely capturing the correct algebraic
structures – the class of all groups. An impressionistic version of such
a formal language might be a kind of type theory with judgments of the
following form:</p>
<p><span class="math display">\[
   \mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\cdot
\mathrm{y}\mathrel{:}\mathcal{G}
\]</span> <span class="math display">\[
   \mathrm{x}\mathrel{:}\mathcal{G}\vdash \mathrm{x}^{-1}
\mathrel{:}\mathcal{G}
\]</span> <span class="math display">\[
     \vdash e \mathrel{:}\mathcal{G}
\]</span> Such a language would also need to have an equality judgment
capturing the equational theory of groups:</p>
<p><span class="math display">\[
\frac{}{\mathrm{x}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\cdot e \equiv
\mathrm{x}}
\]</span></p>
<p><span class="math display">\[
\frac{}{\mathrm{x}\mathrel{:}\mathcal{G}\vdash e \cdot \mathrm{x}\equiv
\mathrm{x}}
\]</span></p>
<p><span class="math display">\[
\frac{}{\mathrm{x}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\cdot
\mathrm{x}^{-1} \equiv e}
\]</span></p>
<p><span class="math display">\[
\frac{}{\mathrm{x}\mathrel{:}\mathcal{G}\vdash \mathrm{x}^{-1} \cdot
\mathrm{x}\equiv e}
\]</span></p>
<p><span class="math display">\[
\frac{}{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}, \mathrm{z}\mathrel{:}\mathcal{G}\vdash
(\mathrm{x}\cdot \mathrm{y}) \cdot \mathrm{z}\equiv \mathrm{x}\cdot
(\mathrm{y}\cdot \mathrm{z})}
\]</span></p>
<p><span class="math display">\[
\frac{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\equiv \mathrm{y}\quad
\mathrm{z}\mathrel{:}\mathcal{G}}{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}, \mathrm{z}\mathrel{:}\mathcal{G}\vdash
\mathrm{x}\cdot \mathrm{z}\equiv \mathrm{y}\cdot \mathrm{z}}
\]</span></p>
<p><span class="math display">\[
\frac{\mathrm{z}\mathrel{:}\mathcal{G}\quad
\mathrm{x}\mathrel{:}\mathcal{G}, \mathrm{y}\mathrel{:}\mathcal{G}\vdash
\mathrm{x}\equiv \mathrm{y}}{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}, \mathrm{z}\mathrel{:}\mathcal{G}\vdash
\mathrm{z}\cdot \mathrm{x}\equiv \mathrm{z}\cdot \mathrm{y}}
\]</span></p>
<p><span class="math display">\[
\frac{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\equiv
\mathrm{y}}{\mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}\vdash \mathrm{x}^{-1} \equiv
\mathrm{y}^{-1}}
\]</span></p>
<p>Now, in order for us to interpret this theory we need not just to
interpret the symbols of the theory, but to give a semantics of each
judgment, and thus to give meaning first to contexts, and then to
judgments. Here is an idea for how we might do so – given a judgment of
this form: <span class="math display">\[
   \mathrm{x}\mathrel{:}\mathcal{G},
\mathrm{y}\mathrel{:}\mathcal{G}\vdash \mathrm{x}\cdot
\mathrm{y}\mathrel{:}\mathcal{G}
\]</span> which we are trying to interpret in some set <span
class="math inline">\(\mathrm{X}\)</span>, we naively interpret this as
some function: <span class="math display">\[
  \mathrm{X}\times \mathrm{X}\rightarrow \mathrm{X}
\]</span></p>
<p>Similarly, given a term judgment like: <span class="math display">\[
   \mathrm{x}\mathrel{:}\mathcal{G}, \mathrm{y}\mathrel{:}\mathcal{G},
\mathrm{z}\mathrel{:}\mathcal{G}\vdash (\mathrm{x}\cdot \mathrm{y})
\cdot \mathrm{z}\mathrel{:}\mathcal{G}
\]</span></p>
<p>we want to interpret this semantically as a function of the form:
<span class="math display">\[
  \mathrm{X}\times \mathrm{X}\times \mathrm{X}\rightarrow \mathrm{X}
\]</span></p>
<p>Not wishing to get too into the weeds on this particular example, we
make two observations about where such thinking leads. Firstly, this is
essentially how one might arrive at the notion of a <a
href="https://ncatlab.org/nlab/show/Lawvere+theory">Lawvere Theory</a> –
Lawvere’s abstraction of the notion of syntax and semantics. What this
means in this case, roughly, is that given any word on <span
class="math inline">\(\mathrm{n}\)</span> variables in the free group
<span
class="math inline">\(\mathcal{Fr}\langle\mathrm{x}\_1,\ldots,x\_n\rangle\)</span>,
the semantics of our theory gives an interpretation of such a word as a
morphism: <span class="math display">\[
  \underbrace{\mathrm{X}\times \cdots \times \mathrm{X}}_{n} \rightarrow
\mathrm{X}
\]</span></p>
<p>We can package this observation up algebraically by constructing a
certain syntactic category <span
class="math inline">\(\mathbb{T}_{\mathrm{Grp}}\)</span> with objects
given by sets of variables, <span class="math inline">\(\Gamma =
\{\mathrm{x}\_1, \ldots, \mathrm{x}\_\mathrm{n}\}\)</span>, and
morphisms between contexts <span class="math inline">\(\{\mathrm{x}\_1,
\ldots, \mathrm{x}\_\mathrm{n}\} \rightarrow \{\mathrm{y}\_1, \ldots,
\mathrm{y}\_m\}\)</span> given by an m-tuple of words in the free group
on <span class="math inline">\(\Gamma\)</span>: <span
class="math inline">\(\mathrm{y}_\mathrm{j}=
\mathrm{w}_\mathrm{j}\left[x\_1,\ldots,\mathrm{x}_{\mathrm{n}}\right]\)</span>.
This category has products given by the disjoint union of contexts.
Condensing the above then, a model of our theory (in <span
class="math inline">\(\mathcal{Set}\)</span>) is nothing but a
product-preserving functor from this <em>syntactic category</em>: <span
class="math display">\[
  \mathrm{M} : \underbrace{\mathbb{T}_{\mathrm{Grp}} \rightarrow
\mathcal{Set}}_{\mathrm{product\ preserving}}
\]</span></p>
<p>Secondly, we note that, in a certain sense, our theory gives rise to
a <em>canonical model</em> where we take as our set of elements in
context <span class="math inline">\(\Gamma = \{\mathrm{x}\_1, \ldots,
\mathrm{x}\_\mathrm{n}\}\)</span>, precisely the equivalence class of
judgments for the theory and, we take for morphisms, equivalence classes
of substitutions. We won’t spell out this example here as we do
something similar for simply typed lambda calculus, but we will keep in
mind the following takeaways: - A semantics of a type theory might be
thought of as a certain kind of algebraic theory - This semantics comes
with a canonical syntactical model of the theory which should, in some
sense, be the free theory. - Our semantics should be <em>sound</em> for
the type theory – anything we prove in the type theory should be true
for all algebraic models. - Ideally, our semantics should also be
<em>complete</em> for the theory – If we can prove some judgment is true
for all models, then it should be true for the theory. More is true: if
something is true across all models, then it is true of the canonical
model, and thus syntactically true. Thus, we see that providing a
canonical model is fundamentally connected to the idea of
completeness.</p>
<h2 id="semantics-of-simply-typed-lambda-calculus">Semantics of Simply
Typed Lambda Calculus</h2>
<p>It is an oft-repeated (perhaps more than understood) fact that the
simply typed lambda calculus (STLC) is a kind of “internal language” or
“syntax” for the theory of Cartesian Closed Categories (CCC’s). As such,
in the paradigm we have been sketching, we can think of Cartesian closed
categories as semantic models of the theory of the type theory described
by STLC. What does this intuitively mean? Consider a prototypical term
formation judgment in STLC:</p>
<p><span class="math display">\[
\begin{array}{@{}c@{\quad}l@{}}
\vcenter{\hbox{$
  \dfrac{x_{\mathrm{i}} : a_{\mathrm{i}} \in \Gamma}{\Gamma \vdash
\mathrm{var}(x_{\mathrm{i}}) : a_{\mathrm{i}}}
$}}
&amp; \text{(Var)}
\end{array}
\]</span></p>
<p>Semantically, this is pleasantly simple: assuming we have a natural
interpretation for each of the simple types, then if we are given some
context <span class="math inline">\(\Gamma = \mathrm{x}\_1
\mathrel{:}\mathrm{a}_1, \cdots,
\mathrm{x}\_\mathrm{n}\mathrel{:}\mathrm{a}_\mathrm{n}\)</span>, we
interpret this as a product (in some ambient category <span
class="math inline">\(\mathcal{C}\)</span>): <span
class="math inline">\(\text{⟦}a_1\text{⟧} \;\times\; \cdots \;\times\;
\text{⟦}a_{\mathrm{n}} \text{⟧}\)</span>. This term is interpreted as
nothing other than a projection from an iterated product: <span
class="math display">\[
\text{⟦}\mathrm{var}(x_{\mathrm{i}})\text{⟧}
\mathrel{\mathop{:}}=\operatorname{proj}_{\mathrm{i}} :
\text{⟦}\mathrm{a}_1\text{⟧} \;\times\; \cdots \;\times\;
\text{⟦}\mathrm{a}_{\mathrm{n}}\text{⟧} \rightarrow
\text{⟦}\mathrm{a}_{\mathrm{i}}\text{⟧}
\]</span></p>
<p>Similarly, suppose we have the following application typing
judgment:</p>
<p><span class="math display">\[
\mathrm{f}: \mathrm{a}\Rightarrow b,  \mathrm{x}: \mathrm{a}\vdash
\mathrm{f}\cdot \mathrm{x}
\]</span></p>
<p>This should correspond to taking a function object i.e. an abstract
collection where each of the (generalized) elements correspond to a
function (in <span class="math inline">\(\mathcal{C}\)</span>), and an
element of the domain of the function and “evaluating” the function on
that argument:</p>
<p><span class="math display">\[
\text{⟦}\mathrm{f}\cdot \mathrm{x}\text{⟧}
\mathrel{\mathop{:}}=\operatorname{eval}: (\text{⟦}\mathrm{f}\text{⟧} :
\text{⟦}\mathrm{b}\text{⟧}^{\text{⟦}a\text{⟧}}) \;\times\;
(\text{⟦}x\text{⟧} : \text{⟦}a\text{⟧}) \rightarrow
\text{⟦}\mathrm{b}\text{⟧}
\]</span></p>
<p>The evaluation map is precisely one of the defining features of an <a
href="https://ncatlab.org/nlab/show/exponential+object#definition">exponential
object</a> which is one of the defining structures in a Cartesian closed
category. More generally then, we will see how each of the terms in STLC
precisely corresponds to a feature in a CCC. In the above terminology
then, we can think of the STLC as a syntax for the algebraic theory of
CCC’s. Moreover, from the STLC we should be able to construct a
syntactical category which is a <em>canonical</em> model for the type
theory. Let us see then how we can begin to develop such a semantics in
<code>agda</code>.</p>
<h2 id="agda-development">Agda Development</h2>
<h3 id="contexts-renamings-and-substitution">Contexts, Renamings and
Substitution</h3>
<p>We use intrinsically well-scoped, well-typed syntax throughout,
defining single-variable substitution as a special case of parallel
substitution. In order to get off the ground, we first define a
subcategory of substitutions called renamings. A renaming from one
context <span class="math inline">\(\Gamma\)</span> to another <span
class="math inline">\(\Delta\)</span> gives us a way of taking any typed
variable <span
class="math inline">\((\mathrm{x}\mathrel{:}\mathrm{t})  \in
\Gamma\)</span> and giving back a variable in <span
class="math inline">\(\Delta\)</span> of the same type. Here is the
syntax we will use for types, terms, and contexts:</p>
<div class="sourceCode" id="cb1"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true" tabindex="-1"></a><span class="kw">pattern</span> <span class="ot">_</span><span class="cn">▸</span><span class="ot">_</span> as a <span class="ot">=</span> a ∷ as</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true" tabindex="-1"></a><span class="kw">pattern</span> <span class="cn">∅</span> <span class="ot">=</span> []</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true" tabindex="-1"></a><span class="co">-- `typed` de bruijn indices</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true" tabindex="-1"></a><span class="kw">infix</span> <span class="dv">4</span> <span class="ot">_</span>∈<span class="ot">_</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>∈<span class="ot">_</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="dt">Type</span><span class="ot">}</span> <span class="ot">:</span> <span class="ot">(</span>a <span class="ot">:</span> A<span class="ot">)</span> <span class="ot">→</span> List A <span class="ot">-&gt;</span> <span class="dt">Type</span> <span class="kw">where</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true" tabindex="-1"></a>  <span class="cn">Z</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ <span class="ot">:</span> List A<span class="ot">}{</span>n <span class="ot">:</span> A<span class="ot">}</span> <span class="ot">-&gt;</span> n ∈ Γ <span class="cn">▸</span> n</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true" tabindex="-1"></a>  <span class="cn">S</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ n m<span class="ot">}</span> <span class="ot">-&gt;</span> n ∈ Γ <span class="ot">-&gt;</span> n ∈ Γ <span class="cn">▸</span> m</span></code></pre></div>
<div class="sourceCode" id="cb2"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- We add two primitive types to our theory:</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true" tabindex="-1"></a><span class="co">--  - <span class="cn">𝟙</span> corresponding to the terminal object</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true" tabindex="-1"></a><span class="co">--  - <span class="cn">𝕆</span> corresponding to any object</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true" tabindex="-1"></a><span class="co">--    [suggestively named as it could be empty]</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>Ty</dfn> <span class="ot">:</span> <span class="dt">Type</span> <span class="kw">where</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true" tabindex="-1"></a>  <span class="cn">𝕆</span> <span class="ot">:</span> <dfn>Ty</dfn></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true" tabindex="-1"></a>  <span class="cn">𝟙</span> <span class="ot">:</span> <dfn>Ty</dfn></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span> <span class="ot">:</span> <dfn>Ty</dfn> <span class="ot">→</span> <dfn>Ty</dfn> <span class="ot">→</span> <dfn>Ty</dfn></span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true" tabindex="-1"></a><span class="cn">Ctxt</span> <span class="ot">:</span> <span class="dt">Set</span></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true" tabindex="-1"></a><span class="cn">Ctxt</span> <span class="ot">=</span> List <dfn>Ty</dfn></span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true" tabindex="-1"></a><span class="co">-- Intrinsically well-typed well-scoped terms</span></span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>Tm</dfn> <span class="ot">:</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <dfn>Ty</dfn> <span class="ot">→</span> <span class="dt">Type</span> <span class="kw">where</span></span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true" tabindex="-1"></a>  <span class="cn">var</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> <dfn>Tm</dfn> Γ ty</span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span><span class="cn">∙</span><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ dom cod<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ <span class="ot">(</span>dom <span class="cn">⇒</span> cod<span class="ot">)</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ dom <span class="ot">→</span> <dfn>Tm</dfn> Γ cod</span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true" tabindex="-1"></a>  <span class="cn">fun</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ dom cod<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> dom<span class="ot">)</span> cod <span class="ot">→</span> <dfn>Tm</dfn> Γ <span class="ot">(</span>dom <span class="cn">⇒</span> cod<span class="ot">)</span></span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true" tabindex="-1"></a>  <span class="cn">tt</span>  <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ <span class="cn">𝟙</span></span></code></pre></div>
<p>From here, there are two ways one can define renamings, the first,
which we will not use, is as an inductive type:</p>
<div class="sourceCode" id="cb3"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <dfn>IndRen</dfn> <span class="ot">(</span>Δ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">)</span> <span class="ot">:</span> <span class="ot">(</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">)</span> <span class="ot">→</span> <span class="dt">Set</span> <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">ε</span>   <span class="ot">:</span> <dfn>IndRen</dfn> Δ <span class="cn">∅</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">_</span><span class="cn">,</span><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ a<span class="ot">}</span> <span class="ot">(</span>ρ <span class="ot">:</span> <dfn>IndRen</dfn> Δ Γ<span class="ot">)</span> <span class="ot">(</span>x <span class="ot">:</span> a ∈ Δ<span class="ot">)</span> <span class="ot">→</span> <dfn>IndRen</dfn> Δ <span class="ot">(</span>Γ <span class="cn">▸</span> a<span class="ot">)</span></span></code></pre></div>
<p>This approach is nice, and comes with a reasonable built-in notion of
equality of renamings. A second approach, and the one we follow in this
development, is the more naive definition in terms of functions:</p>
<div class="sourceCode" id="cb4"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true" tabindex="-1"></a><dfn>Ren</dfn> <span class="ot">:</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <span class="dt">Set</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true" tabindex="-1"></a><dfn>Ren</dfn> Γ Δ <span class="ot">=</span> <span class="ot">∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> ty ∈ Δ <span class="ot">→</span> ty ∈ Γ</span></code></pre></div>
<p>This says exactly that if we are given any variable in
<code>Δ</code>, we get a corresponding variable (of the same type) in
<code>Γ</code>. In order for this definition to have a reasonable notion
of equality, we will assume function extensionality throughout and so
postulate the following two extensionality variants from the
<code>agda</code> std-lib:</p>
<div class="sourceCode" id="cb5"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true" tabindex="-1"></a><span class="kw">postulate</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true" tabindex="-1"></a>  <dfn>fun-ext</dfn> <span class="ot">:</span> Extensionality ℓzero ℓzero</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true" tabindex="-1"></a>  <dfn>fun-ext-imp</dfn> <span class="ot">:</span> ExtensionalityImplicit ℓzero ℓzero</span></code></pre></div>
<p>We then have the following notion of renaming equivalence:</p>
<div class="sourceCode" id="cb6"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≡Ren</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>ρ σ <span class="ot">:</span> <dfn>Ren</dfn> Γ Δ<span class="ot">)</span> <span class="ot">→</span> <span class="dt">Type</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≡Ren</dfn><span class="ot">_</span> <span class="ot">{</span>Δ <span class="ot">=</span> Δ<span class="ot">}</span> ρ σ <span class="ot">=</span> <span class="ot">∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>v <span class="ot">:</span> ty ∈ Δ<span class="ot">)</span> <span class="ot">→</span> ρ v ≡ σ v</span></code></pre></div>
<p>For our purposes, perhaps the most salient observation about
renamings is that the collection of all contexts with renamings<a
href="#fn1" class="footnote-ref" id="fnref1"
role="doc-noteref"><sup>1</sup></a> between them, which we denote
<code>Ren</code>, forms a category with the following identity and
composition:</p>
<div class="sourceCode" id="cb7"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Ren</dfn> Γ Γ</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren</dfn> <span class="ot">=</span> <span class="ot">λ</span> v <span class="ot">→</span> v</span></code></pre></div>
<div class="sourceCode" id="cb8"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>∘R</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Ξ Δ Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Ren</dfn> Γ Δ <span class="ot">→</span> <dfn>Ren</dfn> Ξ Γ <span class="ot">→</span> <dfn>Ren</dfn> Ξ Δ</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>∘R</dfn><span class="ot">_</span> ρ σ <span class="ot">=</span> <span class="ot">λ</span> v <span class="ot">→</span> σ <span class="ot">(</span>ρ v<span class="ot">)</span></span></code></pre></div>
<p>We then note that we can perform an operation of weakening (called
<code>ext</code>) on renamings, that they are a congruence with respect
to weakening, and that weakening distributes over composition:</p>
<div class="sourceCode" id="cb9"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- weaken a renaming by adding the same type to each context</span></span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Ren</dfn> Γ Δ <span class="ot">→</span> <dfn>Ren</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> ty<span class="ot">)</span> <span class="ot">(</span>Δ <span class="cn">▸</span> ty<span class="ot">)</span></span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> ρ <span class="cn">Z</span> <span class="ot">=</span> <span class="cn">Z</span></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> ρ <span class="ot">(</span><span class="cn">S</span> pf<span class="ot">)</span> <span class="ot">=</span> <span class="cn">S</span> <span class="ot">(</span>ρ pf<span class="ot">)</span></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- weakening preserves equality of renamings:</span></span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true" tabindex="-1"></a><dfn>ext-≡</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">{</span>ρ σ <span class="ot">:</span> <dfn>Ren</dfn> Δ Γ<span class="ot">}</span> <span class="ot">→</span> ρ <dfn>≡Ren</dfn> σ <span class="ot">→</span> <dfn>ext</dfn> <span class="ot">{</span>ty <span class="ot">=</span> ty<span class="ot">}</span> ρ <dfn>≡Ren</dfn> <dfn>ext</dfn> σ</span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true" tabindex="-1"></a><dfn>ext-≡</dfn> eq <span class="cn">Z</span> <span class="ot">=</span> refl</span>
<span id="cb9-9"><a href="#cb9-9" aria-hidden="true" tabindex="-1"></a><dfn>ext-≡</dfn> eq <span class="ot">(</span><span class="cn">S</span> v<span class="ot">)</span> <span class="ot">=</span> cong <span class="cn">S</span> <span class="ot">(</span>eq v<span class="ot">)</span></span>
<span id="cb9-10"><a href="#cb9-10" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb9-11"><a href="#cb9-11" aria-hidden="true" tabindex="-1"></a><dfn>ext-∘R</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Ξ Γ Δ ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>σ <span class="ot">:</span> <dfn>Ren</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>τ <span class="ot">:</span> <dfn>Ren</dfn> Ξ Δ<span class="ot">)</span>  <span class="ot">→</span></span>
<span id="cb9-12"><a href="#cb9-12" aria-hidden="true" tabindex="-1"></a>  <dfn>ext</dfn> <span class="ot">{</span>ty <span class="ot">=</span> ty<span class="ot">}</span> <span class="ot">(</span>σ <dfn>∘R</dfn> τ<span class="ot">)</span> <dfn>≡Ren</dfn> <span class="ot">(</span><dfn>ext</dfn> σ <dfn>∘R</dfn> <dfn>ext</dfn> τ<span class="ot">)</span></span>
<span id="cb9-13"><a href="#cb9-13" aria-hidden="true" tabindex="-1"></a><dfn>ext-∘R</dfn> σ τ <span class="cn">Z</span> <span class="ot">=</span> refl</span>
<span id="cb9-14"><a href="#cb9-14" aria-hidden="true" tabindex="-1"></a><dfn>ext-∘R</dfn> σ τ <span class="ot">(</span><span class="cn">S</span> v<span class="ot">)</span> <span class="ot">=</span> refl</span></code></pre></div>
<p>We then have that renaming acts to perform parallel renaming of
variables on terms, as we would hope:</p>
<div class="sourceCode" id="cb10"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ t<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <dfn>Ren</dfn> Δ Γ <span class="ot">→</span> <dfn>Tm</dfn> Δ t</span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> ρ <span class="ot">=</span> <span class="cn">var</span> <span class="ot">(</span>ρ v<span class="ot">)</span></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> ρ <span class="ot">=</span> <span class="ot">(</span><dfn>rename</dfn> rator ρ<span class="ot">)</span> <span class="cn">∙</span> <span class="ot">(</span><dfn>rename</dfn> rand ρ<span class="ot">)</span></span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="ot">(</span><span class="cn">fun</span> body<span class="ot">)</span> ρ <span class="ot">=</span> <span class="cn">fun</span> <span class="ot">(</span><dfn>rename</dfn> body <span class="ot">(</span><dfn>ext</dfn> ρ<span class="ot">))</span></span>
<span id="cb10-5"><a href="#cb10-5" aria-hidden="true" tabindex="-1"></a><dfn>rename</dfn> <span class="cn">tt</span> ρ <span class="ot">=</span> <span class="cn">tt</span></span></code></pre></div>
<p>Given the above, that renamings are the morphisms in a category
<code>Ren</code>, and that these morphisms act <em>contravariantly</em>
on terms (of a fixed type), we might suspect that we should think of
<code>Tm _  t</code> as a <a
href="https://ncatlab.org/nlab/show/presheaf">presheaf</a> on
<code>Ren</code>. Before we show that this is indeed the case, we must
first develop some of the basic equational theory, in our set-up,
proving that renaming preserves Ren-equality:</p>
<div class="sourceCode" id="cb11"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true" tabindex="-1"></a><dfn>rename-≡</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ t<span class="ot">}</span> <span class="ot">→</span> <span class="ot">{</span>ρ σ <span class="ot">:</span> <dfn>Ren</dfn> Δ Γ<span class="ot">}</span> <span class="ot">→</span> ρ <dfn>≡Ren</dfn> σ <span class="ot">→</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">)</span> <span class="ot">→</span> <dfn>rename</dfn> tm ρ ≡ <dfn>rename</dfn> tm σ</span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true" tabindex="-1"></a><dfn>rename-≡</dfn> eq <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> <span class="ot">=</span> cong <span class="ot">(λ</span> v <span class="ot">→</span> <span class="cn">var</span> v <span class="ot">)</span> <span class="ot">(</span>eq v<span class="ot">)</span></span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true" tabindex="-1"></a><dfn>rename-≡</dfn> eq <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> <span class="ot">=</span> cong₂ <span class="ot">(λ</span> rator rand <span class="ot">→</span> rator <span class="cn">∙</span> rand <span class="ot">)</span> <span class="ot">(</span><dfn>rename-≡</dfn> eq rator<span class="ot">)</span> <span class="ot">(</span><dfn>rename-≡</dfn> eq rand<span class="ot">)</span></span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true" tabindex="-1"></a><dfn>rename-≡</dfn> eq <span class="ot">(</span><span class="cn">fun</span> body<span class="ot">)</span> <span class="ot">=</span> cong <span class="cn">fun</span> <span class="ot">(</span><dfn>rename-≡</dfn> <span class="ot">(</span><dfn>ext-≡</dfn> eq<span class="ot">)</span> body<span class="ot">)</span></span>
<span id="cb11-5"><a href="#cb11-5" aria-hidden="true" tabindex="-1"></a><dfn>rename-≡</dfn> eq <span class="cn">tt</span> <span class="ot">=</span> refl</span></code></pre></div>
<p>We can then show that the renaming action respects composition of
renamings (functoriality):</p>
<div class="sourceCode" id="cb12"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> ≡-Reasoning</span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true" tabindex="-1"></a><dfn>rename-∘R</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span> Ξ Γ Δ ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ ty<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>σ <span class="ot">:</span> <dfn>Ren</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>τ <span class="ot">:</span> <dfn>Ren</dfn> Ξ Δ<span class="ot">)</span>  <span class="ot">→</span></span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span><dfn>rename</dfn> tm <span class="ot">(</span>σ <dfn>∘R</dfn> τ<span class="ot">))</span> ≡ <dfn>rename</dfn> <span class="ot">(</span><dfn>rename</dfn> tm σ<span class="ot">)</span> τ</span>
<span id="cb12-4"><a href="#cb12-4" aria-hidden="true" tabindex="-1"></a><dfn>rename-∘R</dfn> <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> σ τ <span class="ot">=</span> refl</span>
<span id="cb12-5"><a href="#cb12-5" aria-hidden="true" tabindex="-1"></a><dfn>rename-∘R</dfn> <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> σ τ</span>
<span id="cb12-6"><a href="#cb12-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">rewrite</span> <dfn>rename-∘R</dfn> rator σ τ</span>
<span id="cb12-7"><a href="#cb12-7" aria-hidden="true" tabindex="-1"></a>  <span class="kw">rewrite</span> <dfn>rename-∘R</dfn> rand σ τ</span>
<span id="cb12-8"><a href="#cb12-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">=</span> refl</span>
<span id="cb12-9"><a href="#cb12-9" aria-hidden="true" tabindex="-1"></a><dfn>rename-∘R</dfn> <span class="ot">{</span>ty <span class="ot">=</span> ty<span class="ot">}</span> <span class="ot">(</span><span class="cn">fun</span> body<span class="ot">)</span> σ τ <span class="ot">=</span></span>
<span id="cb12-10"><a href="#cb12-10" aria-hidden="true" tabindex="-1"></a>  begin</span>
<span id="cb12-11"><a href="#cb12-11" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span><span class="cn">fun</span> <span class="ot">(</span><dfn>rename</dfn> body <span class="ot">(</span><dfn>ext</dfn> <span class="ot">(</span>σ <dfn>∘R</dfn> τ<span class="ot">))))</span></span>
<span id="cb12-12"><a href="#cb12-12" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Use <dfn>ext-∘R</dfn> to distribute <dfn>ext</dfn> over composition</span></span>
<span id="cb12-13"><a href="#cb12-13" aria-hidden="true" tabindex="-1"></a>      ≡⟨ cong <span class="cn">fun</span> <span class="ot">(</span><dfn>rename-≡</dfn> <span class="ot">(</span><dfn>ext-∘R</dfn> σ τ<span class="ot">)</span> <span class="ot">_)</span> ⟩</span>
<span id="cb12-14"><a href="#cb12-14" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span><span class="cn">fun</span> <span class="ot">(</span><dfn>rename</dfn> body <span class="ot">((</span><dfn>ext</dfn> σ<span class="ot">)</span> <dfn>∘R</dfn> <span class="ot">(</span><dfn>ext</dfn> τ<span class="ot">))))</span></span>
<span id="cb12-15"><a href="#cb12-15" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Apply <dfn>rename-∘R</dfn> inductively to the body</span></span>
<span id="cb12-16"><a href="#cb12-16" aria-hidden="true" tabindex="-1"></a>      ≡⟨ cong <span class="cn">fun</span> <span class="ot">(</span><dfn>rename-∘R</dfn> body <span class="ot">(</span><dfn>ext</dfn> σ<span class="ot">)</span> <span class="ot">(</span><dfn>ext</dfn> τ<span class="ot">))</span> ⟩</span>
<span id="cb12-17"><a href="#cb12-17" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span><span class="cn">fun</span> <span class="ot">(</span><dfn>rename</dfn> <span class="ot">(</span><dfn>rename</dfn> body <span class="ot">(</span><dfn>ext</dfn> σ<span class="ot">))</span> <span class="ot">(</span><dfn>ext</dfn> τ<span class="ot">)))</span> ∎</span>
<span id="cb12-18"><a href="#cb12-18" aria-hidden="true" tabindex="-1"></a><dfn>rename-∘R</dfn> <span class="cn">tt</span> σ τ <span class="ot">=</span> refl</span></code></pre></div>
<p>Similarly, after we observe that weakening of the identity is
Ren-equivalent to the identity, we can show that renaming also preserves
the identity up to propositional equality of terms:</p>
<div class="sourceCode" id="cb13"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true" tabindex="-1"></a><dfn>ext-id</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>ty Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>ext</dfn> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> <span class="ot">{</span>ty <span class="ot">=</span> ty<span class="ot">}</span> <dfn>id-Ren</dfn> <dfn>≡Ren</dfn> <dfn>id-Ren</dfn></span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true" tabindex="-1"></a><dfn>ext-id</dfn> <span class="cn">Z</span> <span class="ot">=</span> refl</span>
<span id="cb13-3"><a href="#cb13-3" aria-hidden="true" tabindex="-1"></a><dfn>ext-id</dfn> <span class="ot">(</span><span class="cn">S</span> v<span class="ot">)</span> <span class="ot">=</span> refl</span>
<span id="cb13-4"><a href="#cb13-4" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb13-5"><a href="#cb13-5" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren-≡</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ t<span class="ot">}</span> <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">)</span> <span class="ot">→</span> <dfn>rename</dfn> tm <dfn>id-Ren</dfn> ≡ tm</span>
<span id="cb13-6"><a href="#cb13-6" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren-≡</dfn> <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> <span class="ot">=</span> refl</span>
<span id="cb13-7"><a href="#cb13-7" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren-≡</dfn> <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> <span class="ot">=</span> cong₂ <span class="ot">(λ</span> f x <span class="ot">→</span> f <span class="cn">∙</span> x<span class="ot">)</span> <span class="ot">(</span><dfn>id-Ren-≡</dfn> rator<span class="ot">)</span> <span class="ot">(</span><dfn>id-Ren-≡</dfn> rand<span class="ot">)</span></span>
<span id="cb13-8"><a href="#cb13-8" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren-≡</dfn> <span class="ot">(</span><span class="cn">fun</span> body<span class="ot">)</span> <span class="ot">=</span> cong <span class="ot">(λ</span> a <span class="ot">→</span> <span class="cn">fun</span> a<span class="ot">)</span> <dfn>eq-lem</dfn></span>
<span id="cb13-9"><a href="#cb13-9" aria-hidden="true" tabindex="-1"></a>   <span class="kw">where</span></span>
<span id="cb13-10"><a href="#cb13-10" aria-hidden="true" tabindex="-1"></a>     <dfn>eq-lem</dfn> <span class="ot">:</span> <span class="ot">_</span></span>
<span id="cb13-11"><a href="#cb13-11" aria-hidden="true" tabindex="-1"></a>     <dfn>eq-lem</dfn> <span class="ot">=</span> begin</span>
<span id="cb13-12"><a href="#cb13-12" aria-hidden="true" tabindex="-1"></a>       <dfn>rename</dfn> body <span class="ot">(</span><dfn>ext</dfn> <dfn>id-Ren</dfn><span class="ot">)</span></span>
<span id="cb13-13"><a href="#cb13-13" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- <dfn>ext</dfn> <dfn>id-Ren</dfn> = <dfn>id-Ren</dfn></span></span>
<span id="cb13-14"><a href="#cb13-14" aria-hidden="true" tabindex="-1"></a>         ≡⟨ <dfn>rename-≡</dfn> <dfn>ext-id</dfn> body ⟩</span>
<span id="cb13-15"><a href="#cb13-15" aria-hidden="true" tabindex="-1"></a>       <dfn>rename</dfn> body <dfn>id-Ren</dfn></span>
<span id="cb13-16"><a href="#cb13-16" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- Apply <dfn>id-Ren-≡</dfn> inductively</span></span>
<span id="cb13-17"><a href="#cb13-17" aria-hidden="true" tabindex="-1"></a>         ≡⟨ <dfn>id-Ren-≡</dfn> body ⟩</span>
<span id="cb13-18"><a href="#cb13-18" aria-hidden="true" tabindex="-1"></a>       body ∎</span>
<span id="cb13-19"><a href="#cb13-19" aria-hidden="true" tabindex="-1"></a><dfn>id-Ren-≡</dfn> <span class="cn">tt</span> <span class="ot">=</span> refl</span></code></pre></div>
<p>Together, <code>rename-∘R</code> and <code>id-Ren-≡</code> give us
the data of a presheaf: <code>Tm _ t</code> is contravariant and
functorial in the renaming action.</p>
<p>Finally, we use renamings to define term weakening, by noting that
the de Bruijn increment operator – <code>S</code> – defines a weakening
from <code>Γ</code> to <code>Γ ▸ ty</code>:</p>
<div class="sourceCode" id="cb14"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true" tabindex="-1"></a>wk-Tm tm <span class="ot">=</span> <dfn>rename</dfn> tm <span class="cn">S</span></span></code></pre></div>
<p>With renamings in hand, we can develop the full theory of
substitutions. Just as a renaming takes a (typed) variable from context
<code>\Gamma</code> and gives a (typed) variable in a context
<code>Δ</code>, a substitution takes a (typed) variable from
<code>Γ</code> and gives back a term in context <code>Δ</code>:</p>
<div class="sourceCode" id="cb15"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb15-1"><a href="#cb15-1" aria-hidden="true" tabindex="-1"></a><dfn>Sub</dfn> <span class="ot">:</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <span class="dt">Type</span></span>
<span id="cb15-2"><a href="#cb15-2" aria-hidden="true" tabindex="-1"></a><dfn>Sub</dfn> Δ Γ <span class="ot">=</span> <span class="ot">∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> <dfn>Tm</dfn> Δ ty</span></code></pre></div>
<p>In order to properly describe the equational theory of such
substitutions, we would first need to have the correct equational theory
of terms (which would require already having defined substitution of
terms), and so, for now, we have a notion of “raw equality” of
substitutions:</p>
<div class="sourceCode" id="cb16"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb16-1"><a href="#cb16-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≣Sub</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Δ Γ<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>ρ σ <span class="ot">:</span> <dfn>Sub</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="dt">Type</span></span>
<span id="cb16-2"><a href="#cb16-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≣Sub</dfn><span class="ot">_</span> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> ρ σ <span class="ot">=</span>  <span class="ot">∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>v <span class="ot">:</span> ty ∈ Γ<span class="ot">)</span> <span class="ot">→</span> ρ v ≡ σ v</span></code></pre></div>
<p>Just as <code>Ren</code> formed a category with objects given by
contexts, we have a category <code>Sub</code> where the morphisms are
now substitutions with the following identity and composition:</p>
<div class="sourceCode" id="cb17"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb17-1"><a href="#cb17-1" aria-hidden="true" tabindex="-1"></a><dfn>Sub-id</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Sub</dfn> Γ Γ</span>
<span id="cb17-2"><a href="#cb17-2" aria-hidden="true" tabindex="-1"></a><dfn>Sub-id</dfn> v <span class="ot">=</span> <span class="cn">var</span> v</span>
<span id="cb17-3"><a href="#cb17-3" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb17-4"><a href="#cb17-4" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>∘𝕊</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Ξ Δ Γ<span class="ot">}</span> <span class="ot">→</span> <dfn>Sub</dfn> Γ Δ <span class="ot">→</span> <dfn>Sub</dfn> Ξ Γ <span class="ot">→</span> <dfn>Sub</dfn> Ξ Δ</span>
<span id="cb17-5"><a href="#cb17-5" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>∘𝕊</dfn><span class="ot">_</span> ρ σ v <span class="ot">=</span> <dfn>subst</dfn> σ <span class="ot">(</span>ρ v<span class="ot">)</span></span></code></pre></div>
<p>We again have a notion of weakening for a substitution:</p>
<div class="sourceCode" id="cb18"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb18-1"><a href="#cb18-1" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Sub</dfn> Γ Δ <span class="ot">→</span> <dfn>Sub</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> ty<span class="ot">)</span> <span class="ot">(</span>Δ <span class="cn">▸</span> ty<span class="ot">)</span></span>
<span id="cb18-2"><a href="#cb18-2" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> σ <span class="cn">Z</span> <span class="ot">=</span> <span class="cn">var</span> <span class="cn">Z</span></span>
<span id="cb18-3"><a href="#cb18-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- Here we perform substitution under a binder and so also need to weaken</span></span>
<span id="cb18-4"><a href="#cb18-4" aria-hidden="true" tabindex="-1"></a><span class="co">-- the term we give back</span></span>
<span id="cb18-5"><a href="#cb18-5" aria-hidden="true" tabindex="-1"></a><dfn>ext</dfn> σ <span class="ot">(</span><span class="cn">S</span> pf<span class="ot">)</span> <span class="ot">=</span> <dfn>Ren</dfn><span class="ot">.</span>wk-Tm <span class="ot">(</span>σ pf<span class="ot">)</span></span></code></pre></div>
<p>and we again have a contravariant action of substitutions on
terms:</p>
<div class="sourceCode" id="cb19"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb19-1"><a href="#cb19-1" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ t<span class="ot">}</span> <span class="ot">→</span> <dfn>Sub</dfn> Δ Γ <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <dfn>Tm</dfn> Δ t</span>
<span id="cb19-2"><a href="#cb19-2" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> σ <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> <span class="ot">=</span> σ v</span>
<span id="cb19-3"><a href="#cb19-3" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> σ <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> <span class="ot">=</span> <span class="ot">(</span><dfn>subst</dfn> σ rator<span class="ot">)</span> <span class="cn">∙</span> <span class="ot">(</span><dfn>subst</dfn> σ rand<span class="ot">)</span></span>
<span id="cb19-4"><a href="#cb19-4" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> σ <span class="ot">(</span><span class="cn">fun</span> body<span class="ot">)</span> <span class="ot">=</span> <span class="cn">fun</span> <span class="ot">(</span><dfn>subst</dfn> <span class="ot">(</span><dfn>ext</dfn> σ<span class="ot">)</span> body<span class="ot">)</span></span>
<span id="cb19-5"><a href="#cb19-5" aria-hidden="true" tabindex="-1"></a><dfn>subst</dfn> σ <span class="cn">tt</span> <span class="ot">=</span> <span class="cn">tt</span></span></code></pre></div>
<p>As before, we have that this action is functorial (preserves identity
and composition up to propositional equality):</p>
<div class="sourceCode" id="cb20"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb20-1"><a href="#cb20-1" aria-hidden="true" tabindex="-1"></a>Sub-id-≣ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ t<span class="ot">}</span> <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">)</span> <span class="ot">→</span> <dfn>subst</dfn> <dfn>Sub-id</dfn> tm ≡ tm</span>
<span id="cb20-2"><a href="#cb20-2" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb20-3"><a href="#cb20-3" aria-hidden="true" tabindex="-1"></a><dfn>∘Sub-≡</dfn> <span class="ot">:</span></span>
<span id="cb20-4"><a href="#cb20-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">∀</span> <span class="ot">{</span>Γ Δ Θ ty<span class="ot">}</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Δ ty<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb20-5"><a href="#cb20-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>ρ <span class="ot">:</span> <dfn>Sub</dfn> Γ Δ<span class="ot">)</span> <span class="ot">(</span>ρ&#39; <span class="ot">:</span> <dfn>Sub</dfn> Θ Γ<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb20-6"><a href="#cb20-6" aria-hidden="true" tabindex="-1"></a>  <dfn>subst</dfn> ρ&#39; <span class="ot">(</span><dfn>subst</dfn> ρ tm<span class="ot">)</span> ≡ <dfn>subst</dfn> <span class="ot">(</span>ρ <dfn>∘𝕊</dfn> ρ&#39;<span class="ot">)</span> tm</span></code></pre></div>
<p>We leave the laborious details, regarding the mechanics of
substitution, of these lemmas to the proof development for those
interested<a href="#fn2" class="footnote-ref" id="fnref2"
role="doc-noteref"><sup>2</sup></a>.</p>
<p>Finally, we have what we need to define single variable substitution
which we will need for the equational theory:</p>
<div class="sourceCode" id="cb21"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb21-1"><a href="#cb21-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- substitution of a single variable</span></span>
<span id="cb21-2"><a href="#cb21-2" aria-hidden="true" tabindex="-1"></a><dfn>sub/</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ ty <span class="ot">→</span> <dfn>Sub</dfn> Γ <span class="ot">(</span>Γ <span class="cn">▸</span> ty<span class="ot">)</span></span>
<span id="cb21-3"><a href="#cb21-3" aria-hidden="true" tabindex="-1"></a><dfn>sub/</dfn> arg <span class="cn">Z</span> <span class="ot">=</span> arg</span>
<span id="cb21-4"><a href="#cb21-4" aria-hidden="true" tabindex="-1"></a><dfn>sub/</dfn> <span class="ot">_</span>   <span class="ot">(</span><span class="cn">S</span> v<span class="ot">)</span> <span class="ot">=</span> <span class="cn">var</span> v</span>
<span id="cb21-5"><a href="#cb21-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb21-6"><a href="#cb21-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- substitute for the `topmost` variable in a term</span></span>
<span id="cb21-7"><a href="#cb21-7" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>/[</dfn><span class="ot">_</span>] <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ ty₁ ty₂<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> ty₁<span class="ot">)</span> ty₂ <span class="ot">→</span> <dfn>Tm</dfn> Γ ty₁ <span class="ot">→</span> <dfn>Tm</dfn> Γ ty₂</span>
<span id="cb21-8"><a href="#cb21-8" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>/[</dfn><span class="ot">_</span>] <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> <span class="ot">{</span>ty₁ <span class="ot">=</span> ty₁<span class="ot">}</span> sub-tm arg <span class="ot">=</span> <dfn>subst</dfn> <span class="ot">(</span><dfn>sub/</dfn> arg<span class="ot">)</span> sub-tm</span></code></pre></div>
<h3 id="equational-theory-of-stlc">Equational Theory of STLC</h3>
<p>With substitution in hand, we can define an inductive type to capture
the judgments in the equational theory of terms as follows:</p>
<div class="sourceCode" id="cb22"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb22-1"><a href="#cb22-1" aria-hidden="true" tabindex="-1"></a><span class="kw">data</span> <span class="ot">_</span>≡Tm<span class="ot">_</span> <span class="ot">:</span> <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>t <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <span class="dt">Type</span> <span class="kw">where</span></span>
<span id="cb22-2"><a href="#cb22-2" aria-hidden="true" tabindex="-1"></a>  <span class="cn">reflexivity</span> <span class="ot">:</span></span>
<span id="cb22-3"><a href="#cb22-3" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ ty<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb22-4"><a href="#cb22-4" aria-hidden="true" tabindex="-1"></a>      tm ≡Tm tm</span>
<span id="cb22-5"><a href="#cb22-5" aria-hidden="true" tabindex="-1"></a>  <span class="cn">symmetry</span> <span class="ot">:</span></span>
<span id="cb22-6"><a href="#cb22-6" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>t1 t2 <span class="ot">:</span> <dfn>Tm</dfn> Γ ty<span class="ot">}</span> <span class="ot">→</span> t1 ≡Tm t2 <span class="ot">→</span></span>
<span id="cb22-7"><a href="#cb22-7" aria-hidden="true" tabindex="-1"></a>      t2 ≡Tm t1</span>
<span id="cb22-8"><a href="#cb22-8" aria-hidden="true" tabindex="-1"></a>  <span class="cn">transitivity</span> <span class="ot">:</span></span>
<span id="cb22-9"><a href="#cb22-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>t1 t2 t3 <span class="ot">:</span> <dfn>Tm</dfn> Γ ty<span class="ot">}</span> <span class="ot">→</span> t1 ≡Tm t2 <span class="ot">→</span> t2 ≡Tm t3 <span class="ot">→</span></span>
<span id="cb22-10"><a href="#cb22-10" aria-hidden="true" tabindex="-1"></a>      t1 ≡Tm t3</span>
<span id="cb22-11"><a href="#cb22-11" aria-hidden="true" tabindex="-1"></a>  <span class="cn">β-red</span> <span class="ot">:</span></span>
<span id="cb22-12"><a href="#cb22-12" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀</span> <span class="ot">{</span>dom cod Γ<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>body <span class="ot">:</span> <dfn>Tm</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> dom<span class="ot">)</span> cod<span class="ot">)(</span>arg <span class="ot">:</span> <dfn>Tm</dfn> Γ dom<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb22-13"><a href="#cb22-13" aria-hidden="true" tabindex="-1"></a>      <span class="ot">((</span><span class="cn">fun</span> body<span class="ot">)</span> <span class="cn">∙</span> arg<span class="ot">)</span> ≡Tm <span class="ot">(</span>body <dfn>/[</dfn> arg ]<span class="ot">)</span></span>
<span id="cb22-14"><a href="#cb22-14" aria-hidden="true" tabindex="-1"></a>  <span class="cn">η-fn</span> <span class="ot">:</span></span>
<span id="cb22-15"><a href="#cb22-15" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀{</span>dom cod Γ<span class="ot">}(</span>fn-tm <span class="ot">:</span> <dfn>Tm</dfn> Γ <span class="ot">(</span>dom <span class="cn">⇒</span> cod<span class="ot">))</span> <span class="ot">→</span></span>
<span id="cb22-16"><a href="#cb22-16" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span><span class="cn">fun</span> <span class="ot">((</span><dfn>Ren</dfn><span class="ot">.</span>wk-Tm fn-tm<span class="ot">)</span> <span class="cn">∙</span> <span class="ot">(</span><span class="cn">var</span> <span class="cn">Z</span><span class="ot">)))</span> ≡Tm fn-tm</span>
<span id="cb22-17"><a href="#cb22-17" aria-hidden="true" tabindex="-1"></a>  <span class="cn">var-cong</span> <span class="ot">:</span></span>
<span id="cb22-18"><a href="#cb22-18" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀</span> <span class="ot">{</span>Γ ty<span class="ot">}</span> <span class="ot">{</span>v v&#39; <span class="ot">:</span> ty ∈ Γ<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb22-19"><a href="#cb22-19" aria-hidden="true" tabindex="-1"></a>    v ≡ v&#39; <span class="ot">→</span></span>
<span id="cb22-20"><a href="#cb22-20" aria-hidden="true" tabindex="-1"></a>    <span class="cn">var</span> v ≡Tm <span class="cn">var</span> v&#39;</span>
<span id="cb22-21"><a href="#cb22-21" aria-hidden="true" tabindex="-1"></a>  <span class="cn">∙-cong</span> <span class="ot">:</span></span>
<span id="cb22-22"><a href="#cb22-22" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀{</span>dom cod Γ<span class="ot">}(</span>fn₁ fn₂ <span class="ot">:</span> <dfn>Tm</dfn> Γ <span class="ot">(</span>dom <span class="cn">⇒</span> cod<span class="ot">))</span> <span class="ot">(</span>arg₁ arg₂ <span class="ot">:</span> <dfn>Tm</dfn> Γ dom<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb22-23"><a href="#cb22-23" aria-hidden="true" tabindex="-1"></a>      fn₁ ≡Tm fn₂ <span class="ot">→</span> arg₁ ≡Tm arg₂ <span class="ot">→</span></span>
<span id="cb22-24"><a href="#cb22-24" aria-hidden="true" tabindex="-1"></a>      fn₁ <span class="cn">∙</span> arg₁ ≡Tm fn₂ <span class="cn">∙</span> arg₂</span>
<span id="cb22-25"><a href="#cb22-25" aria-hidden="true" tabindex="-1"></a>  <span class="cn">fun-cong</span> <span class="ot">:</span></span>
<span id="cb22-26"><a href="#cb22-26" aria-hidden="true" tabindex="-1"></a>    <span class="ot">∀{</span>dom cod Γ<span class="ot">}(</span>bd₁ bd₂ <span class="ot">:</span> <dfn>Tm</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> dom<span class="ot">)</span> cod<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb22-27"><a href="#cb22-27" aria-hidden="true" tabindex="-1"></a>      bd₁ ≡Tm bd₂ <span class="ot">→</span> <span class="ot">(</span><span class="cn">fun</span> bd₁<span class="ot">)</span> ≡Tm <span class="ot">(</span><span class="cn">fun</span> bd₂<span class="ot">)</span></span>
<span id="cb22-28"><a href="#cb22-28" aria-hidden="true" tabindex="-1"></a>  <span class="cn">𝟙-η</span> <span class="ot">:</span> <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>tm-𝟙 <span class="ot">:</span> <dfn>Tm</dfn> Γ <span class="cn">𝟙</span><span class="ot">)</span> <span class="ot">→</span> tm-𝟙 ≡Tm <span class="cn">tt</span></span></code></pre></div>
<p>We note the following: - We make equality automatically an
equivalence relation, corresponding to the usual rules:</p>
<p><span class="math display">\[
\dfrac{}{\Gamma \vdash \mathrm{t}\equiv \mathrm{t}\mathrel{:}\mathrm{a}}
\quad \text{(Refl)}
\qquad
\dfrac{\Gamma \vdash \mathrm{t}_1 \equiv \mathrm{t}_2
\mathrel{:}\mathrm{a}}{\Gamma \vdash \mathrm{t}_2 \equiv \mathrm{t}_1
\mathrel{:}\mathrm{a}} \quad \text{(Sym)}
\]</span> <span class="math display">\[
\dfrac{\Gamma \vdash \mathrm{t}_1 \equiv \mathrm{t}_2
\mathrel{:}\mathrm{a}\quad \Gamma \vdash \mathrm{t}_2 \equiv
\mathrm{t}_3 \mathrel{:}\mathrm{a}}{\Gamma \vdash \mathrm{t}_1 \equiv
\mathrm{t}_3 \mathrel{:}\mathrm{a}} \quad \text{(Trans)}
\]</span></p>
<ul>
<li>The β-rule – <code>β-red</code> corresponds exactly to the usual
rule of applying a lambda term to an argument reducing via
substitution:</li>
</ul>
<p><span class="math display">\[
\dfrac{\Gamma,\, \mathrm{x}\mathrel{:}\mathrm{a}\vdash
\mathrm{t}\mathrel{:}\mathrm{b}\quad \Gamma \vdash s
\mathrel{:}\mathrm{a}}
      {\Gamma \vdash (\lambda\, \mathrm{x}\mathrel{:}\mathrm{a}.\;
\mathrm{t})\; s \equiv \mathrm{t}[s/\mathrm{x}] \mathrel{:}\mathrm{b}}
\quad \text{(β)}
\]</span></p>
<ul>
<li>Similarly, the η rule, <code>η-fn</code>, is the standard rule that
gives us a way of writing any term of a function type as a lambda
term:</li>
</ul>
<p><span class="math display">\[
\dfrac{\Gamma \vdash \mathrm{f}\mathrel{:}\mathrm{a}\Rightarrow
\mathrm{b}}
      {\Gamma \vdash \lambda\, \mathrm{x}.\; \mathrm{f}\;
\mathrm{x}\equiv \mathrm{f}\mathrel{:}\mathrm{a}\Rightarrow \mathrm{b}}
\quad \text{(η-fn)}
\qquad
\dfrac{\Gamma \vdash \mathrm{t}\mathrel{:}\mathbf{1}}
      {\Gamma \vdash \mathrm{t}\equiv \mathtt{tt} \mathrel{:}\mathbf{1}}
\quad \text{(η-\(\mathbf{1}\))}
\]</span></p>
<ul>
<li>Lastly, we want the equality relation to be a congruence with
respect to the term formation rules:</li>
</ul>
<p><span class="math display">\[
\dfrac{\Gamma \vdash \mathrm{f}_1 \equiv \mathrm{f}_2
\mathrel{:}\mathrm{a}\Rightarrow \mathrm{b}\quad \Gamma \vdash s_1
\equiv s_2 \mathrel{:}\mathrm{a}}
      {\Gamma \vdash \mathrm{f}_1\; s_1 \equiv \mathrm{f}_2\; s_2
\mathrel{:}\mathrm{b}}
\quad \text{(Cong-App)}
\]</span> <span class="math display">\[
\dfrac{\Gamma,\, \mathrm{x}\mathrel{:}\mathrm{a}\vdash \mathrm{t}_1
\equiv \mathrm{t}_2 \mathrel{:}\mathrm{b}}
      {\Gamma \vdash \lambda\, \mathrm{x}\mathrel{:}\mathrm{a}.\;
\mathrm{t}_1 \equiv \lambda\, \mathrm{x}\mathrel{:}\mathrm{a}.\;
\mathrm{t}_2 \mathrel{:}\mathrm{a}\Rightarrow \mathrm{b}}
\quad \text{(Cong-Lam)}
\]</span></p>
<p>Using the notion of equivalence relation (and setoid – a type
carrying some chosen equivalence relation) from the standard library, we
can then register that equality gives an equivalence relation (with
associated setoid):</p>
<div class="sourceCode" id="cb23"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb23-1"><a href="#cb23-1" aria-hidden="true" tabindex="-1"></a><dfn>≡Tm-Equality</dfn> <span class="ot">:</span> <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}{</span>ty <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> IsEquivalence <span class="ot">{</span>A <span class="ot">=</span> <dfn>Tm</dfn> Γ ty<span class="ot">}</span> <span class="ot">_</span>≡Tm<span class="ot">_</span></span>
<span id="cb23-2"><a href="#cb23-2" aria-hidden="true" tabindex="-1"></a><dfn>≡Tm-Equality</dfn> <span class="ot">=</span></span>
<span id="cb23-3"><a href="#cb23-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">record</span> <span class="ot">{</span></span>
<span id="cb23-4"><a href="#cb23-4" aria-hidden="true" tabindex="-1"></a>    refl <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>x<span class="ot">}</span> <span class="ot">→</span> <span class="cn">reflexivity</span> x <span class="ot">;</span></span>
<span id="cb23-5"><a href="#cb23-5" aria-hidden="true" tabindex="-1"></a>    sym <span class="ot">=</span> <span class="cn">symmetry</span> <span class="ot">;</span></span>
<span id="cb23-6"><a href="#cb23-6" aria-hidden="true" tabindex="-1"></a>    trans <span class="ot">=</span> <span class="ot">λ</span> eq₁ eq₂ <span class="ot">→</span> <span class="cn">transitivity</span> eq₁ eq₂</span>
<span id="cb23-7"><a href="#cb23-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">}</span></span>
<span id="cb23-8"><a href="#cb23-8" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb23-9"><a href="#cb23-9" aria-hidden="true" tabindex="-1"></a><dfn>≡Tm-setoid</dfn> <span class="ot">:</span> <span class="ot">(</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">)</span> <span class="ot">(</span>ty <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">)</span> <span class="ot">→</span> Setoid ℓzero ℓzero</span>
<span id="cb23-10"><a href="#cb23-10" aria-hidden="true" tabindex="-1"></a><dfn>≡Tm-setoid</dfn> Γ ty <span class="ot">=</span></span>
<span id="cb23-11"><a href="#cb23-11" aria-hidden="true" tabindex="-1"></a>  <span class="kw">record</span></span>
<span id="cb23-12"><a href="#cb23-12" aria-hidden="true" tabindex="-1"></a>    <span class="ot">{</span> Carrier       <span class="ot">=</span> <dfn>Tm</dfn> Γ ty</span>
<span id="cb23-13"><a href="#cb23-13" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="ot">_</span>≈<span class="ot">_</span>           <span class="ot">=</span> <span class="ot">_</span>≡Tm<span class="ot">_</span></span>
<span id="cb23-14"><a href="#cb23-14" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> isEquivalence <span class="ot">=</span> <dfn>≡Tm-Equality</dfn></span>
<span id="cb23-15"><a href="#cb23-15" aria-hidden="true" tabindex="-1"></a>    <span class="ot">}</span></span></code></pre></div>
<p>This also, finally, gives us a proper equational theory of
substitutions:</p>
<div class="sourceCode" id="cb24"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb24-1"><a href="#cb24-1" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≡Sub</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Δ Γ<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>ρ σ <span class="ot">:</span> <dfn>Sub</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="dt">Type</span></span>
<span id="cb24-2"><a href="#cb24-2" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>≡Sub</dfn><span class="ot">_</span> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> ρ σ <span class="ot">=</span>  <span class="ot">∀</span> <span class="ot">{</span>ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">∀</span> <span class="ot">(</span>v <span class="ot">:</span> ty ∈ Γ<span class="ot">)</span> <span class="ot">→</span> ρ v ≡Tm σ v</span></code></pre></div>
<h3 id="a-set-theoretic-model">A Set-Theoretic Model</h3>
<p>We finally then have enough of the basic theory developed to
construct a particular model of the theory. As mentioned in the
introduction, our model will take each term judgment <span
class="math inline">\(\Gamma \vdash \mathtt{tm}
\mathrel{:}\mathrm{t}\)</span> to be interpreted as a function from the
interpretation of the context <span
class="math inline">\(\text{⟦}\Gamma\text{⟧}\)</span> to the
interpretation of the type <span
class="math inline">\(\text{⟦}\mathrm{t}\text{⟧}\)</span>. As before,
each context is semantically just the product of the interpretation of
each type. We interpret variables as projections, <code>tt</code> as the
unique term in <code>⊤</code>, function terms as terms in a
set-theoretic function type, and function application as the composition
of function evaluation, and the fork of the semantics of each component
of application:</p>
<div class="sourceCode" id="cb25"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb25-1"><a href="#cb25-1" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧𝕋 <span class="ot">:</span> <dfn>Ty</dfn> <span class="ot">→</span> <span class="dt">Set</span></span>
<span id="cb25-2"><a href="#cb25-2" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">𝕆</span> ⟧𝕋 <span class="ot">=</span> ⊥</span>
<span id="cb25-3"><a href="#cb25-3" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">𝟙</span> ⟧𝕋 <span class="ot">=</span> ⊤</span>
<span id="cb25-4"><a href="#cb25-4" aria-hidden="true" tabindex="-1"></a>⟦ dom <span class="cn">⇒</span> cod ⟧𝕋 <span class="ot">=</span> ⟦ dom ⟧𝕋  <span class="ot">→</span> ⟦ cod ⟧𝕋</span>
<span id="cb25-5"><a href="#cb25-5" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb25-6"><a href="#cb25-6" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧ℂ <span class="ot">:</span> <span class="cn">Ctxt</span> <span class="ot">→</span> <span class="dt">Set</span></span>
<span id="cb25-7"><a href="#cb25-7" aria-hidden="true" tabindex="-1"></a>⟦ [] ⟧ℂ <span class="ot">=</span> ⊤</span>
<span id="cb25-8"><a href="#cb25-8" aria-hidden="true" tabindex="-1"></a>⟦ <span class="ot">(</span>Γ <span class="cn">▸</span> t<span class="ot">)</span> ⟧ℂ <span class="ot">=</span> ⟦ Γ ⟧ℂ × ⟦ t ⟧𝕋</span>
<span id="cb25-9"><a href="#cb25-9" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb25-10"><a href="#cb25-10" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>ty Γ<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> <span class="ot">(</span>⟦ Γ ⟧ℂ <span class="ot">→</span> ⟦ ty ⟧𝕋<span class="ot">)</span></span>
<span id="cb25-11"><a href="#cb25-11" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="cn">Z</span> Γ <span class="ot">=</span> proj₂ Γ</span>
<span id="cb25-12"><a href="#cb25-12" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="ot">(</span><span class="cn">S</span> pf<span class="ot">)</span> Γ <span class="ot">=</span> ⟦var⟧ pf <span class="ot">(</span>proj₁ Γ<span class="ot">)</span></span>
<span id="cb25-13"><a href="#cb25-13" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb25-14"><a href="#cb25-14" aria-hidden="true" tabindex="-1"></a><span class="co">-- We define and use an explicit <dfn>eval</dfn> function</span></span>
<span id="cb25-15"><a href="#cb25-15" aria-hidden="true" tabindex="-1"></a><span class="co">-- to more cleanly generalize to a CCC</span></span>
<span id="cb25-16"><a href="#cb25-16" aria-hidden="true" tabindex="-1"></a><dfn>eval</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>a b <span class="ot">:</span> <span class="dt">Type</span><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>a <span class="ot">→</span> b<span class="ot">)</span> × a <span class="ot">→</span> b</span>
<span id="cb25-17"><a href="#cb25-17" aria-hidden="true" tabindex="-1"></a><dfn>eval</dfn> <span class="ot">(</span>f <span class="cn">,</span> a <span class="ot">)</span> <span class="ot">=</span> f a</span>
<span id="cb25-18"><a href="#cb25-18" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb25-19"><a href="#cb25-19" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ t<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <span class="ot">(</span>⟦ Γ ⟧ℂ <span class="ot">→</span> ⟦ t ⟧𝕋<span class="ot">)</span></span>
<span id="cb25-20"><a href="#cb25-20" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">var</span> v ⟧ <span class="ot">=</span> ⟦var⟧ v</span>
<span id="cb25-21"><a href="#cb25-21" aria-hidden="true" tabindex="-1"></a>⟦ rator <span class="cn">∙</span> rand ⟧ <span class="ot">=</span> <span class="ot">λ</span> ρ <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">(</span>⟦ rator ⟧ ρ <span class="cn">,</span> ⟦ rand ⟧ ρ<span class="ot">)</span></span>
<span id="cb25-22"><a href="#cb25-22" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">fun</span> f ⟧ <span class="ot">=</span> <span class="ot">λ</span> ρ a <span class="ot">→</span> ⟦ f ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">)</span></span>
<span id="cb25-23"><a href="#cb25-23" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">tt</span> ⟧ <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> <span class="cn">tt</span></span></code></pre></div>
<p>There is a sense in which this model allows us to show the
consistency of our raw theory:</p>
<div class="sourceCode" id="cb26"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb26-1"><a href="#cb26-1" aria-hidden="true" tabindex="-1"></a><dfn>consistent</dfn> <span class="ot">:</span> <dfn>Tm</dfn> <span class="cn">∅</span> <span class="cn">𝕆</span> <span class="ot">→</span> ⊥</span>
<span id="cb26-2"><a href="#cb26-2" aria-hidden="true" tabindex="-1"></a><dfn>consistent</dfn> t <span class="ot">=</span> ⟦ t ⟧ <span class="cn">tt</span></span></code></pre></div>
<p>However, this is a somewhat empty sort of consistency. In this model
we map <span class="math inline">\(\mathcal{O}\)</span> to <span
class="math inline">\(\bot\)</span>, and this shows that we can’t
directly construct a term in the possibly empty type, as we would then
be able to directly construct a term of type <span
class="math inline">\(\bot\)</span>. In order for this to
<em>really</em> capture the consistency of STLC, we require not just a
way to interpret term judgments, but that our interpretation also
respects the equational judgments. That our semantics respects the
equations or the proof theory of our system is usually known as
<em>soundness</em>. As we will do this in greater generality, and the
proof involves developing various of the kinds of finicky manipulations
associated with substitutions that typify this kind of result, we only
note the importance of the substitution lemma which we state without
proof<a href="#fn3" class="footnote-ref" id="fnref3"
role="doc-noteref"><sup>3</sup></a>:</p>
<div class="sourceCode" id="cb27"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb27-1"><a href="#cb27-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- the semantics of a substituted term is a composition</span></span>
<span id="cb27-2"><a href="#cb27-2" aria-hidden="true" tabindex="-1"></a><span class="co">-- of a composition followed by the semantics of the</span></span>
<span id="cb27-3"><a href="#cb27-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- term morphism:</span></span>
<span id="cb27-4"><a href="#cb27-4" aria-hidden="true" tabindex="-1"></a><span class="co">--</span></span>
<span id="cb27-5"><a href="#cb27-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- So we can think semantically about a substitution as:</span></span>
<span id="cb27-6"><a href="#cb27-6" aria-hidden="true" tabindex="-1"></a><span class="co">-- ⟦ Δ ⟧ -- σ --&gt; ⟦ Γ ⟧ -- ⟦ tm ⟧ --&gt; ⟦ t ⟧</span></span>
<span id="cb27-7"><a href="#cb27-7" aria-hidden="true" tabindex="-1"></a><dfn>sub-lem</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ t<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>σ <span class="ot">:</span> <dfn>Sub</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">)</span></span>
<span id="cb27-8"><a href="#cb27-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(∀</span> <span class="ot">(</span>δ <span class="ot">:</span> ⟦ Δ ⟧ℂ<span class="ot">)</span> <span class="ot">→</span> ⟦ <dfn>subst</dfn> σ tm  ⟧ δ ≡ <span class="ot">(</span>⟦ tm ⟧ <span class="ot">(</span>⟦ σ ⟧𝕊 δ<span class="ot">)))</span></span></code></pre></div>
<div class="sourceCode" id="cb28"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb28-1"><a href="#cb28-1" aria-hidden="true" tabindex="-1"></a><span class="co">-- Our set-theoretical model respects our equality judgment</span></span>
<span id="cb28-2"><a href="#cb28-2" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">:</span> <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>t <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">{</span>lhs rhs <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">}</span></span>
<span id="cb28-3"><a href="#cb28-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">→</span> <span class="ot">(</span>lhs ≡Tm rhs<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>⟦ lhs ⟧<span class="ot">)</span> ≡ <span class="ot">(</span>⟦ rhs ⟧<span class="ot">)</span></span>
<span id="cb28-4"><a href="#cb28-4" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">reflexivity</span> tm<span class="ot">)</span> <span class="ot">=</span> refl</span>
<span id="cb28-5"><a href="#cb28-5" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">symmetry</span> eq<span class="ot">)</span> <span class="kw">with</span> ⟦⟧-Soundness eq</span>
<span id="cb28-6"><a href="#cb28-6" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> rhs≡lhs <span class="ot">=</span> sym rhs≡lhs</span>
<span id="cb28-7"><a href="#cb28-7" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">transitivity</span> eq1 eq2<span class="ot">)</span> <span class="kw">with</span> ⟦⟧-Soundness eq1 <span class="cn">,</span> ⟦⟧-Soundness eq2</span>
<span id="cb28-8"><a href="#cb28-8" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> t1≡t2 <span class="cn">,</span> t2≡t3 <span class="ot">=</span> trans t1≡t2 t2≡t3</span>
<span id="cb28-9"><a href="#cb28-9" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">β-red</span> body arg<span class="ot">)</span> <span class="ot">=</span></span>
<span id="cb28-10"><a href="#cb28-10" aria-hidden="true" tabindex="-1"></a>   begin</span>
<span id="cb28-11"><a href="#cb28-11" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(λ</span> ρ <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">((λ</span> a <span class="ot">→</span> ⟦ body ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">))</span> <span class="cn">,</span> ⟦ arg ⟧ ρ<span class="ot">))</span></span>
<span id="cb28-12"><a href="#cb28-12" aria-hidden="true" tabindex="-1"></a>       ≡⟨ refl ⟩</span>
<span id="cb28-13"><a href="#cb28-13" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(λ</span> ρ <span class="ot">→</span> ⟦ body ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> ⟦ arg ⟧ ρ<span class="ot">))</span></span>
<span id="cb28-14"><a href="#cb28-14" aria-hidden="true" tabindex="-1"></a>       <span class="co">-- cong tetris using that ⟦ <dfn>Sub</dfn>.<dfn>Sub-id</dfn> ⟧𝕊 ρ ≡ ρ</span></span>
<span id="cb28-15"><a href="#cb28-15" aria-hidden="true" tabindex="-1"></a>       ≡⟨ <dfn>fun-ext</dfn> <span class="ot">(λ</span> ρ <span class="ot">→</span> cong <span class="ot">(λ</span> f <span class="ot">→</span> ⟦ body ⟧ <span class="ot">(</span>f <span class="cn">,</span> ⟦ arg ⟧ ρ<span class="ot">))</span> <span class="ot">(</span>sym <span class="ot">(</span>⟦⟧-Sub-id ρ<span class="ot">))</span> <span class="ot">)</span> ⟩</span>
<span id="cb28-16"><a href="#cb28-16" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(λ</span> ρ <span class="ot">→</span> ⟦ body ⟧ <span class="ot">(</span>⟦ <dfn>Sub</dfn><span class="ot">.</span><dfn>Sub-id</dfn> ⟧𝕊 ρ <span class="cn">,</span> ⟦ arg ⟧ ρ<span class="ot">))</span></span>
<span id="cb28-17"><a href="#cb28-17" aria-hidden="true" tabindex="-1"></a>       ≡⟨ refl ⟩</span>
<span id="cb28-18"><a href="#cb28-18" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(λ</span> ρ <span class="ot">→</span> ⟦ body ⟧ <span class="ot">(</span>⟦ <dfn>Sub</dfn><span class="ot">.</span><dfn>sub/</dfn> arg ⟧𝕊 ρ<span class="ot">))</span></span>
<span id="cb28-19"><a href="#cb28-19" aria-hidden="true" tabindex="-1"></a>       ≡⟨ sym <span class="ot">(</span><dfn>fun-ext</dfn> <span class="ot">(λ</span> δ <span class="ot">→</span> <dfn>sub-lem</dfn> <span class="ot">(</span><dfn>Sub</dfn><span class="ot">.</span><dfn>sub/</dfn> arg<span class="ot">)</span> body δ<span class="ot">))</span> ⟩</span>
<span id="cb28-20"><a href="#cb28-20" aria-hidden="true" tabindex="-1"></a>     ⟦ body <dfn>Sub</dfn><span class="ot">.</span><dfn>/[</dfn> arg ] ⟧ ∎</span>
<span id="cb28-21"><a href="#cb28-21" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">η-fn</span> fn-tm<span class="ot">)</span></span>
<span id="cb28-22"><a href="#cb28-22" aria-hidden="true" tabindex="-1"></a>   <span class="ot">=</span></span>
<span id="cb28-23"><a href="#cb28-23" aria-hidden="true" tabindex="-1"></a>   begin</span>
<span id="cb28-24"><a href="#cb28-24" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(λ</span> ρ a <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">(</span>⟦ <dfn>Ren</dfn><span class="ot">.</span>wk-Tm fn-tm ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">)</span> <span class="cn">,</span> a<span class="ot">))</span></span>
<span id="cb28-25"><a href="#cb28-25" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- using ⟦ <dfn>Ren</dfn>.wk-Tm fn-tm ⟧ ≡ ⟦ tm ⟧ ∘ proj₁</span></span>
<span id="cb28-26"><a href="#cb28-26" aria-hidden="true" tabindex="-1"></a>       ≡⟨ <dfn>fun-ext</dfn> <span class="ot">(λ</span> ρ <span class="ot">→</span></span>
<span id="cb28-27"><a href="#cb28-27" aria-hidden="true" tabindex="-1"></a>            <dfn>fun-ext</dfn> <span class="ot">(λ</span> a <span class="ot">→</span></span>
<span id="cb28-28"><a href="#cb28-28" aria-hidden="true" tabindex="-1"></a>              cong₂ <span class="ot">(λ</span> f a <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">(</span>f <span class="cn">,</span> a<span class="ot">))</span></span>
<span id="cb28-29"><a href="#cb28-29" aria-hidden="true" tabindex="-1"></a>              <span class="ot">(</span>cong <span class="ot">(λ</span> fn <span class="ot">→</span> fn <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">))</span> <span class="ot">(</span>⟦⟧-Wk-Tm fn-tm<span class="ot">))</span> refl<span class="ot">))</span> ⟩</span>
<span id="cb28-30"><a href="#cb28-30" aria-hidden="true" tabindex="-1"></a>     ⟦ fn-tm ⟧ ∎</span>
<span id="cb28-31"><a href="#cb28-31" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">𝟙-η</span> tm-𝟙<span class="ot">)</span></span>
<span id="cb28-32"><a href="#cb28-32" aria-hidden="true" tabindex="-1"></a>   <span class="ot">=</span> refl</span>
<span id="cb28-33"><a href="#cb28-33" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">var-cong</span> v≡v&#39;<span class="ot">)</span> <span class="ot">=</span> cong ⟦var⟧ v≡v&#39;</span>
<span id="cb28-34"><a href="#cb28-34" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">∙-cong</span> fn₁ fn₂ arg₁ arg₂ eq₁ eq₂<span class="ot">)</span></span>
<span id="cb28-35"><a href="#cb28-35" aria-hidden="true" tabindex="-1"></a>   <span class="kw">with</span> ⟦⟧-Soundness eq₁ <span class="cn">,</span> ⟦⟧-Soundness eq₂</span>
<span id="cb28-36"><a href="#cb28-36" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span>  ⟦eq₁⟧ <span class="cn">,</span> ⟦eq₂⟧ <span class="ot">=</span></span>
<span id="cb28-37"><a href="#cb28-37" aria-hidden="true" tabindex="-1"></a>    begin</span>
<span id="cb28-38"><a href="#cb28-38" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(λ</span> ρ <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">(</span>⟦ fn₁ ⟧ ρ <span class="cn">,</span> ⟦ arg₁ ⟧ ρ<span class="ot">))</span></span>
<span id="cb28-39"><a href="#cb28-39" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- We use <dfn>fun-ext</dfn> and then inductively use equalities on each component</span></span>
<span id="cb28-40"><a href="#cb28-40" aria-hidden="true" tabindex="-1"></a>        ≡⟨ <dfn>fun-ext</dfn> <span class="ot">(λ</span> ρ <span class="ot">→</span></span>
<span id="cb28-41"><a href="#cb28-41" aria-hidden="true" tabindex="-1"></a>          cong <dfn>eval</dfn> <span class="ot">(</span>≡-× <span class="ot">(</span>cong <span class="ot">(λ</span> f <span class="ot">→</span> f ρ<span class="ot">)</span> ⟦eq₁⟧<span class="ot">)</span> <span class="ot">(</span>cong <span class="ot">(λ</span> a <span class="ot">→</span> a ρ<span class="ot">)</span> ⟦eq₂⟧<span class="ot">)))</span> ⟩</span>
<span id="cb28-42"><a href="#cb28-42" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(λ</span> ρ <span class="ot">→</span> <dfn>eval</dfn> <span class="ot">(</span>⟦ fn₂ ⟧ ρ <span class="cn">,</span> ⟦ arg₂ ⟧ ρ<span class="ot">))</span> ∎</span>
<span id="cb28-43"><a href="#cb28-43" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">fun-cong</span> bd₁ bd₂ eq<span class="ot">)</span></span>
<span id="cb28-44"><a href="#cb28-44" aria-hidden="true" tabindex="-1"></a>  <span class="kw">with</span> ⟦⟧-Soundness eq</span>
<span id="cb28-45"><a href="#cb28-45" aria-hidden="true" tabindex="-1"></a><span class="ot">...</span> <span class="ot">|</span> ⟦eq⟧ <span class="ot">=</span></span>
<span id="cb28-46"><a href="#cb28-46" aria-hidden="true" tabindex="-1"></a>  begin</span>
<span id="cb28-47"><a href="#cb28-47" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(λ</span> ρ a <span class="ot">→</span> ⟦ bd₁ ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">))</span></span>
<span id="cb28-48"><a href="#cb28-48" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- We use <dfn>fun-ext</dfn> and then inductively use the equality on the body</span></span>
<span id="cb28-49"><a href="#cb28-49" aria-hidden="true" tabindex="-1"></a>        ≡⟨ <dfn>fun-ext</dfn> <span class="ot">(λ</span> ρ <span class="ot">→</span> <dfn>fun-ext</dfn> <span class="ot">(λ</span> a <span class="ot">→</span> cong <span class="ot">(λ</span> f <span class="ot">→</span> f <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">))</span> ⟦eq⟧<span class="ot">))</span>   ⟩</span>
<span id="cb28-50"><a href="#cb28-50" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(λ</span> ρ a <span class="ot">→</span> ⟦ bd₂ ⟧ <span class="ot">(</span>ρ <span class="cn">,</span> a<span class="ot">))</span> ∎</span></code></pre></div>
<h3 id="models-in-cartesian-closed-categories">Models in Cartesian
Closed Categories</h3>
<p>We have then a particular model using the ambient type theory (which
we think of, for the purposes here, as <code>Set</code>). More
generally, as we mentioned, this relies on nothing more than
capabilities offered by any CCC. Let us then generalize the above model
in this setting. For that purpose, we will use the rather excellent <a
href="https://github.com/agda/agda-categories">agda-categories
library</a>. We start with these imports we need from the library<a
href="#fn4" class="footnote-ref" id="fnref4"
role="doc-noteref"><sup>4</sup></a>:</p>
<div class="sourceCode" id="cb29"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb29-1"><a href="#cb29-1" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Category <span class="kw">using</span> <span class="ot">(</span>Category<span class="ot">)</span></span>
<span id="cb29-2"><a href="#cb29-2" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>BinaryProducts <span class="kw">using</span> <span class="ot">(</span>BinaryProducts<span class="ot">)</span></span>
<span id="cb29-3"><a href="#cb29-3" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Product<span class="ot">.</span>Core <span class="kw">using</span> <span class="ot">(</span>Product<span class="ot">)</span></span>
<span id="cb29-4"><a href="#cb29-4" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Exponential</span>
<span id="cb29-5"><a href="#cb29-5" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Terminal</span>
<span id="cb29-6"><a href="#cb29-6" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>CartesianClosed</span>
<span id="cb29-7"><a href="#cb29-7" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>Cartesian</span>
<span id="cb29-8"><a href="#cb29-8" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Product<span class="ot">.</span>Morphisms 𝒞 <span class="kw">using</span> <span class="ot">(</span>[<span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span>]<span class="ot">_</span>×id<span class="ot">)</span></span></code></pre></div>
<p>We assume throughout that we have a given Cartesian closed category
with object <span class="math inline">\(\mathcal{O}\)</span> and so our
development is a parameterized module fixing a particular Cartesian
closed category along with an object of the category:</p>
<div class="sourceCode" id="cb30"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb30-1"><a href="#cb30-1" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> CCCSemantics</span>
<span id="cb30-2"><a href="#cb30-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">{</span>o ℓ e<span class="ot">}</span></span>
<span id="cb30-3"><a href="#cb30-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>𝒞 <span class="ot">:</span> Category o ℓ e<span class="ot">)</span></span>
<span id="cb30-4"><a href="#cb30-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>CC-𝒞 <span class="ot">:</span> CartesianClosed 𝒞<span class="ot">)</span></span>
<span id="cb30-5"><a href="#cb30-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>𝒪 <span class="ot">:</span> Category<span class="ot">.</span>Obj 𝒞<span class="ot">)</span></span></code></pre></div>
<p>We then introduce these various shortenings for the various bits of
categorical structure that <span
class="math inline">\(\mathcal{C}\)</span> has:</p>
<div class="sourceCode" id="cb31"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb31-1"><a href="#cb31-1" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> 𝒞 <span class="ot">=</span> Category 𝒞</span>
<span id="cb31-2"><a href="#cb31-2" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> CC-𝒞 <span class="ot">=</span> CartesianClosed CC-𝒞</span>
<span id="cb31-3"><a href="#cb31-3" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> Cart <span class="ot">=</span> Cartesian CC-𝒞<span class="ot">.</span>cartesian</span>
<span id="cb31-4"><a href="#cb31-4" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> 𝒞-× <span class="ot">=</span> BinaryProducts Cart<span class="ot">.</span>products</span>
<span id="cb31-5"><a href="#cb31-5" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> 𝒞-Pr <span class="ot">(</span>A B <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">)</span> <span class="ot">=</span> Product <span class="ot">(</span>𝒞-×<span class="ot">.</span>product <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">})</span></span>
<span id="cb31-6"><a href="#cb31-6" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> 𝒞-𝟙 <span class="ot">=</span> Terminal Cart<span class="ot">.</span>terminal</span>
<span id="cb31-7"><a href="#cb31-7" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> exp <span class="ot">(</span>A B <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">)</span> <span class="ot">=</span>  Exponential <span class="ot">(</span>CC-𝒞<span class="ot">.</span>exp <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">})</span></span>
<span id="cb31-8"><a href="#cb31-8" aria-hidden="true" tabindex="-1"></a><span class="kw">module</span> Exp <span class="ot">=</span> CC-𝒞<span class="ot">.</span>exp</span>
<span id="cb31-9"><a href="#cb31-9" aria-hidden="true" tabindex="-1"></a><span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Product<span class="ot">.</span>Morphisms 𝒞 <span class="kw">using</span> <span class="ot">(</span>[<span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span>]<span class="ot">_</span>×id<span class="ot">)</span></span>
<span id="cb31-10"><a href="#cb31-10" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb31-11"><a href="#cb31-11" aria-hidden="true" tabindex="-1"></a><dfn>CC-Pr</dfn> <span class="ot">:</span> <span class="ot">{</span>A B <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">}</span> <span class="ot">→</span> Product 𝒞 A B</span>
<span id="cb31-12"><a href="#cb31-12" aria-hidden="true" tabindex="-1"></a><dfn>CC-Pr</dfn> <span class="ot">=</span> 𝒞-×<span class="ot">.</span>product</span>
<span id="cb31-13"><a href="#cb31-13" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb31-14"><a href="#cb31-14" aria-hidden="true" tabindex="-1"></a><dfn>Exp-Pr</dfn> <span class="ot">:</span> <span class="ot">{</span>A B <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">}</span> <span class="ot">→</span> Product 𝒞 <span class="ot">(</span>CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span>B^A <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">})</span> A</span>
<span id="cb31-15"><a href="#cb31-15" aria-hidden="true" tabindex="-1"></a><dfn>Exp-Pr</dfn> <span class="ot">=</span> Exp<span class="ot">.</span>product</span>
<span id="cb31-16"><a href="#cb31-16" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb31-17"><a href="#cb31-17" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>𝒞⇒</dfn><span class="ot">_</span> <span class="ot">:</span> <span class="ot">(</span>A <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>B <span class="ot">:</span> 𝒞<span class="ot">.</span>Obj<span class="ot">)</span> <span class="ot">→</span> 𝒞<span class="ot">.</span>Obj</span>
<span id="cb31-18"><a href="#cb31-18" aria-hidden="true" tabindex="-1"></a><span class="ot">_</span><dfn>𝒞⇒</dfn><span class="ot">_</span> A B <span class="ot">=</span> E<span class="ot">.</span>B^A</span>
<span id="cb31-19"><a href="#cb31-19" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb31-20"><a href="#cb31-20" aria-hidden="true" tabindex="-1"></a>  <span class="kw">module</span> E <span class="ot">=</span> exp A B</span>
<span id="cb31-21"><a href="#cb31-21" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb31-22"><a href="#cb31-22" aria-hidden="true" tabindex="-1"></a><dfn>𝒞Hom</dfn> <span class="ot">:</span> Rel 𝒞<span class="ot">.</span>Obj ℓ</span>
<span id="cb31-23"><a href="#cb31-23" aria-hidden="true" tabindex="-1"></a><dfn>𝒞Hom</dfn> <span class="ot">=</span> 𝒞<span class="ot">._</span><span class="cn">⇒</span><span class="ot">_</span></span></code></pre></div>
<p>We can then start to give our generalization of the set-theoretic
semantics. Firstly, types are interpreted inductively on their structure
as we would expect:</p>
<div class="sourceCode" id="cb32"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb32-1"><a href="#cb32-1" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧𝕋 <span class="ot">:</span> <dfn>Ty</dfn> <span class="ot">→</span> 𝒞<span class="ot">.</span>Obj</span>
<span id="cb32-2"><a href="#cb32-2" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">𝕆</span> ⟧𝕋 <span class="ot">=</span> 𝒪</span>
<span id="cb32-3"><a href="#cb32-3" aria-hidden="true" tabindex="-1"></a><span class="co">-- <span class="cn">𝟙</span> is interpreted as the terminal object</span></span>
<span id="cb32-4"><a href="#cb32-4" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">𝟙</span> ⟧𝕋 <span class="ot">=</span> 𝒞-𝟙<span class="ot">.</span>⊤</span>
<span id="cb32-5"><a href="#cb32-5" aria-hidden="true" tabindex="-1"></a><span class="co">-- Function types are interpreted as function objects</span></span>
<span id="cb32-6"><a href="#cb32-6" aria-hidden="true" tabindex="-1"></a>⟦ dom 𝕋⇒ cod ⟧𝕋 <span class="ot">=</span> ⟦ dom ⟧𝕋 <dfn>𝒞⇒</dfn> ⟦ cod ⟧𝕋</span></code></pre></div>
<p>Similarly, contexts are again interpreted as products, where an
n-arity product: <span class="math inline">\(\mathrm{X}_1 \times \cdots
\times \mathrm{X}_{\mathrm{n}}\)</span> is defined to be the
left-parenthesized iterated binary product <span
class="math inline">\(\mathrm{X}_1 \times \cdots \times
\mathrm{X}_{\mathrm{n}} := (\cdots(\mathrm{X}_1 \times \mathrm{X}_2)
\times \mathrm{X}_3)\times \ldots \times \mathrm{X}_{\mathrm{n}- 1}
)\times \mathrm{X}_{\mathrm{n}}\)</span></p>
<div class="sourceCode" id="cb33"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb33-1"><a href="#cb33-1" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧ℂ <span class="ot">:</span> <span class="cn">Ctxt</span> <span class="ot">→</span> 𝒞<span class="ot">.</span>Obj</span>
<span id="cb33-2"><a href="#cb33-2" aria-hidden="true" tabindex="-1"></a>⟦ [] ⟧ℂ <span class="ot">=</span> 𝒞-𝟙<span class="ot">.</span>⊤</span>
<span id="cb33-3"><a href="#cb33-3" aria-hidden="true" tabindex="-1"></a>⟦ <span class="ot">(</span>Γ <span class="cn">▸</span> t<span class="ot">)</span> ⟧ℂ <span class="ot">=</span> ⟦ Γ ⟧ℂ × ⟦ t ⟧𝕋</span>
<span id="cb33-4"><a href="#cb33-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb33-5"><a href="#cb33-5" aria-hidden="true" tabindex="-1"></a>  <span class="kw">open</span> 𝒞-×</span></code></pre></div>
<p>Variables are then once again iterated projections, only this time
the projection morphisms are generalized to those that exist in any CCC.
As before, we define this inductively based on the debruijn index, using
the current top type if the variable is <code>Z</code>, and otherwise
compose with <span class="math inline">\(\pi_1\)</span> and recurse:</p>
<div class="sourceCode" id="cb34"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb34-1"><a href="#cb34-1" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>ty Γ<span class="ot">}</span> <span class="ot">→</span> ty ∈ Γ <span class="ot">→</span> <span class="ot">(</span><dfn>𝒞Hom</dfn> ⟦ Γ ⟧ℂ ⟦ ty ⟧𝕋<span class="ot">)</span></span>
<span id="cb34-2"><a href="#cb34-2" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="cn">Z</span> <span class="ot">=</span> 𝒞-×<span class="ot">.</span>π₂</span>
<span id="cb34-3"><a href="#cb34-3" aria-hidden="true" tabindex="-1"></a>⟦var⟧ <span class="ot">(</span><span class="cn">S</span> pf<span class="ot">)</span> <span class="ot">=</span> ⟦var⟧ pf 𝒞<span class="ot">.</span>∘ 𝒞-×<span class="ot">.</span>π₁</span>
<span id="cb34-4"><a href="#cb34-4" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb34-5"><a href="#cb34-5" aria-hidden="true" tabindex="-1"></a>  <span class="kw">open</span> 𝒞</span>
<span id="cb34-6"><a href="#cb34-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">open</span> 𝒞-×</span></code></pre></div>
<p>We then get to our generalization of the interpretation of terms we
hinted at in the beginning:</p>
<div class="sourceCode" id="cb35"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb35-1"><a href="#cb35-1" aria-hidden="true" tabindex="-1"></a>⟦<span class="ot">_</span>⟧ <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ t<span class="ot">}</span> <span class="ot">→</span> <dfn>Tm</dfn> Γ t <span class="ot">→</span> <span class="ot">(</span><dfn>𝒞Hom</dfn> ⟦ Γ ⟧ℂ ⟦ t ⟧𝕋<span class="ot">)</span></span>
<span id="cb35-2"><a href="#cb35-2" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">var</span> v ⟧ <span class="ot">=</span> ⟦var⟧ v</span>
<span id="cb35-3"><a href="#cb35-3" aria-hidden="true" tabindex="-1"></a>⟦ <span class="ot">_</span><span class="cn">∙</span><span class="ot">_</span> <span class="ot">{</span>dom <span class="ot">=</span> d<span class="ot">}</span> <span class="ot">{</span>cod <span class="ot">=</span> c<span class="ot">}</span> rator rand ⟧ <span class="ot">=</span></span>
<span id="cb35-4"><a href="#cb35-4" aria-hidden="true" tabindex="-1"></a>  E<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ E<span class="ot">.</span>product<span class="ot">.</span>⟨  ⟦ rator ⟧ <span class="cn">,</span>  ⟦ rand ⟧ ⟩</span>
<span id="cb35-5"><a href="#cb35-5" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb35-6"><a href="#cb35-6" aria-hidden="true" tabindex="-1"></a>  <span class="kw">open</span> 𝒞</span>
<span id="cb35-7"><a href="#cb35-7" aria-hidden="true" tabindex="-1"></a>  <span class="kw">module</span> E <span class="ot">=</span> exp ⟦ d ⟧𝕋 ⟦ c ⟧𝕋</span>
<span id="cb35-8"><a href="#cb35-8" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">fun</span> <span class="ot">{</span>dom <span class="ot">=</span> d<span class="ot">}</span> <span class="ot">{</span>cod <span class="ot">=</span> c<span class="ot">}</span> body ⟧ <span class="ot">=</span> E<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧</span>
<span id="cb35-9"><a href="#cb35-9" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb35-10"><a href="#cb35-10" aria-hidden="true" tabindex="-1"></a>  <span class="kw">module</span> E <span class="ot">=</span> exp ⟦ d ⟧𝕋 ⟦ c ⟧𝕋</span>
<span id="cb35-11"><a href="#cb35-11" aria-hidden="true" tabindex="-1"></a>⟦ <span class="cn">tt</span> ⟧ <span class="ot">=</span> 𝒞-𝟙<span class="ot">.</span>!</span></code></pre></div>
<p>We note several things: - In the semantics for function application
we take a composition of the fork
<code>⟨  ⟦ rator ⟧ ,  ⟦ rand ⟧ ⟩</code> with the function
<code>E.eval</code>, this is the universal function that any function
object in a CCC has with type signature:</p>
<div class="sourceCode" id="cb36"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb36-1"><a href="#cb36-1" aria-hidden="true" tabindex="-1"></a><dfn>eval</dfn> <span class="ot">:</span> B^A×A <span class="cn">⇒</span> B</span></code></pre></div>
<ul>
<li>Similarly, functions are interpreted using the function <span
class="math inline">\(\lambda\mathrm{g}\)</span>. This is one of the
fields an Exponential object (as defined in
<code>agda-categories</code>) has, and has type:</li>
</ul>
<div class="sourceCode" id="cb37"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb37-1"><a href="#cb37-1" aria-hidden="true" tabindex="-1"></a><dfn>λg</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">(</span>X×A <span class="ot">:</span> Product X A<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>Product<span class="ot">.</span>A×B X×A <span class="cn">⇒</span> B<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>X <span class="cn">⇒</span> B^A<span class="ot">)</span></span></code></pre></div>
<p>In more conventional notation this is a function <span
class="math inline">\(\operatorname{Hom}(\mathrm{X}\times \mathcal{A},
B) \rightarrow \operatorname{Hom}(X, B^A)\)</span>. This is the usual
operation of currying, or said another way, this is the universal arrow
distinguishing the function object, wherein we can also recover the
original arrow by pairing with this arrow and using <code>eval</code>. -
The arrow <code>𝒞-𝟙.!</code> is just the universal arrow to the terminal
object.</p>
<p>We again have a substitution lemma, saying just the same as before.
We give the proof, to give the flavour that proving things in a CCC is
just the same sort of equational reasoning we would do when reasoning
about the set theoretical model:</p>
<div class="sourceCode" id="cb38"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb38-1"><a href="#cb38-1" aria-hidden="true" tabindex="-1"></a><dfn>sub-lem</dfn> <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ t<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>σ <span class="ot">:</span> <dfn>Sub</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>tm <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">)</span></span>
<span id="cb38-2"><a href="#cb38-2" aria-hidden="true" tabindex="-1"></a>      <span class="ot">→</span> ⟦ <dfn>subst</dfn> σ tm  ⟧ 𝒞<span class="ot">.</span>≈ <span class="ot">(</span>⟦ tm ⟧ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊<span class="ot">)</span></span>
<span id="cb38-3"><a href="#cb38-3" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb38-4"><a href="#cb38-4" aria-hidden="true" tabindex="-1"></a><dfn>sub-lem</dfn> σ <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> <span class="ot">=</span> ⟦var⟧-Sub σ v</span>
<span id="cb38-5"><a href="#cb38-5" aria-hidden="true" tabindex="-1"></a><dfn>sub-lem</dfn> σ <span class="ot">(</span>rator <span class="cn">∙</span> rand<span class="ot">)</span> <span class="ot">=</span></span>
<span id="cb38-6"><a href="#cb38-6" aria-hidden="true" tabindex="-1"></a>      begin</span>
<span id="cb38-7"><a href="#cb38-7" aria-hidden="true" tabindex="-1"></a>        CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ Pr<span class="ot">.</span>⟨ ⟦ <dfn>subst</dfn> σ rator ⟧ <span class="cn">,</span> ⟦ <dfn>subst</dfn> σ rand ⟧ ⟩</span>
<span id="cb38-8"><a href="#cb38-8" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- Use <dfn>lemma</dfn> to factor out ⟦ σ ⟧𝕊 from product</span></span>
<span id="cb38-9"><a href="#cb38-9" aria-hidden="true" tabindex="-1"></a>          ≈⟨ ∘-resp-≈ Equiv<span class="ot">.</span>refl <dfn>lemma</dfn> ⟩</span>
<span id="cb38-10"><a href="#cb38-10" aria-hidden="true" tabindex="-1"></a>        CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ rator ⟧ <span class="cn">,</span> ⟦ rand ⟧ ⟩ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊<span class="ot">)</span></span>
<span id="cb38-11"><a href="#cb38-11" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- Reassociate</span></span>
<span id="cb38-12"><a href="#cb38-12" aria-hidden="true" tabindex="-1"></a>          ≈⟨ sym-assoc ⟩</span>
<span id="cb38-13"><a href="#cb38-13" aria-hidden="true" tabindex="-1"></a>        <span class="ot">(</span>CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ Pr<span class="ot">.</span>⟨ ⟦ rator ⟧ <span class="cn">,</span> ⟦ rand ⟧ ⟩<span class="ot">)</span> 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊 ∎</span>
<span id="cb38-14"><a href="#cb38-14" aria-hidden="true" tabindex="-1"></a>      <span class="kw">where</span></span>
<span id="cb38-15"><a href="#cb38-15" aria-hidden="true" tabindex="-1"></a>        <span class="kw">module</span> Pr <span class="ot">=</span> CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span>product</span>
<span id="cb38-16"><a href="#cb38-16" aria-hidden="true" tabindex="-1"></a>        <span class="kw">open</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>Category<span class="ot">.</span>HomReasoning 𝒞</span>
<span id="cb38-17"><a href="#cb38-17" aria-hidden="true" tabindex="-1"></a>        <dfn>lemma</dfn> <span class="ot">:</span></span>
<span id="cb38-18"><a href="#cb38-18" aria-hidden="true" tabindex="-1"></a>          <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ <dfn>subst</dfn> σ rator ⟧ <span class="cn">,</span> ⟦ <dfn>subst</dfn> σ rand ⟧ ⟩<span class="ot">)</span></span>
<span id="cb38-19"><a href="#cb38-19" aria-hidden="true" tabindex="-1"></a>            𝒞<span class="ot">.</span>≈</span>
<span id="cb38-20"><a href="#cb38-20" aria-hidden="true" tabindex="-1"></a>          <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ rator ⟧ <span class="cn">,</span> ⟦ rand ⟧ ⟩ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊<span class="ot">)</span></span>
<span id="cb38-21"><a href="#cb38-21" aria-hidden="true" tabindex="-1"></a>        <dfn>lemma</dfn> <span class="ot">=</span></span>
<span id="cb38-22"><a href="#cb38-22" aria-hidden="true" tabindex="-1"></a>          begin</span>
<span id="cb38-23"><a href="#cb38-23" aria-hidden="true" tabindex="-1"></a>            <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ <dfn>subst</dfn> σ rator ⟧ <span class="cn">,</span> ⟦ <dfn>subst</dfn> σ rand ⟧ ⟩<span class="ot">)</span></span>
<span id="cb38-24"><a href="#cb38-24" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- Apply <dfn>sub-lem</dfn> inductively to both components</span></span>
<span id="cb38-25"><a href="#cb38-25" aria-hidden="true" tabindex="-1"></a>              ≈⟨ Pr<span class="ot">.</span>⟨⟩-cong₂ <span class="ot">(</span><dfn>sub-lem</dfn> σ rator<span class="ot">)</span> <span class="ot">(</span><dfn>sub-lem</dfn> σ rand<span class="ot">)</span> ⟩</span>
<span id="cb38-26"><a href="#cb38-26" aria-hidden="true" tabindex="-1"></a>            <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ rator ⟧ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊 <span class="cn">,</span> ⟦ rand ⟧ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊 ⟩<span class="ot">)</span></span>
<span id="cb38-27"><a href="#cb38-27" aria-hidden="true" tabindex="-1"></a>              <span class="co">-- Factor out common ⟦ σ ⟧𝕊 from product</span></span>
<span id="cb38-28"><a href="#cb38-28" aria-hidden="true" tabindex="-1"></a>              ≈⟨ Equiv<span class="ot">.</span>sym Pr<span class="ot">.</span>∘-distribʳ-⟨⟩ ⟩</span>
<span id="cb38-29"><a href="#cb38-29" aria-hidden="true" tabindex="-1"></a>            <span class="ot">(</span>Pr<span class="ot">.</span>⟨ ⟦ rator ⟧ <span class="cn">,</span> ⟦ rand ⟧ ⟩ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊<span class="ot">)</span> ∎</span>
<span id="cb38-30"><a href="#cb38-30" aria-hidden="true" tabindex="-1"></a><dfn>sub-lem</dfn> σ <span class="ot">(</span><span class="cn">fun</span> <span class="ot">{</span>dom <span class="ot">=</span> d<span class="ot">}</span> tm<span class="ot">)</span> <span class="ot">=</span></span>
<span id="cb38-31"><a href="#cb38-31" aria-hidden="true" tabindex="-1"></a>      begin</span>
<span id="cb38-32"><a href="#cb38-32" aria-hidden="true" tabindex="-1"></a>        Exp<span class="ot">.</span><dfn>λg</dfn> <dfn>CC-Pr</dfn> ⟦ <dfn>subst</dfn> <span class="ot">(</span><dfn>Sub</dfn><span class="ot">.</span><dfn>ext</dfn> σ<span class="ot">)</span> tm ⟧</span>
<span id="cb38-33"><a href="#cb38-33" aria-hidden="true" tabindex="-1"></a>          ≈⟨ Exp<span class="ot">.</span>λ-cong <dfn>CC-Pr</dfn> <span class="ot">(</span><dfn>sub-lem</dfn> <span class="ot">(</span><dfn>Sub</dfn><span class="ot">.</span><dfn>ext</dfn> σ<span class="ot">)</span> tm<span class="ot">)</span> ⟩</span>
<span id="cb38-34"><a href="#cb38-34" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- inductively rewrite <dfn>subst</dfn> (<dfn>Sub</dfn>.<dfn>ext</dfn> σ) tm</span></span>
<span id="cb38-35"><a href="#cb38-35" aria-hidden="true" tabindex="-1"></a>        CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>λg</dfn> <dfn>CC-Pr</dfn> <span class="ot">(</span>⟦ tm ⟧ 𝒞<span class="ot">.</span>∘ ⟦ <dfn>Sub</dfn><span class="ot">.</span><dfn>ext</dfn> <span class="ot">{</span>ty <span class="ot">=</span> d<span class="ot">}</span> σ ⟧𝕊<span class="ot">)</span></span>
<span id="cb38-36"><a href="#cb38-36" aria-hidden="true" tabindex="-1"></a>          ≈⟨ Exp<span class="ot">.</span>λ-cong <dfn>CC-Pr</dfn> <span class="ot">(</span>∘-resp-≈ Equiv<span class="ot">.</span>refl <span class="ot">(</span>⟦sub⟧-Wk σ<span class="ot">))</span> ⟩</span>
<span id="cb38-37"><a href="#cb38-37" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- use ⟦sub⟧-Wk to rewrite ⟦ <dfn>Sub</dfn>.<dfn>ext</dfn> σ ⟧𝕊</span></span>
<span id="cb38-38"><a href="#cb38-38" aria-hidden="true" tabindex="-1"></a>        Exp<span class="ot">.</span><dfn>λg</dfn> <dfn>CC-Pr</dfn> <span class="ot">(</span>⟦ tm ⟧ 𝒞<span class="ot">.</span>∘ [ <dfn>CC-Pr</dfn> <span class="cn">⇒</span> <dfn>CC-Pr</dfn> ] ⟦ σ ⟧𝕊 ×id<span class="ot">)</span></span>
<span id="cb38-39"><a href="#cb38-39" aria-hidden="true" tabindex="-1"></a>          ≈⟨ Equiv<span class="ot">.</span>sym <span class="ot">(</span>CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>subst</dfn> <dfn>CC-Pr</dfn> <dfn>CC-Pr</dfn><span class="ot">)</span> ⟩</span>
<span id="cb38-40"><a href="#cb38-40" aria-hidden="true" tabindex="-1"></a>          <span class="co">-- factor out component in <dfn>λg</dfn></span></span>
<span id="cb38-41"><a href="#cb38-41" aria-hidden="true" tabindex="-1"></a>        Exp<span class="ot">.</span><dfn>λg</dfn> <dfn>CC-Pr</dfn> ⟦ tm ⟧ 𝒞<span class="ot">.</span>∘ ⟦ σ ⟧𝕊 ∎</span>
<span id="cb38-42"><a href="#cb38-42" aria-hidden="true" tabindex="-1"></a>      <span class="kw">where</span></span>
<span id="cb38-43"><a href="#cb38-43" aria-hidden="true" tabindex="-1"></a>        <span class="kw">open</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>Category<span class="ot">.</span>HomReasoning 𝒞</span>
<span id="cb38-44"><a href="#cb38-44" aria-hidden="true" tabindex="-1"></a>    <dfn>sub-lem</dfn> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> <span class="ot">{</span>Δ <span class="ot">=</span> Δ<span class="ot">}</span> σ <span class="cn">tt</span> <span class="ot">=</span></span>
<span id="cb38-45"><a href="#cb38-45" aria-hidden="true" tabindex="-1"></a>      𝒞-𝟙<span class="ot">.</span>!-unique <span class="ot">(</span>𝒞<span class="ot">._</span>∘<span class="ot">_</span> <span class="ot">{</span>B <span class="ot">=</span> ⟦ Γ ⟧ℂ<span class="ot">}</span> <span class="ot">(</span>⟦<span class="ot">_</span>⟧ <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> <span class="cn">tt</span><span class="ot">)</span> ⟦ σ ⟧𝕊<span class="ot">)</span></span></code></pre></div>
<p>We rely on two lemmas, the first is just the specialized version for
the variable case<a href="#fn5" class="footnote-ref" id="fnref5"
role="doc-noteref"><sup>5</sup></a>:</p>
<div class="sourceCode" id="cb39"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb39-1"><a href="#cb39-1" aria-hidden="true" tabindex="-1"></a>⟦var⟧-Sub <span class="ot">:</span>  <span class="ot">∀</span> <span class="ot">{</span>Γ Ξ ty<span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>τ <span class="ot">:</span> <dfn>Sub</dfn> Ξ Γ<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb39-2"><a href="#cb39-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">(</span>v <span class="ot">:</span> ty ∈ Γ<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb39-3"><a href="#cb39-3" aria-hidden="true" tabindex="-1"></a>  ⟦ τ v ⟧ 𝒞<span class="ot">.</span>≈ <span class="ot">(</span>⟦var⟧ v 𝒞<span class="ot">.</span>∘ ⟦ τ ⟧𝕊<span class="ot">)</span></span></code></pre></div>
<p>The second is a weakening lemma <a href="#fn6" class="footnote-ref"
id="fnref6" role="doc-noteref"><sup>6</sup></a>:</p>
<div class="sourceCode" id="cb40"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb40-1"><a href="#cb40-1" aria-hidden="true" tabindex="-1"></a>⟦sub⟧-Wk <span class="ot">:</span> <span class="ot">∀</span> <span class="ot">{</span>Γ Δ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}{</span>ty <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>σ <span class="ot">:</span> <dfn>Sub</dfn> Δ Γ<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb40-2"><a href="#cb40-2" aria-hidden="true" tabindex="-1"></a>  ⟦ <dfn>Sub</dfn><span class="ot">.</span><dfn>ext</dfn> <span class="ot">{</span>ty <span class="ot">=</span> ty<span class="ot">}</span> σ ⟧𝕊 𝒞<span class="ot">.</span>≈ <span class="ot">(</span>[ <dfn>CC-Pr</dfn> <span class="cn">⇒</span> <dfn>CC-Pr</dfn> ] ⟦ σ ⟧𝕊 ×id<span class="ot">)</span></span></code></pre></div>
<p>This says that a weakened extension <span
class="math inline">\(\sigma\)</span> acts on a product as the product
map <span class="math inline">\(⟦ \sigma ⟧ \times  ⟦ id ⟧\)</span>, in
other words, it semantically doesn’t affect the topmost weakened
variable, and acts the same as <span
class="math inline">\(\sigma\)</span> on all other variables.</p>
<p>This is again the key lemma we need to prove soundness. Here is the
shape of the proof:</p>
<div class="sourceCode" id="cb41"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb41-1"><a href="#cb41-1" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">:</span></span>
<span id="cb41-2"><a href="#cb41-2" aria-hidden="true" tabindex="-1"></a>      <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>t <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">{</span>lhs rhs <span class="ot">:</span> <dfn>Tm</dfn> Γ t<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb41-3"><a href="#cb41-3" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span>lhs ≡Tm rhs<span class="ot">)</span> <span class="ot">→</span> <span class="ot">(</span>⟦ lhs ⟧<span class="ot">)</span> 𝒞<span class="ot">.</span>≈ <span class="ot">(</span>⟦ rhs ⟧<span class="ot">)</span></span>
<span id="cb41-4"><a href="#cb41-4" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">reflexivity</span> tm<span class="ot">)</span> <span class="ot">=</span> Equiv<span class="ot">.</span>refl</span>
<span id="cb41-5"><a href="#cb41-5" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">symmetry</span> eq<span class="ot">)</span> <span class="ot">=</span> Equiv<span class="ot">.</span>sym <span class="ot">(</span>⟦⟧-Soundness eq<span class="ot">)</span></span>
<span id="cb41-6"><a href="#cb41-6" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">transitivity</span> eq₁ eq₂<span class="ot">)</span> <span class="ot">=</span> Equiv<span class="ot">.</span>trans <span class="ot">(</span>⟦⟧-Soundness eq₁<span class="ot">)</span> <span class="ot">(</span>⟦⟧-Soundness eq₂<span class="ot">)</span></span>
<span id="cb41-7"><a href="#cb41-7" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">β-red</span> body arg<span class="ot">)</span> <span class="ot">=</span> Equiv<span class="ot">.</span>sym <span class="ot">(</span><dfn>β-lemma</dfn> body arg<span class="ot">)</span></span>
<span id="cb41-8"><a href="#cb41-8" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">η-fn</span> fn-tm<span class="ot">)</span> <span class="ot">=</span> η-lemma fn-tm</span>
<span id="cb41-9"><a href="#cb41-9" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">𝟙-η</span> tm-𝟙<span class="ot">)</span> <span class="ot">=</span> Equiv<span class="ot">.</span>sym <span class="ot">(</span>𝒞-𝟙<span class="ot">.</span>!-unique ⟦ tm-𝟙 ⟧<span class="ot">)</span></span>
<span id="cb41-10"><a href="#cb41-10" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">var-cong</span> <span class="ot">{</span>v <span class="ot">=</span> v<span class="ot">}</span> <span class="ot">{</span>v&#39; <span class="ot">=</span> v&#39;<span class="ot">}</span> v≡v&#39;<span class="ot">)</span> <span class="ot">=</span> var-lemma v v&#39; v≡v&#39;</span>
<span id="cb41-11"><a href="#cb41-11" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">∙-cong</span> fn₁ fn₂ arg₁ arg₂ eq₁ eq₂<span class="ot">)</span> <span class="ot">=</span></span>
<span id="cb41-12"><a href="#cb41-12" aria-hidden="true" tabindex="-1"></a>  begin</span>
<span id="cb41-13"><a href="#cb41-13" aria-hidden="true" tabindex="-1"></a>    CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span>product<span class="ot">.</span>⟨ ⟦ fn₁ ⟧ <span class="cn">,</span> ⟦ arg₁ ⟧ ⟩</span>
<span id="cb41-14"><a href="#cb41-14" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Apply ⟦⟧-Soundness inductively to both components</span></span>
<span id="cb41-15"><a href="#cb41-15" aria-hidden="true" tabindex="-1"></a>      ≈⟨ ∘-resp-≈</span>
<span id="cb41-16"><a href="#cb41-16" aria-hidden="true" tabindex="-1"></a>               Equiv<span class="ot">.</span>refl</span>
<span id="cb41-17"><a href="#cb41-17" aria-hidden="true" tabindex="-1"></a>               <span class="ot">(</span>Exp<span class="ot">.</span>product<span class="ot">.</span>⟨⟩-cong₂ <span class="ot">(</span>⟦⟧-Soundness eq₁<span class="ot">)</span> <span class="ot">(</span>⟦⟧-Soundness eq₂<span class="ot">))</span></span>
<span id="cb41-18"><a href="#cb41-18" aria-hidden="true" tabindex="-1"></a>       ⟩</span>
<span id="cb41-19"><a href="#cb41-19" aria-hidden="true" tabindex="-1"></a>    CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ CC-𝒞<span class="ot">.</span>exp<span class="ot">.</span>product<span class="ot">.</span>⟨ ⟦ fn₂ ⟧ <span class="cn">,</span> ⟦ arg₂ ⟧ ⟩ ∎</span>
<span id="cb41-20"><a href="#cb41-20" aria-hidden="true" tabindex="-1"></a>      <span class="kw">where</span></span>
<span id="cb41-21"><a href="#cb41-21" aria-hidden="true" tabindex="-1"></a>        <span class="kw">open</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>Category<span class="ot">.</span>HomReasoning 𝒞</span>
<span id="cb41-22"><a href="#cb41-22" aria-hidden="true" tabindex="-1"></a>⟦⟧-Soundness <span class="ot">(</span><span class="cn">fun-cong</span> bd₁ bd₂ eq<span class="ot">)</span> <span class="ot">=</span> Exp<span class="ot">.</span>λ-cong 𝒞-×<span class="ot">.</span>product <span class="ot">(</span>⟦⟧-Soundness eq<span class="ot">)</span></span></code></pre></div>
<p>Here we dispatch each non-trivial case to some specialized lemma. For
instance here is how the proof for <code>β-red</code> looks:</p>
<div class="sourceCode" id="cb42"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb42-1"><a href="#cb42-1" aria-hidden="true" tabindex="-1"></a><dfn>β-lemma</dfn> <span class="ot">:</span></span>
<span id="cb42-2"><a href="#cb42-2" aria-hidden="true" tabindex="-1"></a>  <span class="ot">{</span>Γ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>d c <span class="ot">:</span> <dfn>Ty</dfn><span class="ot">}</span> <span class="ot">→</span> <span class="ot">(</span>body <span class="ot">:</span> <dfn>Tm</dfn> <span class="ot">(</span>Γ <span class="cn">▸</span> d<span class="ot">)</span> c<span class="ot">)</span> <span class="ot">(</span>arg <span class="ot">:</span> <dfn>Tm</dfn> Γ d<span class="ot">)</span> <span class="ot">→</span></span>
<span id="cb42-3"><a href="#cb42-3" aria-hidden="true" tabindex="-1"></a>  ⟦ body <dfn>/[</dfn> arg ] ⟧ 𝒞<span class="ot">.</span>≈</span>
<span id="cb42-4"><a href="#cb42-4" aria-hidden="true" tabindex="-1"></a>  Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ Exp<span class="ot">.</span>product<span class="ot">.</span>⟨ Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧ <span class="cn">,</span> ⟦ arg ⟧ ⟩</span>
<span id="cb42-5"><a href="#cb42-5" aria-hidden="true" tabindex="-1"></a><dfn>β-lemma</dfn> <span class="ot">{</span>Γ<span class="ot">}{</span>d<span class="ot">}{</span>c<span class="ot">}</span> body arg <span class="ot">=</span></span>
<span id="cb42-6"><a href="#cb42-6" aria-hidden="true" tabindex="-1"></a>  begin</span>
<span id="cb42-7"><a href="#cb42-7" aria-hidden="true" tabindex="-1"></a>     ⟦ body <dfn>/[</dfn> arg ] ⟧</span>
<span id="cb42-8"><a href="#cb42-8" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- def of /[_]</span></span>
<span id="cb42-9"><a href="#cb42-9" aria-hidden="true" tabindex="-1"></a>         ≈⟨ Equiv<span class="ot">.</span>refl ⟩</span>
<span id="cb42-10"><a href="#cb42-10" aria-hidden="true" tabindex="-1"></a>     ⟦ <dfn>Sub</dfn><span class="ot">.</span><dfn>subst</dfn> <span class="ot">(</span><dfn>sub/</dfn> arg<span class="ot">)</span> body ⟧</span>
<span id="cb42-11"><a href="#cb42-11" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- rewrite using substitution <dfn>lemma</dfn></span></span>
<span id="cb42-12"><a href="#cb42-12" aria-hidden="true" tabindex="-1"></a>         ≈⟨ <dfn>sub-lem</dfn> <span class="ot">(</span><dfn>sub/</dfn> arg<span class="ot">)</span> body ⟩</span>
<span id="cb42-13"><a href="#cb42-13" aria-hidden="true" tabindex="-1"></a>     ⟦ body ⟧ 𝒞<span class="ot">.</span>∘ ⟦ <dfn>sub/</dfn> arg ⟧𝕊</span>
<span id="cb42-14"><a href="#cb42-14" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- definition of <dfn>sub/</dfn></span></span>
<span id="cb42-15"><a href="#cb42-15" aria-hidden="true" tabindex="-1"></a>         ≈⟨ Equiv<span class="ot">.</span>refl ⟩</span>
<span id="cb42-16"><a href="#cb42-16" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- definition of <dfn>sub/</dfn></span></span>
<span id="cb42-17"><a href="#cb42-17" aria-hidden="true" tabindex="-1"></a>     ⟦ body ⟧ 𝒞<span class="ot">.</span>∘ ⟨ ⟦ <span class="ot">(</span><span class="cn">var</span> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">})</span> ⟧𝕊 <span class="cn">,</span> ⟦ arg ⟧ ⟩</span>
<span id="cb42-18"><a href="#cb42-18" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- definition of <dfn>Sub-id</dfn></span></span>
<span id="cb42-19"><a href="#cb42-19" aria-hidden="true" tabindex="-1"></a>         ≈⟨ Equiv<span class="ot">.</span>refl ⟩</span>
<span id="cb42-20"><a href="#cb42-20" aria-hidden="true" tabindex="-1"></a>     ⟦ body ⟧ 𝒞<span class="ot">.</span>∘ ⟨ ⟦ <span class="ot">(</span><dfn>Sub-id</dfn> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">})</span> ⟧𝕊 <span class="cn">,</span> ⟦ arg ⟧ ⟩</span>
<span id="cb42-21"><a href="#cb42-21" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- rewrite ⟦ <dfn>Sub-id</dfn> ⟧ as 𝒞.id</span></span>
<span id="cb42-22"><a href="#cb42-22" aria-hidden="true" tabindex="-1"></a>         ≈⟨ ∘-resp-≈ Equiv<span class="ot">.</span>refl <span class="ot">(</span>⟨⟩-congʳ <span class="ot">(</span>⟦⟧-Sub-id <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}))</span> ⟩</span>
<span id="cb42-23"><a href="#cb42-23" aria-hidden="true" tabindex="-1"></a>     ⟦ body ⟧ 𝒞<span class="ot">.</span>∘ ⟨ 𝒞<span class="ot">.</span>id <span class="cn">,</span> ⟦ arg ⟧ ⟩</span>
<span id="cb42-24"><a href="#cb42-24" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- inverse β for function objects:</span></span>
<span id="cb42-25"><a href="#cb42-25" aria-hidden="true" tabindex="-1"></a>         <span class="co">--   rewrite ⟦ body ⟧ to Exp.<dfn>eval</dfn> ∘ λ ⟦ body ⟧ × id</span></span>
<span id="cb42-26"><a href="#cb42-26" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- In more detail, we have:</span></span>
<span id="cb42-27"><a href="#cb42-27" aria-hidden="true" tabindex="-1"></a>         <span class="co">--   g : A × B → C ≈</span></span>
<span id="cb42-28"><a href="#cb42-28" aria-hidden="true" tabindex="-1"></a>         <span class="co">--     A × B -- ĝ × id --&gt; C^B × B -- <dfn>eval</dfn> --&gt; C</span></span>
<span id="cb42-29"><a href="#cb42-29" aria-hidden="true" tabindex="-1"></a>         ≈⟨ ∘-resp-≈ <span class="ot">(</span>Equiv<span class="ot">.</span>sym <span class="ot">(</span>Exp<span class="ot">.</span>β 𝒞-×<span class="ot">.</span>product<span class="ot">))</span> Equiv<span class="ot">.</span>refl ⟩</span>
<span id="cb42-30"><a href="#cb42-30" aria-hidden="true" tabindex="-1"></a>     <span class="ot">(</span>Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ <span class="ot">(</span>[ 𝒞-×<span class="ot">.</span>product <span class="cn">⇒</span> Prod ] Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧ ×id<span class="ot">))</span> 𝒞<span class="ot">.</span>∘ ⟨ 𝒞<span class="ot">.</span>id <span class="cn">,</span> ⟦ arg ⟧ ⟩</span>
<span id="cb42-31"><a href="#cb42-31" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- reassociate</span></span>
<span id="cb42-32"><a href="#cb42-32" aria-hidden="true" tabindex="-1"></a>         ≈⟨ assoc ⟩</span>
<span id="cb42-33"><a href="#cb42-33" aria-hidden="true" tabindex="-1"></a>     Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ <span class="ot">((</span>[ 𝒞-×<span class="ot">.</span>product <span class="cn">⇒</span> Prod ] Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧ ×id<span class="ot">)</span> 𝒞<span class="ot">.</span>∘ ⟨ 𝒞<span class="ot">.</span>id <span class="cn">,</span> ⟦ arg ⟧ ⟩<span class="ot">)</span></span>
<span id="cb42-34"><a href="#cb42-34" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- definition of [_]_×id in terms of [_]_×_</span></span>
<span id="cb42-35"><a href="#cb42-35" aria-hidden="true" tabindex="-1"></a>         ≈⟨ Equiv<span class="ot">.</span>refl ⟩</span>
<span id="cb42-36"><a href="#cb42-36" aria-hidden="true" tabindex="-1"></a>     Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ <span class="ot">((</span>𝒫𝓇<span class="ot">.</span>[ 𝒞-×<span class="ot">.</span>product <span class="cn">⇒</span> Prod ] <span class="ot">(</span>Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧<span class="ot">)</span> × 𝒞<span class="ot">.</span>id<span class="ot">)</span> 𝒞<span class="ot">.</span>∘ ⟨ 𝒞<span class="ot">.</span>id <span class="cn">,</span> ⟦ arg ⟧ ⟩<span class="ot">)</span></span>
<span id="cb42-37"><a href="#cb42-37" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- rewrite using ×∘⟨⟩ i.e.</span></span>
<span id="cb42-38"><a href="#cb42-38" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- f×g∘⟨h₁,h₂⟩ ≈ ⟨f ∘ h₁, g ∘ h₂⟩</span></span>
<span id="cb42-39"><a href="#cb42-39" aria-hidden="true" tabindex="-1"></a>         ≈⟨ ∘-resp-≈ Equiv<span class="ot">.</span>refl [ 𝒞-×<span class="ot">.</span>product <span class="cn">⇒</span> Prod ]×∘⟨⟩ ⟩</span>
<span id="cb42-40"><a href="#cb42-40" aria-hidden="true" tabindex="-1"></a>     Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ Exp<span class="ot">.</span>product<span class="ot">.</span>⟨ <span class="ot">(</span>Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧<span class="ot">)</span> 𝒞<span class="ot">.</span>∘ 𝒞<span class="ot">.</span>id <span class="cn">,</span> 𝒞<span class="ot">.</span>id 𝒞<span class="ot">.</span>∘ ⟦ arg ⟧ ⟩</span>
<span id="cb42-41"><a href="#cb42-41" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- Remove composition with 𝒞.id</span></span>
<span id="cb42-42"><a href="#cb42-42" aria-hidden="true" tabindex="-1"></a>         ≈⟨ ∘-resp-≈ Equiv<span class="ot">.</span>refl <span class="ot">(</span>Exp<span class="ot">.</span>product<span class="ot">.</span>⟨⟩-cong₂ identityʳ identityˡ<span class="ot">)</span> ⟩</span>
<span id="cb42-43"><a href="#cb42-43" aria-hidden="true" tabindex="-1"></a>      Exp<span class="ot">.</span><dfn>eval</dfn> 𝒞<span class="ot">.</span>∘ Exp<span class="ot">.</span>product<span class="ot">.</span>⟨ Exp<span class="ot">.</span><dfn>λg</dfn> 𝒞-×<span class="ot">.</span>product ⟦ body ⟧ <span class="cn">,</span> ⟦ arg ⟧ ⟩ ∎</span>
<span id="cb42-44"><a href="#cb42-44" aria-hidden="true" tabindex="-1"></a>     <span class="kw">where</span></span>
<span id="cb42-45"><a href="#cb42-45" aria-hidden="true" tabindex="-1"></a>          <span class="kw">open</span> <span class="kw">import</span> Categories<span class="ot">.</span>Object<span class="ot">.</span>Product<span class="ot">.</span>Morphisms 𝒞 as 𝒫𝓇</span>
<span id="cb42-46"><a href="#cb42-46" aria-hidden="true" tabindex="-1"></a>            <span class="kw">using</span> <span class="ot">(</span>[<span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span>]<span class="ot">_</span>×<span class="ot">_;</span> [<span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span>]×∘⟨⟩<span class="ot">)</span></span>
<span id="cb42-47"><a href="#cb42-47" aria-hidden="true" tabindex="-1"></a>          Prod <span class="ot">=</span> Exp<span class="ot">.</span>product</span>
<span id="cb42-48"><a href="#cb42-48" aria-hidden="true" tabindex="-1"></a>          <span class="kw">open</span> Categories<span class="ot">.</span>Category<span class="ot">.</span>Category<span class="ot">.</span>HomReasoning 𝒞</span></code></pre></div>
<p>The main thing to appreciate about this proof is that we are just
doing equational reasoning in a Cartesian closed category. Although it
may be the case that the details are quite gnarly and unnecessarily
low-level for how we usually reason about morphisms in a CCC, none of
this is anything out of the ordinary. It is also worth saying that the
heart of the proof – which <code>agda-categories</code> calls
<code>Exp.β</code> – explicitly says how the semantic <span
class="math inline">\(\beta\)</span> reduction works, which is nothing
other than the universal property of an exponential object. Precisely
the same holds true of the η-law<a href="#fn7" class="footnote-ref"
id="fnref7" role="doc-noteref"><sup>7</sup></a> where we use the
uniqueness of the associated curried function to show the semantic <span
class="math inline">\(\eta\)</span> law holds.</p>
<h3 id="the-syntactic-category">The Syntactic Category</h3>
<p>Finally, we don’t give the full development of the syntactic category
– we don’t prove that this category is indeed Cartesian closed – but
just some flavour of how the category looks. Before we get to the full
syntactic category, we give the category of renamings that was mentioned
previously:</p>
<div class="sourceCode" id="cb43"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb43-1"><a href="#cb43-1" aria-hidden="true" tabindex="-1"></a>  <dfn>Ren-Cat</dfn> <span class="ot">:</span> Category ℓzero ℓzero ℓzero</span>
<span id="cb43-2"><a href="#cb43-2" aria-hidden="true" tabindex="-1"></a>  <dfn>Ren-Cat</dfn> <span class="ot">=</span> <span class="kw">record</span></span>
<span id="cb43-3"><a href="#cb43-3" aria-hidden="true" tabindex="-1"></a>    <span class="ot">{</span> Obj <span class="ot">=</span> <span class="cn">Ctxt</span></span>
<span id="cb43-4"><a href="#cb43-4" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span> <span class="ot">=</span> <dfn>Ren</dfn></span>
<span id="cb43-5"><a href="#cb43-5" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="ot">_</span>≈<span class="ot">_</span> <span class="ot">=</span> <span class="ot">_</span><dfn>≡Ren</dfn><span class="ot">_</span></span>
<span id="cb43-6"><a href="#cb43-6" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> id <span class="ot">=</span> <dfn>id-Ren</dfn></span>
<span id="cb43-7"><a href="#cb43-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> <span class="ot">_</span>∘<span class="ot">_</span> <span class="ot">=</span> <span class="ot">_</span><dfn>∘R</dfn><span class="ot">_</span></span>
<span id="cb43-8"><a href="#cb43-8" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> assoc <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl</span>
<span id="cb43-9"><a href="#cb43-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> sym-assoc <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl</span>
<span id="cb43-10"><a href="#cb43-10" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> identityˡ <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl</span>
<span id="cb43-11"><a href="#cb43-11" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> identityʳ <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl</span>
<span id="cb43-12"><a href="#cb43-12" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> identity² <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl</span>
<span id="cb43-13"><a href="#cb43-13" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> equiv <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">→</span> <dfn>≡Ren-equiv</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span></span>
<span id="cb43-14"><a href="#cb43-14" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> ∘-resp-≈ <span class="ot">=</span> <dfn>∘R-resp-≈</dfn></span>
<span id="cb43-15"><a href="#cb43-15" aria-hidden="true" tabindex="-1"></a>    <span class="ot">}</span></span></code></pre></div>
<p>In <code>agda-categories</code> we define a category by giving the
objects, the morphisms and an equivalence relation on the collection of
morphisms. We then have the various category laws<a href="#fn8"
class="footnote-ref" id="fnref8" role="doc-noteref"><sup>8</sup></a>,
along with a proof that the relation is a congruence with respect to
composition. For renamings we have to prove that equality of renamings
is an equivalence relation:</p>
<div class="sourceCode" id="cb44"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb44-1"><a href="#cb44-1" aria-hidden="true" tabindex="-1"></a><dfn>≡Ren-equiv</dfn> <span class="ot">:</span> <span class="ot">{</span>Γ Δ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">→</span> IsEquivalence <span class="ot">(_</span><dfn>≡Ren</dfn><span class="ot">_</span> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">}</span> <span class="ot">{</span>Δ <span class="ot">=</span> Δ<span class="ot">})</span></span>
<span id="cb44-2"><a href="#cb44-2" aria-hidden="true" tabindex="-1"></a><dfn>≡Ren-equiv</dfn> <span class="ot">=</span></span>
<span id="cb44-3"><a href="#cb44-3" aria-hidden="true" tabindex="-1"></a> <span class="kw">record</span> <span class="ot">{</span></span>
<span id="cb44-4"><a href="#cb44-4" aria-hidden="true" tabindex="-1"></a>   refl <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> refl <span class="ot">;</span></span>
<span id="cb44-5"><a href="#cb44-5" aria-hidden="true" tabindex="-1"></a>   sym <span class="ot">=</span> sym-R <span class="ot">;</span></span>
<span id="cb44-6"><a href="#cb44-6" aria-hidden="true" tabindex="-1"></a>   trans <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>r₁<span class="ot">}</span> <span class="ot">{</span>r₂<span class="ot">}</span> <span class="ot">{</span>r₃<span class="ot">}</span> eq₁ eq₂ v <span class="ot">→</span> trans <span class="ot">(</span>eq₁ v<span class="ot">)</span> <span class="ot">(</span>eq₂ v<span class="ot">)</span></span>
<span id="cb44-7"><a href="#cb44-7" aria-hidden="true" tabindex="-1"></a>   <span class="ot">}</span></span></code></pre></div>
<p>We then also need to show that this relation is a congruence for
composition of renamings:</p>
<div class="sourceCode" id="cb45"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb45-1"><a href="#cb45-1" aria-hidden="true" tabindex="-1"></a><dfn>∘R-resp-≈</dfn> <span class="ot">:</span> <span class="ot">{</span>A B C <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>f h <span class="ot">:</span> <dfn>Ren</dfn> B C<span class="ot">}</span> <span class="ot">{</span>g i <span class="ot">:</span> <dfn>Ren</dfn> A B<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb45-2"><a href="#cb45-2" aria-hidden="true" tabindex="-1"></a>   f <dfn>≡Ren</dfn> h <span class="ot">→</span> g <dfn>≡Ren</dfn> i <span class="ot">→</span> <span class="ot">(</span>f <dfn>∘R</dfn> g<span class="ot">)</span> <dfn>≡Ren</dfn> <span class="ot">(</span>h <dfn>∘R</dfn> i<span class="ot">)</span></span>
<span id="cb45-3"><a href="#cb45-3" aria-hidden="true" tabindex="-1"></a><dfn>∘R-resp-≈</dfn> <span class="ot">{</span>f <span class="ot">=</span> f<span class="ot">}{</span>h <span class="ot">=</span> h<span class="ot">}{</span>g <span class="ot">=</span> g<span class="ot">}{</span>i <span class="ot">=</span> i<span class="ot">}</span> f≡Rh g≡Ri v <span class="ot">=</span></span>
<span id="cb45-4"><a href="#cb45-4" aria-hidden="true" tabindex="-1"></a> begin</span>
<span id="cb45-5"><a href="#cb45-5" aria-hidden="true" tabindex="-1"></a>   <span class="ot">(</span>f <dfn>∘R</dfn> g<span class="ot">)</span> v</span>
<span id="cb45-6"><a href="#cb45-6" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- Rewrite f to h using f≡Rh</span></span>
<span id="cb45-7"><a href="#cb45-7" aria-hidden="true" tabindex="-1"></a>     ≡⟨ cong g <span class="ot">(</span>f≡Rh v<span class="ot">)</span> ⟩</span>
<span id="cb45-8"><a href="#cb45-8" aria-hidden="true" tabindex="-1"></a>   <span class="ot">(</span>h <dfn>∘R</dfn> g<span class="ot">)</span> v</span>
<span id="cb45-9"><a href="#cb45-9" aria-hidden="true" tabindex="-1"></a>     <span class="co">-- Rewrite g to i using g≡Ri</span></span>
<span id="cb45-10"><a href="#cb45-10" aria-hidden="true" tabindex="-1"></a>     ≡⟨ g≡Ri <span class="ot">(</span>h v<span class="ot">)</span> ⟩</span>
<span id="cb45-11"><a href="#cb45-11" aria-hidden="true" tabindex="-1"></a>   <span class="ot">(</span>h <dfn>∘R</dfn> i<span class="ot">)</span> v ∎</span>
<span id="cb45-12"><a href="#cb45-12" aria-hidden="true" tabindex="-1"></a> <span class="kw">where</span></span>
<span id="cb45-13"><a href="#cb45-13" aria-hidden="true" tabindex="-1"></a> <span class="kw">open</span> ≡-Reasoning</span></code></pre></div>
<p>Since equality of renamings only involves point-wise propositional
equality of functions, this proof is just the same as showing that if we
compose propositionally equal functions, then they are equal
pointwise.</p>
<p>The full syntactic category corresponds to the free cartesian closed
category (in our case on a single object <span
class="math inline">\(\mathcal{O})\)</span>, but also has a concrete
description as what we called <code>Sub</code>, where the objects of the
category are the contexts, and the morphisms are equivalence classes of
substitutions. Here is how that looks:</p>
<div class="sourceCode" id="cb46"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb46-1"><a href="#cb46-1" aria-hidden="true" tabindex="-1"></a><dfn>Sub-Cat</dfn> <span class="ot">:</span> Category ℓzero ℓzero ℓzero</span>
<span id="cb46-2"><a href="#cb46-2" aria-hidden="true" tabindex="-1"></a><dfn>Sub-Cat</dfn> <span class="ot">=</span> <span class="kw">record</span></span>
<span id="cb46-3"><a href="#cb46-3" aria-hidden="true" tabindex="-1"></a>  <span class="ot">{</span> Obj <span class="ot">=</span> <span class="cn">Ctxt</span></span>
<span id="cb46-4"><a href="#cb46-4" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> <span class="ot">_</span><span class="cn">⇒</span><span class="ot">_</span> <span class="ot">=</span> <dfn>Sub</dfn></span>
<span id="cb46-5"><a href="#cb46-5" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> <span class="ot">_</span>≈<span class="ot">_</span> <span class="ot">=</span> <span class="ot">_</span><dfn>≡Sub</dfn><span class="ot">_</span></span>
<span id="cb46-6"><a href="#cb46-6" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> id <span class="ot">=</span> <dfn>Sub-id</dfn></span>
<span id="cb46-7"><a href="#cb46-7" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> <span class="ot">_</span>∘<span class="ot">_</span> <span class="ot">=</span> <span class="ot">_</span><dfn>∘𝕊</dfn><span class="ot">_</span></span>
<span id="cb46-8"><a href="#cb46-8" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> assoc <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span> <span class="ot">→</span> <dfn>assoc-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span></span>
<span id="cb46-9"><a href="#cb46-9" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> sym-assoc <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span> <span class="ot">→</span> <dfn>sym-assoc-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span></span>
<span id="cb46-10"><a href="#cb46-10" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> identityˡ <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">→</span> <dfn>identityˡ-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span></span>
<span id="cb46-11"><a href="#cb46-11" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> identityʳ <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> <span class="ot">→</span> <dfn>identityʳ-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span></span>
<span id="cb46-12"><a href="#cb46-12" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> identity² <span class="ot">=</span> <dfn>identity²-proof</dfn></span>
<span id="cb46-13"><a href="#cb46-13" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> equiv <span class="ot">=</span> <dfn>≡Sub-equiv</dfn></span>
<span id="cb46-14"><a href="#cb46-14" aria-hidden="true" tabindex="-1"></a>  <span class="ot">;</span> ∘-resp-≈ <span class="ot">=</span> <dfn>∘𝕊-resp-≈</dfn></span>
<span id="cb46-15"><a href="#cb46-15" aria-hidden="true" tabindex="-1"></a>  <span class="ot">}</span></span>
<span id="cb46-16"><a href="#cb46-16" aria-hidden="true" tabindex="-1"></a>  <span class="kw">where</span></span>
<span id="cb46-17"><a href="#cb46-17" aria-hidden="true" tabindex="-1"></a>  <dfn>assoc-proof</dfn> <span class="ot">:</span> <span class="ot">{</span>A B C D <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>h <span class="ot">:</span> <dfn>Sub</dfn> A B<span class="ot">}</span> <span class="ot">{</span>g <span class="ot">:</span> <dfn>Sub</dfn> B C<span class="ot">}</span> <span class="ot">{</span>f <span class="ot">:</span> <dfn>Sub</dfn> C D<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb46-18"><a href="#cb46-18" aria-hidden="true" tabindex="-1"></a>                <span class="ot">((</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> <dfn>∘𝕊</dfn> h<span class="ot">)</span> <dfn>≡Sub</dfn> <span class="ot">(</span>f <dfn>∘𝕊</dfn> <span class="ot">(</span>g <dfn>∘𝕊</dfn> h<span class="ot">))</span></span>
<span id="cb46-19"><a href="#cb46-19" aria-hidden="true" tabindex="-1"></a>  <dfn>assoc-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> v <span class="ot">=</span></span>
<span id="cb46-20"><a href="#cb46-20" aria-hidden="true" tabindex="-1"></a>    begin</span>
<span id="cb46-21"><a href="#cb46-21" aria-hidden="true" tabindex="-1"></a>      <span class="ot">((</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> <dfn>∘𝕊</dfn> h<span class="ot">)</span> v</span>
<span id="cb46-22"><a href="#cb46-22" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Associativity of _∘𝕊_</span></span>
<span id="cb46-23"><a href="#cb46-23" aria-hidden="true" tabindex="-1"></a>        ≈⟨ ∘Sub-≡Tm <span class="ot">(</span>f v<span class="ot">)</span> g h ⟩</span>
<span id="cb46-24"><a href="#cb46-24" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span>f <dfn>∘𝕊</dfn> <span class="ot">(</span>g <dfn>∘𝕊</dfn> h<span class="ot">))</span> v ∎</span>
<span id="cb46-25"><a href="#cb46-25" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span> <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span>
<span id="cb46-26"><a href="#cb46-26" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb46-27"><a href="#cb46-27" aria-hidden="true" tabindex="-1"></a>  <dfn>sym-assoc-proof</dfn> <span class="ot">:</span> <span class="ot">{</span>A B C D <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>h <span class="ot">:</span> <dfn>Sub</dfn> A B<span class="ot">}</span> <span class="ot">{</span>g <span class="ot">:</span> <dfn>Sub</dfn> B C<span class="ot">}</span> <span class="ot">{</span>f <span class="ot">:</span> <dfn>Sub</dfn> C D<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb46-28"><a href="#cb46-28" aria-hidden="true" tabindex="-1"></a>                    <span class="ot">(</span>f <dfn>∘𝕊</dfn> <span class="ot">(</span>g <dfn>∘𝕊</dfn> h<span class="ot">))</span> <dfn>≡Sub</dfn> <span class="ot">((</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> <dfn>∘𝕊</dfn> h<span class="ot">)</span></span>
<span id="cb46-29"><a href="#cb46-29" aria-hidden="true" tabindex="-1"></a>  <dfn>sym-assoc-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>D<span class="ot">}</span> <span class="ot">{</span>h<span class="ot">}</span> <span class="ot">{</span>g<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> v <span class="ot">=</span></span>
<span id="cb46-30"><a href="#cb46-30" aria-hidden="true" tabindex="-1"></a>    begin</span>
<span id="cb46-31"><a href="#cb46-31" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span>f <dfn>∘𝕊</dfn> <span class="ot">(</span>g <dfn>∘𝕊</dfn> h<span class="ot">))</span> v</span>
<span id="cb46-32"><a href="#cb46-32" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Associativity⁻¹ of _∘𝕊_</span></span>
<span id="cb46-33"><a href="#cb46-33" aria-hidden="true" tabindex="-1"></a>        ≈⟨ <span class="cn">symmetry</span> <span class="ot">(</span>∘Sub-≡Tm <span class="ot">(</span>f v<span class="ot">)</span> g h<span class="ot">)</span> ⟩</span>
<span id="cb46-34"><a href="#cb46-34" aria-hidden="true" tabindex="-1"></a>      <span class="ot">((</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> <dfn>∘𝕊</dfn> h<span class="ot">)</span> v ∎</span>
<span id="cb46-35"><a href="#cb46-35" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span> <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span>
<span id="cb46-36"><a href="#cb46-36" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb46-37"><a href="#cb46-37" aria-hidden="true" tabindex="-1"></a>  <dfn>identityˡ-proof</dfn> <span class="ot">:</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>f <span class="ot">:</span> <dfn>Sub</dfn> A B<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb46-38"><a href="#cb46-38" aria-hidden="true" tabindex="-1"></a>                     <span class="ot">(</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> f<span class="ot">)</span> <dfn>≡Sub</dfn> f</span>
<span id="cb46-39"><a href="#cb46-39" aria-hidden="true" tabindex="-1"></a>   <dfn>identityˡ-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> v <span class="ot">=</span></span>
<span id="cb46-40"><a href="#cb46-40" aria-hidden="true" tabindex="-1"></a>     begin</span>
<span id="cb46-41"><a href="#cb46-41" aria-hidden="true" tabindex="-1"></a>       <span class="ot">(</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> f<span class="ot">)</span> v</span>
<span id="cb46-42"><a href="#cb46-42" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- Definition of <dfn>∘𝕊</dfn></span></span>
<span id="cb46-43"><a href="#cb46-43" aria-hidden="true" tabindex="-1"></a>         ≈⟨ <span class="cn">reflexivity</span> <span class="ot">((</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> f<span class="ot">)</span> v<span class="ot">)</span> ⟩</span>
<span id="cb46-44"><a href="#cb46-44" aria-hidden="true" tabindex="-1"></a>       <dfn>subst</dfn> f <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span></span>
<span id="cb46-45"><a href="#cb46-45" aria-hidden="true" tabindex="-1"></a>         <span class="co">-- Definition of <dfn>Sub-id</dfn></span></span>
<span id="cb46-46"><a href="#cb46-46" aria-hidden="true" tabindex="-1"></a>         ≈⟨ <span class="cn">reflexivity</span> <span class="ot">(</span>f v<span class="ot">)</span> ⟩</span>
<span id="cb46-47"><a href="#cb46-47" aria-hidden="true" tabindex="-1"></a>       f v ∎</span>
<span id="cb46-48"><a href="#cb46-48" aria-hidden="true" tabindex="-1"></a>     <span class="kw">where</span> <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span>
<span id="cb46-49"><a href="#cb46-49" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb46-50"><a href="#cb46-50" aria-hidden="true" tabindex="-1"></a>  <dfn>identityʳ-proof</dfn> <span class="ot">:</span> <span class="ot">{</span>A B <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>f <span class="ot">:</span> <dfn>Sub</dfn> A B<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb46-51"><a href="#cb46-51" aria-hidden="true" tabindex="-1"></a>                     <span class="ot">(</span>f <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> <dfn>≡Sub</dfn> f</span>
<span id="cb46-52"><a href="#cb46-52" aria-hidden="true" tabindex="-1"></a>  <dfn>identityʳ-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>f<span class="ot">}</span> v <span class="ot">=</span></span>
<span id="cb46-53"><a href="#cb46-53" aria-hidden="true" tabindex="-1"></a>    begin</span>
<span id="cb46-54"><a href="#cb46-54" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span>f <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> v</span>
<span id="cb46-55"><a href="#cb46-55" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Definition of <dfn>∘𝕊</dfn></span></span>
<span id="cb46-56"><a href="#cb46-56" aria-hidden="true" tabindex="-1"></a>        ≈⟨ <span class="cn">reflexivity</span> <span class="ot">((</span>f <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> v<span class="ot">)</span> ⟩</span>
<span id="cb46-57"><a href="#cb46-57" aria-hidden="true" tabindex="-1"></a>      <dfn>subst</dfn> <dfn>Sub-id</dfn> <span class="ot">(</span>f v<span class="ot">)</span></span>
<span id="cb46-58"><a href="#cb46-58" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- <dfn>Sub-id</dfn> is identity for substitution</span></span>
<span id="cb46-59"><a href="#cb46-59" aria-hidden="true" tabindex="-1"></a>        ≈⟨ subst-id <span class="ot">(</span>f v<span class="ot">)</span> ⟩</span>
<span id="cb46-60"><a href="#cb46-60" aria-hidden="true" tabindex="-1"></a>      f v ∎</span>
<span id="cb46-61"><a href="#cb46-61" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span> <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span>
<span id="cb46-62"><a href="#cb46-62" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb46-63"><a href="#cb46-63" aria-hidden="true" tabindex="-1"></a>  <dfn>identity²-proof</dfn> <span class="ot">:</span> <span class="ot">{</span>A <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb46-64"><a href="#cb46-64" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> <dfn>≡Sub</dfn> <dfn>Sub-id</dfn> <span class="ot">{</span>A<span class="ot">}</span></span>
<span id="cb46-65"><a href="#cb46-65" aria-hidden="true" tabindex="-1"></a>  <dfn>identity²-proof</dfn> <span class="ot">{</span>A<span class="ot">}</span> v <span class="ot">=</span></span>
<span id="cb46-66"><a href="#cb46-66" aria-hidden="true" tabindex="-1"></a>    begin</span>
<span id="cb46-67"><a href="#cb46-67" aria-hidden="true" tabindex="-1"></a>      <span class="ot">(</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> v</span>
<span id="cb46-68"><a href="#cb46-68" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Definition of <dfn>∘𝕊</dfn></span></span>
<span id="cb46-69"><a href="#cb46-69" aria-hidden="true" tabindex="-1"></a>        ≈⟨ <span class="cn">reflexivity</span> <span class="ot">((</span><dfn>Sub-id</dfn> <dfn>∘𝕊</dfn> <dfn>Sub-id</dfn><span class="ot">)</span> v<span class="ot">)</span> ⟩</span>
<span id="cb46-70"><a href="#cb46-70" aria-hidden="true" tabindex="-1"></a>      <dfn>subst</dfn> <dfn>Sub-id</dfn> <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span></span>
<span id="cb46-71"><a href="#cb46-71" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Definition of <dfn>subst</dfn> for <span class="cn">var</span></span></span>
<span id="cb46-72"><a href="#cb46-72" aria-hidden="true" tabindex="-1"></a>        ≈⟨ <span class="cn">reflexivity</span> <span class="ot">(</span><span class="cn">var</span> v<span class="ot">)</span> ⟩</span>
<span id="cb46-73"><a href="#cb46-73" aria-hidden="true" tabindex="-1"></a>      <span class="cn">var</span> v</span>
<span id="cb46-74"><a href="#cb46-74" aria-hidden="true" tabindex="-1"></a>        <span class="co">-- Definition of <dfn>Sub-id</dfn></span></span>
<span id="cb46-75"><a href="#cb46-75" aria-hidden="true" tabindex="-1"></a>        ≈⟨ <span class="cn">reflexivity</span> <span class="ot">(</span><dfn>Sub-id</dfn> v<span class="ot">)</span> ⟩</span>
<span id="cb46-76"><a href="#cb46-76" aria-hidden="true" tabindex="-1"></a>      <dfn>Sub-id</dfn> v ∎</span>
<span id="cb46-77"><a href="#cb46-77" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span> <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span></code></pre></div>
<p>As for <code>Ren</code>, the objects are contexts and the morphisms
are substitutions. We then have to give an equivalence relation on
morphisms. In our case, equivalence is equality of substitutions:</p>
<div class="sourceCode" id="cb47"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb47-1"><a href="#cb47-1" aria-hidden="true" tabindex="-1"></a><dfn>≡Sub-equiv</dfn> <span class="ot">:</span> <span class="ot">{</span>Γ Δ <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">→</span> IsEquivalence <span class="ot">(_</span><dfn>≡Sub</dfn><span class="ot">_</span> <span class="ot">{</span>Δ <span class="ot">=</span> Δ<span class="ot">}</span> <span class="ot">{</span>Γ <span class="ot">=</span> Γ<span class="ot">})</span></span>
<span id="cb47-2"><a href="#cb47-2" aria-hidden="true" tabindex="-1"></a><dfn>≡Sub-equiv</dfn> <span class="ot">{</span>Γ<span class="ot">}</span> <span class="ot">{</span>Δ<span class="ot">}</span> <span class="ot">=</span></span>
<span id="cb47-3"><a href="#cb47-3" aria-hidden="true" tabindex="-1"></a>  <span class="kw">record</span> <span class="ot">{</span></span>
<span id="cb47-4"><a href="#cb47-4" aria-hidden="true" tabindex="-1"></a>    refl <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">_</span> <span class="ot">→</span> E<span class="ot">.</span>refl <dfn>≡Tm-Equality</dfn></span>
<span id="cb47-5"><a href="#cb47-5" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> sym <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>r₁<span class="ot">}</span> <span class="ot">{</span>r₂<span class="ot">}</span> eq v <span class="ot">→</span> E<span class="ot">.</span>sym <dfn>≡Tm-Equality</dfn> <span class="ot">(</span>eq v<span class="ot">)</span></span>
<span id="cb47-6"><a href="#cb47-6" aria-hidden="true" tabindex="-1"></a>    <span class="ot">;</span> trans <span class="ot">=</span> <span class="ot">λ</span> <span class="ot">{</span>r₁<span class="ot">}</span> <span class="ot">{</span>r₂<span class="ot">}</span> <span class="ot">{</span>r₃<span class="ot">}</span> eq₁ eq₂ v <span class="ot">→</span> E<span class="ot">.</span>trans <dfn>≡Tm-Equality</dfn> <span class="ot">(</span>eq₁ v<span class="ot">)</span> <span class="ot">(</span>eq₂ v<span class="ot">)</span></span>
<span id="cb47-7"><a href="#cb47-7" aria-hidden="true" tabindex="-1"></a>    <span class="ot">}</span></span>
<span id="cb47-8"><a href="#cb47-8" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb47-9"><a href="#cb47-9" aria-hidden="true" tabindex="-1"></a>    <span class="kw">module</span> E <span class="ot">=</span> IsEquivalence</span></code></pre></div>
<p>Our proof that composition of substitutions is a congruence is
structurally the same, relying on analogous equational properties of
term equality, e.g. <a
href="/agda/stlc-semantics/STLC.html#SyntacticCategories.subst-cong">subst-cong</a>:</p>
<div class="sourceCode" id="cb48"><pre
class="sourceCode agda"><code class="sourceCode agda"><span id="cb48-1"><a href="#cb48-1" aria-hidden="true" tabindex="-1"></a></span>
<span id="cb48-2"><a href="#cb48-2" aria-hidden="true" tabindex="-1"></a><dfn>∘𝕊-resp-≈</dfn> <span class="ot">:</span> <span class="ot">{</span>A B C <span class="ot">:</span> <span class="cn">Ctxt</span><span class="ot">}</span> <span class="ot">{</span>f h <span class="ot">:</span> <dfn>Sub</dfn> B C<span class="ot">}</span> <span class="ot">{</span>g i <span class="ot">:</span> <dfn>Sub</dfn> A B<span class="ot">}</span> <span class="ot">→</span></span>
<span id="cb48-3"><a href="#cb48-3" aria-hidden="true" tabindex="-1"></a>  f <dfn>≡Sub</dfn> h <span class="ot">→</span> g <dfn>≡Sub</dfn> i <span class="ot">→</span> <span class="ot">(</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> <dfn>≡Sub</dfn> <span class="ot">(</span>h <dfn>∘𝕊</dfn> i<span class="ot">)</span></span>
<span id="cb48-4"><a href="#cb48-4" aria-hidden="true" tabindex="-1"></a><dfn>∘𝕊-resp-≈</dfn> <span class="ot">{</span>A<span class="ot">}</span> <span class="ot">{</span>B<span class="ot">}</span> <span class="ot">{</span>C<span class="ot">}</span> <span class="ot">{</span>f <span class="ot">=</span> f<span class="ot">}{</span>h <span class="ot">=</span> h<span class="ot">}{</span>g <span class="ot">=</span> g<span class="ot">}{</span>i <span class="ot">=</span> i<span class="ot">}</span> f≡𝕊h g≡𝕊i v <span class="ot">=</span></span>
<span id="cb48-5"><a href="#cb48-5" aria-hidden="true" tabindex="-1"></a>  begin</span>
<span id="cb48-6"><a href="#cb48-6" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span>f <dfn>∘𝕊</dfn> g<span class="ot">)</span> v</span>
<span id="cb48-7"><a href="#cb48-7" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Rewrite f to h using subst-cong</span></span>
<span id="cb48-8"><a href="#cb48-8" aria-hidden="true" tabindex="-1"></a>      ≈⟨ subst-cong g <span class="ot">(</span>f v<span class="ot">)</span> <span class="ot">(</span>h v<span class="ot">)</span> <span class="ot">(</span>f≡𝕊h v<span class="ot">)</span> ⟩</span>
<span id="cb48-9"><a href="#cb48-9" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span>h <dfn>∘𝕊</dfn> g<span class="ot">)</span> v</span>
<span id="cb48-10"><a href="#cb48-10" aria-hidden="true" tabindex="-1"></a>      <span class="co">-- Rewrite g to i using subst-≣𝕊</span></span>
<span id="cb48-11"><a href="#cb48-11" aria-hidden="true" tabindex="-1"></a>      ≈⟨ subst-≣𝕊 <span class="ot">(</span>h v<span class="ot">)</span> g≡𝕊i ⟩</span>
<span id="cb48-12"><a href="#cb48-12" aria-hidden="true" tabindex="-1"></a>    <span class="ot">(</span>h <dfn>∘𝕊</dfn> i<span class="ot">)</span> v ∎</span>
<span id="cb48-13"><a href="#cb48-13" aria-hidden="true" tabindex="-1"></a>    <span class="kw">where</span></span>
<span id="cb48-14"><a href="#cb48-14" aria-hidden="true" tabindex="-1"></a>    <span class="kw">open</span> Reasoning <span class="ot">(</span><dfn>≡Tm-setoid</dfn> A <span class="ot">(</span>type-of v<span class="ot">))</span></span></code></pre></div>
<p>From here we would want to show that this category is Cartesian
closed, and this would be the path towards showing CCCs are complete
with respect to the equational theory of STLC. What would this
categorical structure look like? The product of contexts <span
class="math inline">\(\Gamma\)</span> and <span
class="math inline">\(\Delta\)</span> is their concatenation <span
class="math inline">\(\Gamma, \Delta\)</span> (this is similar to the
Lawvere category of an algebraic theory). The unit for this category is
given by the empty context <span
class="math inline">\(\emptyset\)</span>. A function object must be an
adjoint to the product, and so would satisfy: <span
class="math display">\[
\mathrm{Sub}(X, \Delta^\Gamma) \cong \mathrm{Sub}(X \times \Gamma,
\Delta)
\]</span></p>
<p>Suppose <span class="math inline">\(\mathrm{X}= \mathrm{x}\_1, \ldots
, \mathrm{x}_{\mathrm{n}}\)</span>, <span class="math inline">\(\Gamma =
\mathrm{y}\_1, \ldots, \mathrm{y}_{\mathrm{m}}\)</span>, and <span
class="math inline">\(\Delta = \mathrm{d}_1, \ldots,
\mathrm{d}_{\mathrm{k}}\)</span>. This means that a substitution from
<span class="math inline">\(\mathrm{X}\)</span> into context <span
class="math inline">\(\Delta^\Gamma\)</span> should be given by a
sequence of terms <span
class="math inline">\(\mathrm{t}_{\mathrm{i}}\)</span> of type <span
class="math inline">\(\mathrm{d}_{\mathrm{i}}\)</span>, for each <span
class="math inline">\(\mathrm{i}\)</span>, in context <span
class="math inline">\(\mathrm{X}, \Gamma\)</span>. By repeated lambda
abstraction (the inverse of what logicians call the Deduction theorem),
we can think of giving terms of the form <span
class="math inline">\(\mathrm{X}, \Gamma \vdash \mathrm{t}_{\mathrm{i}}
: \mathrm{d}_{\mathrm{i}}\)</span>, as equivalently giving terms <span
class="math inline">\(\mathrm{X}\vdash \tilde{\mathrm{t}_{\mathrm{i}}} :
\Gamma \Rightarrow \mathrm{d}_{\mathrm{i}}\)</span>. Here <span
class="math inline">\(\Gamma \Rightarrow \mathrm{d}\)</span> is notation
for:</p>
<p><span class="math display">\[
\Gamma \Rightarrow \mathrm{d}:= \mathrm{y}_1 \Rightarrow (\mathrm{y}_2
\Rightarrow \ldots \Rightarrow (\mathrm{y}_{\mathrm{m}} \Rightarrow
\mathrm{d})\ldots)
\]</span></p>
<p>We can thus <em>define</em> the exponential as the pointwise
exponential: <span class="math display">\[
\Delta^\Gamma := \Gamma \Rightarrow \mathrm{d}_1, \ldots, \Gamma
\Rightarrow \mathrm{d}_{\mathrm{k}}
\]</span></p>
<p>In other words, a substitution <span
class="math inline">\(\mathrm{X}\rightarrow \Delta^\Gamma\)</span> is
nothing other than a sequence of curried terms for each type in <span
class="math inline">\(\Delta\)</span>. In order to complete the
development, we would need to show this has an <code>evaluation</code>
morphism, which would be a substitution of the form: <span
class="math display">\[
\Delta^\Gamma \times \Gamma \rightarrow \Delta
\]</span></p>
<p>If we mediate on this then we see the evaluation map will apply each
variable in <span class="math inline">\(\Delta^\Gamma\)</span> to each
of the variables in <span class="math inline">\(\Gamma\)</span> giving
back a term of type <span class="math inline">\(\mathrm{d}_{\mathrm{i}}
\in \Delta\)</span>.</p>
<p>That is a sketch of the Cartesian closed structure, but we leave its
full development to a future version of this post. Once we prove that it
is a CCC, then this would give us completeness: since the syntactic
category is itself a CCC model, any equality that holds in all CCC
models must already hold in the equational theory. We also note that
while we have established the necessary components for
<code>Tm _ t</code> being a presheaf on <code>Ren-Cat</code> –
<code>rename-∘R</code> and <code>id-Ren-≡</code> provide the
functoriality and identity – we have not packaged this up formally as a
presheaf on the above <code>Ren-Cat</code>, nor have we shown the
analogous result for <code>Sub-Cat</code>.</p>
<p>For more information about these ideas, Lambek and Scott’s
<em>Introduction to Higher-Order Categorical Logic</em> is an essential
resource. They vastly generalize the above by considering <span
class="math inline">\(\lambda\)</span>-theories – type theories
extending STLC with specified equations – and CCCs satisfying those
equations. They show that the syntactic category construction gives a
functor from <span class="math inline">\(\lambda\)</span>-theories to
CCCs, the internal language of a CCC gives a functor in the other
direction, and that these two functors give an equivalence of
categories.</p>
<section id="footnotes" class="footnotes footnotes-end-of-document"
role="doc-endnotes">
<hr />
<ol>
<li id="fn1"><p>This is not precisely correct, as we will see below, we
should in fact take <em>equivalence classes</em> of renamings with
respect to their equational theory<a href="#fnref1"
class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2"><p>To be found <a
href="/agda/stlc-semantics/STLC.html#Sub.Sub-id">here for Sub-id</a> and
<a href="/agda/stlc-semantics/STLC.html#Sub.∘Sub-≡">here for
∘Sub-≡</a><a href="#fnref2" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li id="fn3"><p>see <a
href="/agda/stlc-semantics/STLC.html#Semantics.SetModel.sub-lem">here</a>
for exploring the substitution lemma and <a
href="/agda/stlc-semantics/STLC.html#CCC.CCCSemantics.SubstitutionLemma.⟦⟧-Soundness">here</a>
for the proof of soundness<a href="#fnref3" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li id="fn4"><p>the full development for this section can be followed
starting <a href="/agda/stlc-semantics/STLC.html#CCC">here</a><a
href="#fnref4" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn5"><p>see <a
href="/agda/stlc-semantics/STLC.html#CCC.CCCSemantics.SubstitutionLemma.⟦var⟧-Sub">here</a>
to explore all the details on proving soundness in the variable case<a
href="#fnref5" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn6"><p>See <a
href="/agda/stlc-semantics/STLC.html#CCC.CCCSemantics.SubstitutionLemma.⟦sub⟧-Wk">here</a>
for details<a href="#fnref6" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
<li id="fn7"><p>The full proof of the <code>η-lemma</code> <a
href="/agda/stlc-semantics/STLC.html#CCC.CCCSemantics.SubstitutionLemma.η-lemma">here</a><a
href="#fnref7" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn8"><p>Anyone familiar with the typical axioms of a category
will notice that there are more laws than might be typical, this is a
trade-off we face when designing an algebraic interface in a dependent
type theory. Some of the design decisions for this interface are
explained in the <a
href="/agda/stlc-semantics/Categories.Category.Core.html#Category">agda-categories
source</a><a href="#fnref8" class="footnote-back"
role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></content>
  </entry>
</feed>
